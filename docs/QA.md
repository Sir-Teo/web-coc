# Verification record

## September 12 — Santa Trap combat and Goblin Picnic

Goblin Picnic now retains its hidden native Santa Trap, five timed 180-damage strikes, original animated components and four Ogg sounds. All first **50 of 90 native layouts** are supported and reachable under the existing alternative-dependency interpretation. The renderer preserves sack-tip shear, reconstructs effects from battle time, supports reduced motion and cleans up on seek/end/scene changes. Audio follows battle-time offsets and replay speed, with pause/mute/visibility cleanup and protection against replaying a completed sample tail. [SANTA-TRAP.md](SANTA-TRAP.md) separates pinned source facts from local engine and particle interpretations.

**887 model/asset cases have passing coverage across 72 files**, including **150 native army/layout battles**. The first full sweep passed 882 and timed out in four existing artwork tests and the 288-battle legacy campaign audit while browser and other host work ran concurrently. All five affected files then passed (nine cases) with two workers and unchanged test limits. Twenty new cases cover Santa combat, frames/geometry and audio; the added map contributes three native army cases. TypeScript and the production build pass.

**19 distinct browser scenarios have passing coverage per engine at DPR 2.** Chromium's final selection passed all 19. WebKit's initial selection passed 17; its existing desktop Payback case timed out under host load, and the first Canvas/GPU comparison exposed a WebKit downsampling difference. The Payback rerun passed unchanged, and comparing at one atlas pixel per output pixel isolated source geometry from browser downsampling filters. The final loaded/tipping/empty comparisons have mean maximum-channel error of **0.0012 / 0.0591 / 0.3892 in Chromium** and **0.1652 / 0.2495 / 0.2263 in WebKit** on the 0–255 scale, with no pixels exceeding a 25-value channel difference. This verifies three rendered source poses, not every native animation frame or engine clock.

Visual review caught and corrected inverted Mesh2D texture coordinates before the frozen runs. A browser replay fixture initially compared serialized key order; comparing complete parsed battle objects confirmed exact state equality. Actual flight/impact screenshots at 1440×960, 390×844 and 844×390 were inspected. Tests cover real map entry, concealment, damage, pause/reduced motion, mesh cleanup, decoded audio and portable replay rewind.

**Campaign production checks pass in both engines**, including the 320-second Payback replay, Pumpkin damage, 20 Obsidian defenders, and Santa's 3000→2100 P.E.K.K.A health. Santa's pre-impact seek, final result and concealment rewind pass through the shipping controls, with all five atlas pages and four Ogg files requested. Chromium repeats these flows after an offline reload. The initial new slider fixture dispatched only `input`; dispatching the matching `change` committed the seek as the real slider does. Runtime code stayed unchanged.

**General production checks also pass in both engines with no reported errors**, including boot, collection, shop, research, tab ownership, required artwork and replay controls; Chromium's offline reload, Army and replay use all 214 cached files. The first smoke attempt's short recording ended before its Pause click. The fixture now records six seconds of real scouting, and macOS Chromium explicitly uses Metal to match the other runners after the default renderer advanced only four simulation seconds during a 15-second polling interval. No game code or existing test timeout was relaxed for these fixes. WebKit offline, Linux-hosted CI and physical-device performance remain unverified.

The frozen runtime SHA-256 is `46556bed360427b63389851a9e633c906a710052e1bbca3a64c492640c2ef6cd` across 75 TypeScript/CSS files. Eleven native assets remain **4,004,922 bytes**, unchanged from the asset milestone. The build precaches **214 files** in `crown-clan-2df11a1a800e`. Evidence is under `output/playtest/santa-combat-*`, `santa-model-timeout-recheck.log`, `santa-webkit-recheck.log`, `santa-campaign-production-report.json` and the `santa-flight-*` / `santa-impact-*` screenshots.

Native spell level selection, trap-action clock interpretation, allegiance, scatter RNG, particle units, altitude/flight projection and `ScaleTimeline` remain unverified against the native engine. Explosion particles still use local graphics. Later campaign mechanics, wider native-art replacement, online systems and physical-device validation remain required; the full production-clone goal is active.

## September 12 — native Santa Trap asset foundation

The pinned client now supplies the original gift-box reveal, flying hogs and sleigh, loaded/empty Santa poses, three falling-gift variants, smoke/debris/shadows and four unchanged Ogg sounds. Eleven assets total **4,004,922 bytes**. Source trap/spell/effect/particle rows and hashes are preserved in [reference/santa-trap](../reference/santa-trap/README.md), separately from the compact runtime data. **Combat is not integrated: Goblin Picnic remains unavailable and campaign support remains 49 of 90.**

The sleigh keeps its complete 320-frame scene graph, including native alpha, component changes and sack-tipping shear. An initial full-frame bake needed about 13 MB; retaining its three animated components reduces the sleigh atlas to about 2.8 MB without lowering source resolution. Recursive comparison verifies all **2040 source draw commands** and their transforms/colors/phases. Eight independent Python format/atlas checks pass, and the Santa importer reproduces all pixels, Ogg bytes and both references. Pumpkin and Skeleton imports still reproduce their prior pixels and metadata.

**864 model/asset tests pass in 69 files**, including five new Santa source/geometry/pixel/audio cases and the unchanged 147 native army/layout battles. TypeScript and the production build pass. **Two browser asset scenarios pass per engine in Chromium and WebKit at DPR 2**: every image and sound decodes, and the loaded/tipping/empty native poses render. Source sprites, present variants and the final browser pose capture were visually reviewed. A test-loader failure from a missing Node JSON import attribute was corrected before these passing runs. Import validation also caught a seven-frame shear that a translation-only compact track would have lost; the final data retains all six affine values.

**General production checks pass in both engines with no reported browser errors.** Chromium reload, Army and replay work offline with **214 cached files**, cache `crown-clan-58371d638dd6`; WebKit offline remains untested. These are existing game flows, not Santa combat validation. Runtime TypeScript/CSS remains unchanged at SHA-256 `d18455ab4e2887148f81ad31734af1a97510272dc1c98e805cfff07832684c34` across 71 files. Evidence is under `output/playtest/santa-*`, including source pose captures, root-track comparison, reproduction checks, model/build and browser logs.

Next work is trap-to-spell integration, timing, deterministic hit placement, allegiance, native particle/sound playback and campaign/replay verification. Native engine clocks, particle units, level selection, GPU pixel identity and physical-device quality remain unverified. Source values and inferred behavior must stay distinguishable; the full production-clone goal remains active.

## September 12 — native Skeleton Trap tiers and Obsidian Tower

Skeleton Traps now use original client coffin artwork for levels 1–4, including ground/air badges, reinforced dark wood at level 3, native opening animation and an unarmed pose. The importer reconstructs both 43-frame trigger clips per tier at 24 fps, retaining 35 unique atlas cells per tier at two pixels per native coordinate unit. Eight native PNGs replace the three generated coffin WebPs. All eight images and metadata reproduce with identical decoded pixels; the unchanged native Pumpkin atlas also reproduces. Six strict-reader tests pass, including polygon cutouts and half-opacity shared edges. Generated defending-character atlases still reproduce byte-for-byte. [The native reference](../reference/skeleton-trap/README.md) records source ownership, hashes and interpretation limits.

Native levels 3 and 4 spawn four and five level-one skeletons respectively. The TH8 home ceiling stays at level 2, and later upgrade requirements display TH9/TH10. Obsidian Tower retains all five original level-three traps, supporting 20 defenders and bringing coverage to **49 of 90 layouts**. A progression closure check confirms stages **1–37** are reachable under the current alternative-dependency interpretation; SantaTrap in Goblin Picnic is the next missing mechanic. Combat version 29 rejects earlier schemas carrying the newly supported trap levels and preserves deterministic spawn origins.

**859 model/asset tests in 68 files pass**, including all **147 supported native army/layout battles**. They cover later upgrade gates and prices, every scheduled ground/air spawn boundary, continued spawning after target death, distinct origins, Obsidian's exact final replay state, repeated seeking, home-state isolation and malformed/older snapshot rejection. TypeScript and production build checks pass.

**19 distinct browser scenarios pass per engine in Chromium and WebKit at DPR 2.** Coverage includes both coffin tiers and badges, home placement/mode/Info/save flows, Obsidian Tower at 1440×960, 390×844 and 844×390, native frames, 20 defenders, reduced motion, portable replay state equality and concealment restoration. Existing native campaign and Pumpkin scenarios also pass. Each first sweep passed 16 and failed three new assertions because serialized object key order differed; comparing the parsed battle objects fixed the fixture without changing runtime code. The corrected four feature cases then passed in each engine. Visual review also corrected a gallery capture taken before the renderer updated and removed selection-panel occlusion from that capture; the final gallery case passed in each engine. These are passing scenario unions, not uninterrupted clean first sweeps.

**General and campaign production checks pass in both engines with no reported browser errors.** Shipping UI imports and seeks an Obsidian replay with the exact 20-defender state, then rewinds to concealed coffins and no defenders. Existing 320-second Payback and Pumpkin replay checks still pass. Chromium repeats the campaign flows offline and general offline reload/Army/replay succeeds with **203 cached files**, cache `crown-clan-72033a42d937`. WebKit offline remains untested.

Runtime source stayed frozen throughout browser and production checks at SHA-256 `d18455ab4e2887148f81ad31734af1a97510272dc1c98e805cfff07832684c34` across 71 files. Evidence: `output/playtest/native-skeleton-verification.json`, `native-skeleton-*-production-report.json`, the `native-skeleton-*.log` files, `native-skeleton-tiers-*.png` and `native-obsidian-*.png`. Native coffin poses, phone combat and gallery captures were visually reviewed.

Native GPU pixel matching, character directional art, particles/sound, exact coffin disappearance timing, combat targeting rules, 41 unsupported campaign layouts, online systems and physical-device qualification remain unfinished. The coffin hides at clip completion by local interpretation; the source clip's final frame still contains the open coffin. The full production-clone goal remains active.

## September 12 — native Pumpkin Bomb artwork and campaign combat

Rat Valley, Brute Force, Bouncy Castle and Full Frontal now retain all ten native Pumpkin Bomb placements. This raises supported layouts from 44 to **48 of 90** and makes stages 1–26 reachable through the current progression rules. The next missing mechanic is level-three Skeleton Traps in Obsidian Tower. Replay version 28 preserves the seasonal identity independently of the normal Bomb archetype; home saves and practice reject it.

The source importer reconstructs the original setup sprite and all 44 trigger frames from pinned SC6/SCTX assets. Nested fuse sparks animate, source transforms share one registration, and the final atlas is rasterized at two pixels per native coordinate unit to retain texture detail without enlarging its world footprint. The strict reader's five adversarial/pixel tests pass; the 45-frame atlas and campaign reference reproduce exactly. Source ownership, hashes and interpretation limits are documented in [reference/pumpkin-bomb](../reference/pumpkin-bomb/README.md).

**847 model/asset tests in 67 files pass**, including all 144 supported native army/layout battles. New checks cover exact ground-trigger and splash boundaries, 25 damage, air/dead-target immunity, escaped targets, one-shot resolution, concealment, passability, destruction exclusion, replay export/rewind, malformed identities and older-version rejection. The two-second fuse is explicitly an inference from action frame 48 and 24 fps; the source animation has 44 frames. Native action-counter and nested playback semantics remain unverified.

**Eight final browser cases pass per engine in Chromium and WebKit at DPR 2.** They exercise native Payback/save/replay/scenery flows, Rat Valley at 1440×960, 390×844 and 844×390, all four enabled campaign buttons, decoded texture alpha, fixed registration, clock-driven frames, paused/reduced-motion poses, damage and shared-replay concealment restoration. The initial one-pixel-per-native-unit export passed functionality but visibly discarded texture detail; the denser export passed the final runs on an unchanged runtime. The atlas and phone rendering were reviewed visually. Early test authoring failures were a wrong fixture method name and a Node JSON import attribute; both were corrected before the passing runs.

**General and campaign production checks pass in both engines with no reported browser errors.** They cover actual native asset requests, a shared Rat Valley replay with the Giant's exact 375 remaining HP, rewind to hidden scouting, long Payback replay/overflow and saved inventory reload. Chromium repeats these flows offline with 198 precached files; WebKit offline remains untested. Source SHA-256 is `a641e9ed848e238dc3a0370ac2d7cd0fe8bb942a5b6e8493afd24a25024a1309` across 71 runtime files; cache is `crown-clan-529b162af4b5`. Evidence: `output/playtest/pumpkin-verification.json`, the `pumpkin-*-final.log` files and `pumpkin-native-*` / `pumpkin-replay-*` captures.

Explosion particles, sound, spent opacity and world-scale calibration still use local interpretation. Native GPU pixel identity, remaining campaign mechanics, complete production readiness and physical-device qualification remain unfinished.

## September 12 — native campaign integration

The campaign screen now lists the pinned native 90-village catalog. Forty-four layouts are mechanically supported, including the full 430-entity Sherbet Towers. The other 46 are explicitly unavailable; Halloween bombs currently block the early route at Rat Valley. Native positions, levels, HP, scenery IDs and combat passability are preserved. Separate native stars and finite-resource records protect prior authored campaign progress. Replay version 27 carries catalog identity, scenery and larger layouts. See [CAMPAIGN-RULES.md](CAMPAIGN-RULES.md) for exact coverage and unresolved interpretation/art gaps.

**825 model tests in 65 files pass**, including 132 native battles, full-layout/replay export checks, malformed progress/history isolation and a deterministic path digest. A stable A* heap fixes severe CPU overhead in two native resource-army scenarios while preserving route ordering. The generated 16-sprite scenery set rebuilds byte-for-byte, and the importer verifies 97 pinned source hashes.

**Twelve final affected browser scenarios pass in Chromium and WebKit at DPR 2.** They cover all 90 thumbnails with concealed Teslas/traps, native Payback wins at 1440×960, 390×844 and 844×390, exact loot/star persistence, overflow, untimed scouting, long/shared replays, Goblin Huts, large-map scenery and home transitions. The Chromium scenery case was rerun after its test was corrected to wait for the renderer. Earlier broader checks passed 31 Chromium scenarios; the WebKit sweep exposed a slider focus race, now covered by the passing final replay tests. A focused regression failed before the fix and passed afterward: live HUD updates must not replace a pointer-held range value merely because it lacks keyboard focus.

**General and native campaign production checks pass in both engines with empty browser-error reports.** They import a native save, reload finite inventory, seek and share a 320-second replay, and confirm exact Payback building/scenery identity. Chromium repeats these flows offline; WebKit offline was not checked. Full-storage overflow reproduces exactly (207 gold, 155 elixir lost; zero credited). The evidence manifest is `output/playtest/native-campaign-verification.json`.

Runtime source is frozen at SHA-256 `5d270cccdd49693171c9e82382e55145ab381d5df763c35ff0b668056e5cf8a2` (70 files), with runtime reference data SHA-256 `f10671df15523b5d56c82d59189a344d085281bf7e36147456c2002919849996`. The build precaches 197 files in `crown-clan-85b043927fbb`. Evidence lives under `output/playtest/native-*`, with the slider regression under `replay-drag-regression-{before,after}.log`.

## September 11 — Blacksmith, ore upgrades and equipped King combat

The TH8 Blacksmith now supports Barbarian Puppet, Rage Vial and Earthquake Boots through level 9, two distinct equipment slots, three ore balances/caps and native missing-ore gem purchases. Stats, recovery, rage and summon waves come from the selected items and levels. Boots delivers five scheduled percentage-damage pulses, clears walls, damages eligible ground defenders and retains its cast position. [BLACKSMITH.md](BLACKSMITH.md) records immutable native data, field interpretations and remaining gaps.

**666 model/asset tests pass across 59 files**, including 18 new cases. They cover every upgrade price, tiered bonuses, building gates, construction deadlines, instant upgrades with busy builders, exact ore/gem deductions, repeated/stale confirmation, swaps, legacy and malformed saves, all four Boots ability tiers, footprint boundaries, wall/trap/Tesla/air rules, defender birth times, summoned boosts, portable replay snapshots/seeks and asset alpha/matte checks. TypeScript and the production build pass.

**28 browser scenarios pass per engine** in uninterrupted final Chromium/Metal and WebKit runs at 2× density. Six new cases per engine exercise equipment selection, ore payment, gem confirmation/cancellation, saved reloads at 1440×960, 390×844, 320×740 and 844×390, locked/insufficient states, sprite registration and quake pause/fade/cleanup. Existing Hero Hall, King art/combat, and replay import/export/seeking scenarios also pass. Desktop/phone panels, gem confirmation, building placement and quake captures were visually reviewed. The review corrected low-contrast labels and filtered green edge spill.

**General, hero and Blacksmith production checks pass in both engines with empty browser-error reports.** The Blacksmith runner imports a validated fixture using shipping UI, upgrades Puppet from 1→2 using ore and 2→3 with a 340-gem shortfall, equips Boots in place of Vial, reloads, deploys and activates the King, waits for all 16 summons, and watches the replay. The gem balance stays 660 throughout replay. Chromium also performs the reload/combat/replay flow offline; WebKit offline was not tested. The manifest holds **179 files**, cache `crown-clan-0a1a0074eaa9`.

Seven generated artwork files total **446,902 bytes**. The building has preserved generated alpha; equipment and ore icons use a reviewed matte extraction and deterministic registration. Exact built-in imagegen prompts, source/output hashes and saved paths are in [the artwork manifest](../art/source/blacksmith-v1/README.md). CI verifies asset reproduction and includes the new browser and production scenarios.

Final runtime source SHA-256: `8e06b9cc58473b097372118e7c855699e02adc562a42bbed6385478d51ea6919` (65 files). Runtime source and shipped assets stayed fixed throughout final browser/production verification. Evidence: `output/playtest/blacksmith-verification.json`, `blacksmith-source.json`, `blacksmith-evidence.py`, `blacksmith-{models,assets,build,chromium,webkit}-final.log`, `blacksmith-production-report.json`, `blacksmith-{hero,general}-production-report.json`, `blacksmith-panel-*.png`, `blacksmith-purchase-*.png`, `blacksmith-quake-*.png`, and `production-blacksmith-*.png`.

Save version stays 4; combat version is 24, expiring old playback while preserving its result records. Normal ore rewards, later Blacksmith/equipment tiers, other/defending heroes, native animation/timing/rounding, online systems and physical-device checks remain unfinished. The full production-clone goal stays active.

## September 11 — King portrait and four directional animation sets

The King now shares a closer default-character identity across his Hero Hall portrait, battle card and combat sprites. Four authored quarter views preserve the sword hand and armored side without mirroring. Each has idle, four walking poses and four attack poses; the renderer follows navigation/target direction and the battle clock. The old static-image bob and attack tilt are removed for the King. [KING-ART.md](KING-ART.md) records the official visual reference, exact built-in prompts, accepted/rejected source cells, importer and remaining fidelity gaps.

**648 model/asset tests pass across 56 files.** Seven new cases verify 36 distinct transparent frames with safe margins and no retained chroma, the versioned portrait, navigation and target-facing quarters, straight-axis stability, four strides, reduced motion and cooldown-aligned attack poses. The unchanged **288-battle campaign matrix** also passes; it does not deploy heroes and is not a hero-inclusive balance audit. TypeScript, the production build and byte-for-byte rebuilding of all five new derivatives pass.

**38 browser scenarios pass in each of Chromium with Metal and WebKit at 2× density**, in uninterrupted final runs. All six new cases verify actual decoded texture pixels, the portrait/panel at 1440px/390px/320px, all four views and nine poses, equipment-preserving orientation, stable runtime anchors, pause, reduced motion, directional casualty reconstruction, repeated replay seeks and cleanup. Existing King combat/equipment, troop movement/animation, defeat, late-troop and replay scenarios pass as part of the selection. The panel, atlas contact sheet, rear pose, defeat, replay and production battle captures were visually reviewed.

Source review rejected baked checkerboard, repeated strides and swapped sword/gauntlet limbs. The rear-right attack strip was regenerated separately after the sheet correction still swapped its equipment. Import review corrected a Sharp composition/crop ordering error, filtered matte spill, sword-safe source cuts, foot registration and a small incoming blade fragment in a neighboring cell's gutter. These repairs preceded final verification. Nine original outputs and exact prompts are retained in art commit `2b5e12d`; the five runtime files total **1,223,036 bytes**.

**General and hero-specific production checks pass in both engines without reported browser errors.** All five new files are required production requests. Hero save import, upgrades, deployment, equipment activation, both summon waves and replay remain functional. Chromium also reloads, opens Army and watches a replay offline, retaining the upgraded King for offline combat. The offline manifest contains **172 files**, cache `crown-clan-cbfcc3b3f7cb`. WebKit offline behavior was not exercised.

Final runtime source SHA-256 is `2fb7af3e88f95fbe286fbdb14a11951d85ad8c83ca33deb388b87785662792bb` (63 files); it and all five asset hashes stayed unchanged during final browser/production checks. Evidence: `output/playtest/king-art-verification.json`, `king-art-source.json`, `king-art-{models,build,assets,chromium,webkit,production,hero-production}-final.log`, `king-art-campaign.json`, `king-art-production-report.json`, `king-art-hero-production-report.json`, `king-art-contact.png`, `king-art-panel-*.png`, `king-art-{rear-attack,defeat,replay}-*.png` and `production-hero-battle-*.png`.

Save version remains 4 and combat version 23. These four generated views are not pixel-identical native assets; intermediate angles, exact gait/action timing, first-hit windup, skins/level variants and dedicated defeat/ability sequences remain open. Physical-device quality, full content/equipment and online systems remain unfinished. The full production-clone goal is active.

## September 11 — King battle instructions

The selected King's hint now follows his actual state: deployment boundary, available activation, used ability and defeat. Passive health updates refresh the hint together with the card, so a defeated King no longer retains deployment instructions. The corrected phone screenshot was visually inspected.

Both affected desktop/touch cases pass in Chromium with Metal and WebKit at 2× density. The touch case checks all four hint states, including defeat without a full HUD render. TypeScript/build and the production hero checks pass again in both engines, with Chromium offline upgrade persistence, deployment, activation and replay. No model rules changed; the preceding 641 model/asset tests and 36-case browser selections remain the broader coverage.

Runtime source SHA-256 is `95bce800de258a9a4d79546a8bf3b4455c3d919e64cc217219ffe8c5d41e177e`, unchanged throughout this follow-up's checks. The rebuilt cache is `crown-clan-1580345b760e`, with 167 files. Evidence: `output/playtest/king-hint-verification.json`, `king-hint-source.json`, `king-hint-{build,chromium,webkit,production}.log`, `king-hint-production-report.json`, and the updated `king-early-ability-*.png` and `production-hero-battle-*.png` captures.

## September 11 — native King progression and default equipment

The King now uses twenty native base-stat and upgrade records, early Town Hall scaling, and fixed level-1 Barbarian Puppet/Rage Vial equipment. Both items activate from TH4. Eight researched Barbarians spawn in two timed waves, with independent boost deadlines; automatic activation occurs on lethal damage. The Hero Hall panel shows equipped totals, attack timing, movement, recovery and item effects. See [KING-COMBAT.md](KING-COMBAT.md) for immutable source URLs/hashes and the native-engine behavior still unverified.

**641 model/asset tests pass across 55 files**, including twenty new King cases. Coverage includes all supported progression records, upgrade payment/deadlines/reload, legacy paid upgrades, early scaling, passive bonuses, fixed recovery, lethal activation/overkill, summon birth times, researched levels, owner death, damage/movement boosts, spell interactions, and exported replay reconstruction. Delayed projectile, destruction-bomb and spell effects cannot affect a summon before its scheduled birth. The final TypeScript check and production build pass.

The **288-battle campaign matrix** passes, retaining a veteran three-star approach to every stage (47 three-star results and one zero-star result across all veteran approaches). The matrix does not deploy heroes; these results validate the existing campaign, not hero-inclusive balance.

**36 distinct browser scenarios pass per engine** in Chromium with Metal and WebKit at 2× density. All five new feature scenarios pass in both engines: native upgrade/stat/save details at 1440px/390px/320px, TH4 touch activation and upgrade gating, timed waves, gold boost tint, pause, reduced motion, repeated replay seeking and cleanup. Existing hero, spell, trap, Skeleton Trap and replay scenarios also pass. Screenshots of the panel, equipment, summon wave and early ability were visually inspected, including the final production captures.

The final Chromium selection passed all 36 cases. WebKit passed 35 initially; the desktop replay-drag fixture attempted to drag before the slider was re-enabled after seeking. After adding an enabled-control wait, both desktop and phone drag cases passed on unchanged game source, completing the 36-case union. An earlier new-feature fixture also needed the renderer's normal sync after a manual seek; that fixture correction preceded the final runs. These are recorded as corrected test runs, not an uninterrupted clean broad WebKit run.

**Production checks pass in both engines without reported browser errors.** The independent hero runner creates a validated save through the model, owns its production preview, and uses shipping UI controls to import, upgrade, deploy, activate, surrender and watch the replay. Both engines reconstruct all eight summons. Chromium also retains the upgraded King and completes combat/replay after an offline reload; WebKit offline behavior was not exercised. General production checks cover boot, menus, artwork, collection, tab handoff and replay. Offline Chromium uses **167 cached files**, cache `crown-clan-c8fa258b6bbb`.

Final runtime source SHA-256 is `d17707c0ff0982e5cb9f2ad510a47346666d17915461ea03b761f46d891cc679` (62 files), unchanged during final browser and production runs. Evidence: `output/playtest/king-verification.json`, `king-source-final.json`, `king-models-final.log`, `king-campaign.json`, `king-build-final.log`, `king-chromium-final.log`, `king-webkit-final.log`, `king-webkit-seek.log`, `king-production-report.json`, `king-hero-production-report.json`, `king-panel-*.png`, `king-equipment-*.png`, `king-puppet-wave-*.png`, `king-early-ability-*.png`, and `production-hero-*.png`.

Combat version is 23; save version remains 4. Equipment selection/upgrading, Blacksmith/ores, defending heroes and later heroes remain unfinished. Existing King artwork is reused and is not pixel matched. Native action timing/targeting/rounding, full content, online systems and physical-device quality remain open; the production-clone goal is incomplete.

## September 11 — Skeleton Trap WebKit follow-up

All **44 remaining WebKit scenarios pass** at 2× density on committed gameplay source `220b0df`, using the normal 30-second timeout. Together with the 24 scenarios already verified, both Chromium and WebKit now have all **68 distinct passing scenarios** from the Skeleton Trap regression selection. This closes the interrupted coverage recorded below. Runtime source stayed at SHA-256 `4faa04b4de79e585904868b19dc60c5048b8f7894de18127b2b0c09860d85540`; no gameplay changes were made during the run. Machine load had eased before the follow-up.

Evidence: `output/playtest/skeleton-webkit-completion.json`, the exact selection in `skeleton-webkit-pending.json`, and the updated `skeleton-verification.json`. The full game, remaining native behavior and physical-device quality are still incomplete.

## September 11 — Skeleton Traps and defending units

Skeleton Traps now have two TH8 levels, persistent ground/air modes, timed spawning and separate defending units. Skeletons pursue eligible attackers, jump their own walls and draw retaliation from eligible troops. Projectile and spell allegiance is explicit. Wizard fireballs now use native five-tile/s fixed-point flight. See [SKELETON-TRAP.md](SKELETON-TRAP.md) for immutable client sources, exact values and remaining behavioral approximations.

**621 model/asset tests pass across 54 files** with `npx vitest run --maxWorkers=2`; the final TypeScript check and production build pass. Twenty-nine new simulation/save/replay cases cover mode/layer eligibility, trigger boundaries, sequential spawning after target loss, exact scheduled times across wide frames, spawn idle, cadence, point-target approaches, wall jumping, target priorities, retaliation, projectile tracking and misses, splash allegiance, spell interactions, attack exhaustion, finish guards, saved layouts and portable replay equality/seeking. Three asset cases verify distinct alpha states/poses and clear atlas margins.

The **288-battle campaign matrix** passes with one trap in each mode on stages 8–12. Every stage retains a veteran three-star route. Veteran results are 47 three-star approaches and one zero-star approach (final fortress from the south, 32%). Healer/P.E.K.K.A and Dragon armies each clear all 48 approaches. This checks the authored local campaign, not multiplayer balance.

**All seven new browser scenarios pass in Chromium with Metal and WebKit at 2× density.** They cover Shop gating, every texture, instant touch placement, the two-trap limit, the five-hour upgrade, mode changes/Info/layout/reload at 1440px/390px/320px, coffin states, six poses for each defender mode, ground/air hit anchors, pause stability, marker layering, replay reconstruction, reduced motion and cleanup. The UI audit corrected the Shop category; coffin opening now follows actual spawn state instead of a floating-point time comparison. Visual review caught health markers hidden by airborne sprites; a retained layer above troops now keeps the red bars and skulls visible. Final screenshots were inspected, including the battle close-up.

**Chromium has 68 distinct passing scenarios in this pass. WebKit has 24, including all seven feature cases.** The expanded WebKit rerun is incomplete: during heavy workstation load, two older defeat scenarios last timed out in setup, and the broad batch was stopped with further scenarios unrun. These are recorded as pending coverage, not passes. Chromium's four earlier setup timeouts passed when rerun separately. The final marker change is covered by eleven Chromium cases, all seven WebKit feature cases and both production smoke checks. The source fingerprints and exact scenario unions are in `output/playtest/skeleton-verification.json`.

The initial model run under load exposed two test-harness limits. The 288-battle functional matrix now has a 60-second timeout; it is not a frame-time benchmark. The replay speed test controls monotonic time so speed conversion is independent of the 8ms execution budget, which remains covered separately. Final model validation used two workers. No runtime time budget was relaxed.

The final production build passes Chromium and WebKit with no reported browser errors. All five new WebPs load. Chromium reloads, opens Army and watches a replay offline with **167 cached files**, cache `crown-clan-927fa46e8846`. Save version remains 4; combat version is 22. Original PNGs and exact built-in prompts are preserved in art commit `41b544f`; deterministic rebuilding of the five derivatives (223,482 bytes total) passes. Glow/checkerboard and clipped-frame candidates were rejected before acceptance.

Evidence: `output/playtest/skeleton-verification.json`, `skeleton-source-final.json`, `skeleton-models-final.log`, `skeleton-campaign.json`, `skeleton-build-final.log`, `skeleton-chromium{,-final}.log`, `skeleton-webkit{,-final,-feature}.log`, `skeleton-production-report.json`, `skeleton-info-*.png`, `skeleton-coffins-*.png`, `skeleton-defenders-*.png`, `skeleton-replay-*.png` and the retained source/atlas contact sheets.

Remaining work includes the interrupted broader WebKit coverage, native target/alert scoring, directional animation, Clan Castle troops and defending heroes, higher Town Halls/content, accounts/multiplayer/clans, and physical-device performance/endurance. The full production-clone goal remains open.

## September 11 — Bomb Tower

Bomb Towers now complete the main TH8 defense roster with native level 1–2 progression, fixed-point ballistic throws and a separate destruction bomb. The death charge waits one second, damages ground troops within 2.75 tiles once, and is canceled when a raid finishes. Two original tower sprites, complete previews, a four-pose roof Bomber and an exposed bomb provide the presentation. See [BOMB-TOWER.md](BOMB-TOWER.md) for immutable primary sources and unresolved native behavior.

**589 model/asset tests pass across 52 files.** Seventeen new simulation/save/replay cases cover numerical progression, target retention, flight speed, moving/dead targets, source destruction, splash boundaries, air immunity, cadence, inactive construction/upgrades, exact fuse boundaries, killing-projectile timestamps, death chains, finish modes, army exhaustion, actor timing and exported replay equality/seeking. The asset case verifies both base/preview pairs, four distinct alpha poses, the exposed bomb and clear margins; deterministic derivative checks pass.

**53 distinct browser scenarios pass per engine** in Chromium with Metal and WebKit at 2× density. Seven new cases cover the Shop gate and every texture, current/next-level Info and reload at 1440px/390px/320px, touch placement with the complete preview, construction/upgrade timers, all Bomber poses, ballistic flight/shadow, stable launch anchors after destruction, ground-level charge layering, exact fuse resolution, reduced motion and repeat replay seeks without duplicate actors. Existing defense, trap, air-control, Tesla, late-troop, projectile, defeat and replay/file scenarios also pass. The first touch fixture read the preview before a rendered frame; the subsequent fixture incorrectly expected the void `upgrade()` method to return true. Both were corrected. Each broad run passed 52 cases, and the corrected touch case passed separately on the unchanged runtime source.

The **288-battle campaign audit** passes with one tower added to stages 9–12. Every stage still has a veteran three-star route. Veteran results are 46 three-star, one one-star (final fortress from the east, 67%) and one zero-star (final fortress from the south, 35%). Healer/P.E.K.K.A and Dragon armies each clear all 48 approaches. These authored scenarios are not multiplayer balance validation.

Production checks pass in Chromium and WebKit at 2× density without reported errors, including all six new asset requests. Chromium reload, Army and replay work offline with **162 cached files**, cache `crown-clan-cafcde86c6b7`.

Runtime source SHA-256 `18643a54e4923c69e6815de52cfacded890d37492c2704381ea011eb7b30fa0b` remained unchanged during the final browser and production runs. Evidence: `output/playtest/bombtower-verification.json`, `bombtower-source.json`, `bombtower-check.log`, `bombtower-art.log`, `bombtower-campaign.json`, `bombtower-{chromium,webkit}.log`, `bombtower-touch-{chromium,webkit}.log`, `bombtower-production-report.json`, `bombtower-contact.png` and `bombtower-{info,flight,fuse,blast}-*.png`. The original PNGs and exact prompts are committed under `art/source/bombtower-v1/`. Native upgrade/terminal-frame behavior, animation, sound and pixel matching remain explicit fidelity gaps; the full game and online systems are unfinished.

## September 11 — Hidden Tesla

Hidden Teslas now extend the TH7–8 defense catalog with six native numerical level records and six original sprites. Concealment covers targeting, paths, crowd separation, deployment boundaries, pointer picking, scouting text, campaign miniatures and all building damage paths. Reveal is permanent after six-tile proximity or 51% displayed destruction. Electrical attacks hit one ground/air target at seven tiles every 0.6 seconds. Upgrading Teslas are visible but inactive; fresh attacks and replay seeking reconstruct their state. See [HIDDEN-TESLA.md](HIDDEN-TESLA.md) for primary sources and explicit fidelity limits.

**571 model/asset tests pass across 50 files.** Twenty-five new simulation/save/replay cases cover all ten troop triggers, dead/out-of-circle exclusions, native progression, hidden Lightning and splash immunity, hidden paths, forced retargeting, upgrading visibility, exact 50/51 thresholds, target retention, P.E.K.K.A behavior, cadence, interruption and exported replay equality/seeking. The asset test verifies six distinct transparent sprites and clear margins; the production derivatives rebuild byte-for-byte.

**46 distinct browser scenarios pass per engine** in Chromium with Metal and WebKit at 2× density. Eight Tesla scenarios cover catalog gates and all textures, Info/reload at 1440px, 390px and 320px, actual hidden picking/text/deployment boundaries, emergence and ground/air bolts, replay seeks, mobile construction, upgrading visibility, reduced motion and destruction during emergence. Review caught the need to reset crop and ground position when a rising tower becomes rubble; the final regression verifies that path. Info captures now wait for the panel animation before screenshots. Existing air-control, defense, trap, late-troop, defeat and replay/file scenarios also pass.

The **288-battle campaign audit** passes with one or two Teslas added to stages 7–12. The veteran army has a three-star route through every village: 47 three-star results and one zero-star result, the final fortress from the south (40% destruction). Healer/P.E.K.K.A and Dragon armies each clear all 48 approaches. This remains authored single-player difficulty, not verified multiplayer balance.

Production checks pass in Chromium and WebKit without reported errors, including all six new asset requests. Chromium reload, Army and replay work offline with **156 cached files**, cache `crown-clan-d9b142e1ae6d`. Runtime source SHA-256 `627ad6193dd4a7c0b296e7314a043a8713d8aba4450d9ac02f470ed3c0c81a69` stayed unchanged during the final browser and production checks. Six generated originals and exact prompts are committed under `art/source/tesla-v1/`; unwanted glow and baked-checkerboard candidates were rejected.

Evidence: `output/playtest/tesla-verification.json`, `tesla-check-final.log`, `tesla-art.log`, `tesla-campaign.json`, `tesla-chromium.log`, `tesla-defenses-chromium.log`, `tesla-webkit.log`, `tesla-production-report.json`, `tesla-contact.png`, and `tesla-{info,hidden,rise,zap}-*.png`. Native fractional-threshold handling, reveal/retarget timing, animation, sound and pixel matching remain approximations. The complete content catalog and online systems are unfinished.

## September 11 — directional air control

Air Sweepers and Seeking Air Mines now extend the local TH1–8 roster. Four Sweeper levels use native health/cost/time/push tables, eight free rotation directions, a traveling gust, zero-damage defense preference and Lightning interruption. Mines use 1,500 single-target damage, five-space eligibility and 3.5 tiles/s homing. Authored campaign stages 6–12 add Sweepers; stages 8–12 add mines. [AIR-CONTROL.md](AIR-CONTROL.md) records the unresolved native cone interpretation, gust geometry/easing and mine activation/target-loss behavior.

**545 model/asset tests across 48 files pass.** The new 25 simulation/save/replay cases cover direction bounds, layout save/undo/redo, front/rear/blind-spot acquisition, all four push strengths, traveling fronts and five-second cadence, ground immunity, interruption, source destruction, defense preference, Balloons/Healers/Dragons, homing speed, single-target damage, inactive traps and exported JSON replay equality. Two asset cases verify 32 distinct transparent directional frames plus three mine states. The final strengthened export test also passes after assigning the exported JSON payload back to the recording.

**35 distinct browser scenarios pass per engine** in Chromium using Metal and in WebKit at 2× density. New checks cover catalog gates, all sprite keys, rotation/Info/layout restore/reload at 1440px, 390px and 320px, touch placement with a real construction timer, instant mine arming, flying mine/gust display and replay seeking. Existing defense, trap, late-troop, replay and replay-file scenarios pass. The initial flight capture was after impact and was moved earlier; the touch fixture's `count` typo was corrected to `countOf`. Verification records the exact union of passing cases, rather than presenting the initial failing runs as clean.

The **288-battle campaign matrix** still finds a three-star veteran approach for every stage. The veteran army earns 47 three-star results and one two-star result across four approaches. The Healer/P.E.K.K.A and Dragon armies each clear all 48 approaches; this remains an authored campaign, not validated multiplayer balance.

Production checks pass in Chromium and WebKit with no reported errors, including all new asset requests. Chromium reloads, opens Army and plays a replay offline with **150 cached files**, cache `crown-clan-a4f74e1abfa3`. Runtime source stayed unchanged during browser and production checks. The 35 original sprites rebuild byte-for-byte with `node scripts/air-control-assets.mjs --check`; generation prompts and accepted PNGs are committed under `art/source/air-control-v1/`.

Evidence: `output/playtest/air-control-verification.json`, `air-control-check.log`, `air-control-export-final.log`, `air-control-art.log`, `air-control-campaign.json`, `air-control-chromium*.log`, `air-control-webkit.log`, `air-control-touch-*.log`, `air-control-production-report.json`, and `air-control-{rotation,info,flight,replay}-*.png`. Native animation comparison, remaining content and online services remain unfinished.

## September 11 — Healer, Dragon and P.E.K.K.A

The elixir Barracks roster now reaches TH8: ten troops, with explicit level 1–3 records for Healer, Dragon and P.E.K.K.A. Primary client CSV records drive health, damage/healing, cadence, housing, movement, range and research. Healer pulses heal living ground groups at their fixed landing location, with hero modifiers and diminishing contributions. Dragon breath applies direct splash; P.E.K.K.A uses heavy ground melee. See [LATE-TROOPS.md](LATE-TROOPS.md) for the immutable data URLs, hashes, exact tables, generated artwork prompts and native behavior still approximated.

**518 model/asset tests in 46 files pass.** Thirty new roster cases cover supported levels and unlocks, research payment/reload, batch capacity, presets, ground-only healing, moving targets, source death, stacking, heroes/Rage, air defense eligibility, battle completion, breath splash, wall strikes, old save formats, invalid levels/counts and deterministic replay. Three asset tests verify all twelve distinct transparent animation frames and matching portraits. A further regression preserves older developer checkpoints. The expanded **288-battle campaign matrix** retains the original four compositions and adds Healer/P.E.K.K.A and Dragon armies. Both new TH8 armies clear all 48 of their stage/approach combinations; this exposes the limited difficulty of the current authored campaign rather than establishing multiplayer balance.

**54 distinct browser scenarios pass in each of Metal Chromium and WebKit at 2× density**, including five new scenarios for the roster's real training/research/info/hotkey controls at 1440×960, 390×844, 844×390 and 568×320, plus battle animation/effects. Coverage also includes existing armies, presets, unlocks, imports, research, effects, replays, pointer input and developer controls. Final affected tests reran successfully: **17 per engine**. AIR badges no longer overlap troop counts on phones or home trays. Dragon flames originate at the mouth and healing orbs at the hands; effects freeze on pause, expire with battle time and clear on exit. The final battle and phone captures were visually reviewed.

Regression work corrected old fixtures that assigned level 4/5 to every troop, and an older unlock test that skipped closing the restored Army drawer. The expanded run also exposed a WebKit developer-panel focus defect: pointer clicks did not focus the submit button before the panel rebuilt. Focus now returns explicitly to the action button or form submitter. Developer checkpoints migrate the roster before validation. These cases pass after the fixes.

The final build and production checks pass in Chromium and WebKit without reported browser errors. All six new WebPs are required by the production smoke check and rebuild byte-for-byte from retained sources. Chromium reloads, opens Army and watches a replay offline with **115 cached files**, cache `crown-clan-232ab3215dc3`. Save version remains 4; combat version 18 records the ten-troop roster and retains older result summaries without attempting incompatible playback.

Evidence: `output/playtest/late-troops-verification.json`, `late-troops-models-verified.log`, `late-troops-campaign.json`, the engine-specific `late-troops-*-{regression,remaining,gameplay,final}.log` files, `late-troops-build-verified.log`, `late-troops-production-report.json`, `late-troops-rebuild.log`, `late-troops-battle-*.png`, `late-troops-tray-*.png` and `healer-info-*.png`. The verification JSON lists the exact passing scenario union and source fingerprint. Generated source artwork was committed separately as `7f25962`.

The full production-clone goal remains open: exact Healer AI, native target/hitbox/windup behavior, defending troops, per-level/directional artwork, higher Town Halls and later content, accounts/multiplayer/clans and physical-device endurance/performance are incomplete.

## September 11 — consistent spell colors and a forked Lightning strike

Healing bottles and auras now use yellow, while Rage uses purple; Lightning keeps blue. The original SVG generator is isolated in `scripts/spell-assets.mjs`, imported by the full asset pipeline and verified with `--check` in CI. Three versioned 256×256 WebPs replace the old URLs, retaining real alpha and totaling 14,002 bytes. A single blue/white forked Lightning graphic replaces the three rectangular beams and uses the existing paused/reduced-motion/cleanup lifecycle. These are authored visual approximations; native silhouettes and deployment staging remain open. See [SPELL-PROGRESSION.md](SPELL-PROGRESSION.md).

**All 11 affected browser scenarios pass in each of Metal Chromium and WebKit at 2×**, covering every spell's researched details at four viewports, paused spell/effect timing, reduced motion and cleanup. Battle captures in both engines show yellow and purple aura footprints and the forked bolt; the graphic reaches alpha 0.5 at 130ms and is destroyed on exit. The generated assets and final spell screens were visually inspected. The preceding **484 model/asset cases and 66 distinct browser scenarios per engine** remain the broader validation; this follow-up changes presentation and asset routing only.

The build and production checks pass in both engines without reported errors. Production now requires all three new spell assets. Chromium reloads, opens Army and watches a replay offline with 109 cached files, cache `crown-clan-5f6da844dc59`. Save and combat formats are unchanged. Evidence: `output/playtest/spell-art-verification.json`, `spell-art-{chromium,webkit}.log`, `spell-art-assets.{log,json}`, `spell-art-build.log`, `spell-art-production-report.json`, `spell-art-capture.{mjs,json}`, `spell-art-battle-*.png`, `spell-art-lightning-fade-*.png` and the refreshed `spell-*-max-*.png` screens.

## September 11 — native spell research, effects and details

Lightning, Healing and Rage levels 1–5 now use explicit research prices, durations, laboratory gates and combat values from Supercell's published game CSV. Lightning uses footprint intersections, storage/Town Hall immunity and defense interruption. Healing delivers 41 discrete pulses with the current 55% hero multiplier. Rage adds researched damage and movement boosts, preserves attack cadence, applies half boosts to heroes, does not stack and lingers after the last pulse. See [SPELL-PROGRESSION.md](SPELL-PROGRESSION.md) for the immutable reference, hashes, tables and remaining timing limitations.

**484 model/asset tests across 44 files pass**, including 36 new spell cases. Coverage includes all twelve research transitions, payment/reload/deadlines, independent laboratory construction, shared troop/spell research, spell unlocks, all five levels' effects, footprint/radius boundaries, immunity, stun/reset, launched shots, healing overlap, Rage/hero interaction, malformed saves and frozen battle/replay levels. The import regression also reproduced coercion of `['heal']` into a spell name; validation now requires an actual string and safely rejects malformed objects. The **192-battle campaign matrix** passes without changes to its spell-free compositions or stage tuning.

**66 distinct browser scenarios have passing coverage in each of Metal Chromium and WebKit at 2× density.** Nine new scenarios cover four spell research/detail viewports and nested dialog focus/scroll restoration. The surrounding gameplay, troop research, facility progression, heroes, Army presets, effects and replay/export/import flows pass. Closing an Army-origin dialog now restores its drawer, scroll and launching control; three existing navigation checks and the production script were updated to assert this behavior. The detail measurements wait for the entrance animation and scroll the action to the center, avoiding transient transforms and fractional edge clipping. All eight dedicated spell cases pass in each engine after those measurement corrections, and the affected hero navigation case also passes again in both.

Desktop, portrait and both landscape sizes were visually inspected. The final build and production checks pass in Chromium and WebKit without reported browser errors. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-ed015a1e2d83`. Save version remains 4. Combat version 17 preserves older result summaries while recording spell levels for current playback and portable files. New spell browser coverage is included in WebKit CI.

Evidence: `output/playtest/spell-progression-verification.json`, `spell-progression-tests-verified.log`, `spell-import-regression-before.log`, `spell-progression-campaign.json`, `spell-progression-{chromium,webkit}-{final,regression,verified}.log`, `spell-progression-hero-{chromium,webkit}.log`, `spell-progression-build-verified.log`, `spell-progression-production-report.json`, and `spell-{research,healing,lightning-max,heal-max,rage-max}-*.png`. Native bottle deployment timing, later spell types, defending troops, full hero equipment and final spell artwork remain unfinished.

## September 11 — normalize countdown rounding at hour boundaries

Long countdowns round the total minutes before splitting hours and minutes. A timer just below three hours now reads `3h` instead of `2h 60m`. Seconds remain visible below an hour, and actual research/construction deadlines and gem prices are unchanged.

The new regression reproduces the original defect before the fix. **33 focused timer and progression cases pass**, including two new formatting cases covering 16 concrete boundary values. The preceding full suite's 446 cases remain the broader validation. Both landscape research scenarios pass again in each of Metal Chromium and WebKit at 2×, asserting the actual live `3h` label while retaining payment, independent construction, scrolling, Finish and Close checks. The updated 568px screenshot was visually reviewed.

The build and production checks pass in both engines without reported errors. Chromium also reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-8715614afee5`. Evidence: `output/playtest/countdown-verification.json`, `countdown-regression-before.log`, `countdown-regression-final.log`, `countdown-{chromium,webkit}.log`, `countdown-build.log` and `countdown-production-report.json`.

## September 11 — compact research and stable dialog refreshes

Research now places its active timer and Laboratory status beside compact troop cards on short landscape screens. The first two research actions fit at 568×320; three cards fit at 844×390. Research, Finish and Close retain 44px touch targets. The status column stays visible while scrolling the catalog. Updating an already open dialog also stops replaying its fade/scale entrance animation; opening a new dialog still animates.

The two new landscape cases initially exposed the repeated entrance animation, then the smallest viewport exposed a clipped Finish button during simultaneous research and construction. Both defects are corrected. **All seven final research-layout and modal-focus cases pass in each of Metal Chromium and WebKit at 2× density.** The preceding broader 38-case run passed its other 37 cases in each engine, including facility progression, starter/specialist research, details and gameplay. Together with the facility pass, **51 distinct browser scenarios per engine have passing coverage**. Active and scrolled landscape screenshots were visually inspected, including the fully visible 568px Finish action. Model validation remains the preceding 446 passing cases; this follow-up changes presentation only.

The final production build and both browser checks pass without reported errors. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-fed32f664d4f`. Save and replay formats are unchanged. Evidence: `output/playtest/research-layout-verification.json`, `research-layout-{chromium,webkit}-final.log`, `research-layout-{chromium,webkit}-compact.log`, `research-layout-build-compact-final.log`, `research-layout-production-report.json` and `research-landscape-{active,scrolled}-*.png`. Screenshot review found a separate countdown rounding defect ("2h 60m" just below three hours), corrected in the follow-up recorded above.

## September 11 — native army facilities and independent Laboratory timers

Barracks levels 1–10, Laboratory levels 1–6 and Spell Factory levels 1–3 now use explicit native health, elixir prices and durations. Two accepted legacy Factory levels also have explicit values. Purchases enforce one Barracks, a Laboratory from TH3 and a Spell Factory from TH5. The highest completed factory supplies housing; imported duplicate buildings and excess prepared spells remain saved without multiplying that capacity. The Army drawer explains excess troop or spell housing and allows removal. Research and Laboratory construction now run independently in either start order, using the completed building level until its upgrade finishes. See [FACILITY-PROGRESSION.md](FACILITY-PROGRESSION.md) for tables, sources and compatibility.

**446 model/asset tests across 42 files pass**, including 27 new cases. Each of the 19 purchasable facility levels is exercised through payment, reload and exact deadline completion. Coverage also includes every TH1–8 count/level gate, stale direct-placement rejection, paid legacy queues and deadlines, preserved health fractions and buildings, spell capacity recovery, both Laboratory start orders, gem completion and exact playback of previous facility health. The unchanged **192-battle campaign matrix** still finds a three-star veteran approach for every stage, and the actual starter can win the opening raid. No campaign layout or multiplier changed.

**44 distinct browser scenarios have passing coverage in each of Metal Chromium and WebKit at 2× density.** The initial selection passed 37 cases in each engine; six new cases assumed literal spaces between table-cell text, and one old drag test tried to buy a second Barracks. The new checks now assert individual cells, and the drag test uses an available Laboratory while retaining actual pointer placement/count assertions. All **24 affected and surrounding scenarios pass** in each engine after those corrections. The eight dedicated facility cases pass again after screenshot framing changes. Phone captures additionally wait for the toast opacity to reach zero; the three affected phone cases are rerun in each engine. The factory-unlock fixture now funds the real prices and gem skips with sufficient storage instead of relying on the former prototype prices.

Desktop, portrait and landscape Info/research panels and the phone excess-spell warning were visually inspected. The production build passes; Chromium and WebKit report no browser errors at 2×. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-ccf7526b270b`. Save version stays 4 and replay version stays 16: prior facility health remains in recorded snapshots and is not reinterpreted. New browser coverage is included in WebKit CI.

Evidence: `output/playtest/facility-progression-verification.json`, `facility-progression-tests.log`, `facility-progression-campaign.json`, `facility-progression-{chromium,webkit}.log`, `facility-progression-{chromium,webkit}-final.log`, `facility-progression-{chromium,webkit}-visual.log`, `facility-progression-{chromium,webkit}-capture.log`, `facility-progression-build-final.log`, `facility-progression-production-report.json` and `facility-{research,info,legacy-spells,shop}-*.png`. The remaining facility silhouettes, later troop/spell unlocks, spell research, Dark army facilities and broader economy remain unfinished.

## September 11 — readable troop details in compact landscape

Troop Info now places the portrait beside its stat grid on short landscape screens. A compact fixed header keeps the troop name and a 44px Close button, while removing repeated headings and branding. All five specialist panels show at least their first six stats immediately at both 844×390 and 568×320, with remaining stats, tactics and the 44px Train action reachable by scrolling. Desktop and portrait layouts keep their existing arrangement. Final WebKit Wizard and Chromium Wall Breaker screenshots were visually reviewed at those two landscape sizes.

**13 affected UI scenarios pass in each of Metal Chromium and WebKit at 2× density**, including research, details, starter progression and Balloon behavior. After the final header refinement, all four detail scenarios pass again in each engine, covering five specialist panels per viewport, stat placement and overflow, Train interactions and actual Close behavior. The added 568×320 research and detail cases bring this troop progression pass to **46 distinct passing browser scenarios per engine** across the recorded runs. The preceding combat commit's 419 model/asset tests remain the model validation; this follow-up changes only presentation CSS and its browser coverage.

The final build and production checks pass in both engines at 2× without reported browser errors. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-5e5c7e9556f5`. Evidence: `output/playtest/troop-details-verification.json`, `troop-details-{chromium,webkit}.log`, `troop-details-{chromium,webkit}-final.log`, `troop-details-build-final.log`, `troop-details-production-report.json`, and `troop-roster-{wizard,wall-breaker}-{844,568}-{chromium,webkit}.png`.

## September 11 — complete supported troop combat and research tables

All seven supported troop types now use explicit Home Village level 1–5 records. This pass adds Giant, Wizard, Balloon, Goblin and Wall Breaker health, hit/DPS values, movement, range, cadence, prices, research durations and laboratory gates. It removes the prototype 30% level multiplier, corrects Wizard splash to 0.3 tiles at full collateral damage, applies researched Balloon death damage at 1.2 tiles, and separates Wall Breaker contact (hit plus death) from early defeat (death only). Goblins prefer and deal double damage to Dark Elixir buildings. Info includes movement speed, damage on destruction and wall contact totals, with a 44px Train action. Save version stays 4 and paid legacy research remains intact. Combat version 16 retains old summaries without incompatible playback. See [TROOP-PROGRESSION.md](TROOP-PROGRESSION.md).

**419 model/asset tests across 41 files pass**, including 32 new progression/combat/replay cases. All 25 added level records are exercised through deployed health and research. Tests cover native movement and range boundaries, Giant cadence/defense preference, Goblin Dark Elixir targeting, Wizard fractional impact/splash, all researched death-damage levels, Rage affecting only Wall Breaker attack damage, one-time explosions and exact JSON/replay reproduction of a 187.5-damage hit. Two mortar fixtures now hold their intended targets in the impact area; the corrected faster Giant movement had allowed them to walk out of the splash circle.

The **192-battle campaign matrix** preserves the previous level-three, 160-space army as a mid-research scenario and adds a TH8 level-five, 200-space roster containing all seven troop types. All 12 stages have a three-star veteran approach; the actual untouched starter can still win the first raid. The mid-research army now falls short of fully clearing the final fortress. Each scenario has compatible Town Hall, laboratory, barracks and camp gates. No campaign layout, multiplier or reward was changed to retain the old balance.

**44 distinct browser scenarios pass in each of Metal Chromium and WebKit at 2× density.** Six new cases inspect research gates, payment, reload, gem completion and five detailed troop panels at 1440×960, 390×844 and 844×390. The existing gameplay, projectile, Balloon, specialist animation, starter research, replay/seek/export/import, tab ownership, context recovery and twenty-raid transition scenarios also pass. A new test initially matched both the background and modal Train buttons; its selector is now scoped to the detail panel. That revealed the real 43px button height, corrected to 44px. The final 11 affected UI scenarios pass in each engine. Desktop and phone details were visually reviewed; the landscape panel remains scrollable but dedicates substantial initial space to its portrait.

The production build passes. Chromium and WebKit production checks report no errors; Chromium also reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-c4947205fb08`. A Metal mixed-roster sample at 1440×960 CSS / 2880×1920 buffer pixels averages 60 FPS in the village and battle, with 16.8ms p95 frames over each four-second sample. It deploys 84 level-five units occupying 200 spaces, with 82 living at the end of the sample, on Apple M5 Pro. The new `--army=mixed` benchmark option makes that composition repeatable. This does not establish physical-phone or sustained thermal performance.

Evidence: `output/playtest/troop-roster-verification.json`, `troop-roster-tests-final.log`, `troop-roster-campaign.json`, `troop-roster-{chromium,webkit}.log`, `troop-roster-{chromium,webkit}-final.log`, `troop-roster-build-final.log`, `troop-roster-production-report.json`, `troop-roster-performance.json`, and `troop-roster-{research,wizard,wall-breaker}*.png`. Full native roster, spell/hero balancing, economy, per-level and directional art, authentic campaign content and online services remain unfinished.

## September 11 — native starter combat and laboratory progression

Barbarian and Archer levels 1–5 now use explicit Home Village HP, damage, research cost/time and laboratory tables. Their movement speeds are 2.2 and 3 tiles/second; ranges are 0.4 and 3.5 tiles; attacks occur once per second. Current, deployed and next-level preview values share one lookup. The pathfinder adds a short final segment within the last reachable cell so a 0.4-tile melee weapon can actually reach a building. Regular wall attacks no longer receive the prototype's hidden 1.6× bonus. Save version remains 4; paid legacy research keeps its deadline and existing levels. Combat version 15 prevents older recordings from playing with changed rules while retaining their result summaries. See [STARTER-TROOP-PROGRESSION.md](STARTER-TROOP-PROGRESSION.md) for sources and exact values.

All **387 model/asset tests across 40 files pass**, including 17 new starter-progression and short-reach cases, deterministic replay/seek, wall/projectile interactions and the 144-battle campaign audit. Every authored stage remains clearable with the existing veteran fixture; the actual unupgraded starter army can still win the opening raid. Campaign layouts and multipliers were not changed. Tests tied to the former research costs, 130-second assumed raid completion and six-second wall breach were updated to the native costs and full battle/breach duration; damage and collision assertions remain. Old replay version assertions now track the current version while explicitly testing legacy snapshots.

**27 distinct browser scenarios pass in each of Metal Chromium and WebKit at 2× density.** They cover native research previews, paid reload and gem completion at 1440px and 390px, Lab 3 gating, details, actual gameplay, replay recording/seek/export/import, graphics recovery, tab ownership and twenty repeated raid transitions. The initial Chromium run found an old replay-layout fixture comparing physical camera width with CSS width; the corrected check includes pixel density and retains every hit-target assertion. Screenshot capture waits for loading and modal transitions, and desktop research/phone details were visually reviewed.

The build and production checks pass in both engines at 2×, including deployment, research, replay, tab handoff and landscape resizing. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-ff5998a0267f`; no browser errors were reported. The new browser regression is included in WebKit CI.

A separate Metal benchmark deploys a capacity-valid 200-unit army (100 level-one Barbarians and 100 level-one Archers). At 1440×960 CSS pixels / 2880×1920 buffer pixels, idle and battle both average 60 FPS, with 16.8ms 95th-percentile frames over each four-second sample. All 200 units remain present during the battle sample. The reported renderer is Apple M5 Pro; this is a short Mac benchmark, not physical-phone or thermal certification. Other troop, spell, hero and building statistics still contain unaudited values.

Evidence: `output/playtest/starter-progression-verification.json`, `starter-progression-tests-final.log`, `starter-progression-campaign.json`, `starter-progression-chromium.log`, `starter-progression-chromium-final.log`, `starter-progression-chromium-ui.log`, `starter-progression-webkit.log`, `starter-progression-webkit-ui.log`, `starter-progression-build.log`, `starter-progression-production-report.json`, `starter-progression-performance.json`, `starter-combat-performance.mjs`, and `starter-{research,stats}-{390,1440}-{chromium,webkit}.png`.

## September 11 — Barbarian identity, portrait and walk cycle

The starter melee troop now uses the Barbarian name and a new matching portrait and four-frame walk atlas throughout preparation, camps, combat and replay. The built-in image-generation tool produced the source artwork using a Supercell character reference. A corrected source replaces the initial opaque checkerboard and repeated stride; the deterministic importer removes its magenta matte, registers the hair, preserves a shared scale and aligns feet. The retained `swordsman` save/replay key, combat stats and save/replay versions are unchanged. See [BARBARIAN-ART.md](BARBARIAN-ART.md) for sources, exact prompts, importer details and remaining animation gaps.

All 37 focused model/asset cases pass across Barbarian artwork, facing, army preparation/unlocks and deterministic replay. Exact asset rebuilds pass. Ten distinct browser scenarios pass in each of Metal Chromium and WebKit at 2× density: army roster, starter naming/portraits and save reload, camp texture/idle, ground-troop animation, and desktop/phone replay. Initial new-fixture failures used a battle-only selector in the village and assumed Phaser retained a fetchable image URL; the final checks use the accessible village card and compare the decoded camp texture pixels directly with the shipping atlas. Screenshots wait for the details modal animation before capture. The portrait and enlarged phone camp were visually reviewed.

The build and production checks pass in Chromium and WebKit at 2×, including the new required artwork, deployment, replay, tab handoff and landscape rotation. Chromium reloads, opens Army and plays a replay offline with 109 cached files, cache `crown-clan-9eaad3630bed`; no browser errors were reported. The new art rebuild and presentation tests are included in CI. This pass covers one low-level appearance; level variants, full directional/attack/death art and physical-phone verification remain open.

Evidence: `output/playtest/barbarian-verification.json`, `barbarian-chromium.log`, `barbarian-chromium-final.log`, `barbarian-webkit.log`, `barbarian-webkit-final.log`, `barbarian-build.log`, `barbarian-production.log`, `barbarian-production-report.json`, `barbarian-details-{chromium,webkit}.png` and `barbarian-camp-phone-{chromium,webkit}.png`.

## September 11 — compact village controls and landscape spacing

Collect no longer covers Zoom out in the compact village HUD. The initial hit-test probe found obstructed controls in nine of thirteen viewport sizes, including hidden activity buttons at 320×568 and several controls at 568×320. The new compact stylesheet gives the side tools explicit clearance above bottom actions and 44×44 CSS-pixel targets. Short portrait views use side grids and place Collect between Attack and Shop; landscape places Collect beside Shop with a separate camera row above. A short army tray hugs its contents, while a developed roster retains horizontal scrolling. Safe-area variables reserve space at the screen edges. See [COMPACT-HUD.md](COMPACT-HUD.md).

The initial 25-case gameplay/UI selection passes in each of Chromium and WebKit. A seven-case Retina selection also passes in Chromium. Six of seven initially pass in WebKit; the new developed-village fixture retained a DOM handle while a pending HUD redraw replaced it. Reading the control and its rectangle in one browser evaluation corrects that test race, while preserving the real center click and required zoom change. All four final compact-HUD cases pass at 2× in both Metal Chromium and WebKit, with the touch case retaining its 3× setting. Across the selections, 29 distinct scenarios per engine have passing coverage.

The dedicated layout cases check 19 viewports for both starter and developed villages: 38 combinations per engine. Five points in each main control must hit that control, and clicking the exact center of Zoom out must change zoom. Small-screen Settings and Collect actions, 3× touch rotation, inset spacing, scrolling to Train, Army/Shop drawers, Edit camera controls and first-run coaching are exercised. Small starter trays are checked for unwanted empty width. Screenshots at 320×480, 568×320 and 844×390 were visually reviewed. The inset case reserves CSS space; physical-device ergonomics remain unverified.

The production build passes Chromium and WebKit at 2× density without reported errors, including actual deployment, collection flights, replay, tab handoff and rotation. Chromium reloads, opens Army and plays a replay offline with 107 cached files, cache `crown-clan-002840677071`. The new layout regression is included in WebKit CI and the Retina configuration. This pass changes CSS and its import only; simulation, save/replay rules and source artwork are unchanged, so model and asset suites were not rerun.

Evidence: `output/playtest/hud-spacing-baseline.json`, `hud-spacing-after.json`, `hud-layout-chromium.log`, `hud-layout-webkit.log`, `hud-layout-retina-chromium.log`, `hud-layout-retina-webkit.log`, `hud-layout-final-chromium.log`, `hud-layout-final-webkit.log`, `hud-layout-build.log`, `hud-layout-production-report.json`, and `hud-layout-*-320x480-*.png` / `hud-layout-*-568x320-*.png` / `hud-layout-*-844x390-*.png`. The consolidated record is `hud-layout-verification.json`.

## September 11 — native Retina and 3× rendering

The canvas now uses physical display pixels with a 16-million-pixel allocation budget and GPU dimension limits. The former 390×844 buffer stayed at that size even on a 3× screen; it now renders at 1170×2532. A 1440×960 Retina view renders at 2880×1920. Camera framing, gesture tolerances, DOM shop dragging, context anchors and resource flights remain in CSS coordinates. Fractional-density rounding uses separate camera axes. Resize and live display changes preserve world focus and CSS zoom, cancel old gestures, and remap stationary mouse input. See [DISPLAY-DENSITY.md](DISPLAY-DENSITY.md).

Two allocation tests pass. The browser matrix contains 35 distinct passing Chromium cases across the focused 19-case default-renderer run, 21-case Retina Metal gameplay run and native pinch case. WebKit passes 33 cases, including 2× terrain/camera coverage and 3× touch input. Two CDP-only capabilities—live density switching and native multi-touch injection—are tested in Chromium. The live-switch test caught devicePixelRatio changing without a resize or media-query notification; a lightweight pre-step comparison now catches that condition. Two existing fixtures also needed explicit waits for a drawer redraw and asynchronous animation cancellation; their original held-button and particle assertions remain intact.

Each density profile resolves alternating one-physical-pixel stripes. The final strengthened graphics-recovery reference includes buildings, troop sprites, text and terrain; three profiles pass in each of Metal Chromium and WebKit with zero changed RGBA pixels after context restoration. The Retina WebKit run also repeats the existing 180 terrain coverage views, field-boundary probes and 15 full-frame comparisons against the old stencil. CI includes the density scenarios and an additional six-case Retina terrain/camera selection.

Both hardware benchmarks use headless Chromium 153.0.8010.12 and the reported Apple M5 Pro Metal renderer, with no other browser test suite from this task running. At 1440×960 CSS/2880×1920 pixels (2×), idle, native 200-troop camps, the 660-actor legacy roster and battle each measure 60 FPS, with 16.7 ms 95th-percentile frames. At 390×844 CSS/1170×2532 pixels (3×), all four samples also measure 60 FPS, with 16.7–16.8 ms 95th-percentile frames. Host load averages are recorded in the reports. The phone-sized run still uses the Mac GPU; physical phones/tablets and longer thermal sessions remain unverified.

The production build passes Chromium and WebKit at 2×, including buffer-size assertions, real deployment, replay, tab handoff and landscape resizing. Chromium reloads, opens Army and plays a replay offline: 107 cached files, cache `crown-clan-55031cfd516c`. No browser errors were reported. This rendering pass does not change game rules, saves, replay versions or source artwork; the last complete model/asset suite remains the 366-test camp-art run in the historical record.

Visual follow-up: at 844×390, Collect covers the center of Zoom out. A DOM hit test returns `collect` at the zoom button's center (x≈804.6, y≈271.6). This separate CSS control-layout defect is resolved in the compact HUD pass recorded above. Higher-density rendering also does not add missing source-art detail, world-text texture resolution, native content or backend services.

Evidence: `output/playtest/display-unit.json`, `display-chromium.log`, `display-retina.log`, `display-webkit.log`, `display-pinch.log`, `display-restoration-metal.log`, `display-restoration-webkit.log`, `display-build.log`, `display-production-report.json`, `display-retina-performance.json`, `display-phone-performance.json`, `display-phone-camps.png`, `display-landscape-followup.json`, and `display-landscape-followup.png`. The consolidated record is `display-verification.json`.

## September 11 — reduce terrain overdraw and verify Metal rendering

The turf mask now draws only the four corners of its bounding rectangle outside the buildable diamond. It removes two full-viewport stencil inversion draws per frame, while retaining the same repeating texture, tint, alpha, clipping, world detail and troop roster. The matching subtract operation still clears the mask before later world objects render. Gameplay, saves, replay rules and artwork are unchanged.

Nine focused browser cases pass in each of Chromium and WebKit. The new regression compares every RGBA framebuffer pixel against the previous inverted-diamond implementation in 15 views per engine: five fractional camera/zoom settings across phone portrait, phone landscape and desktop sizes. Every comparison has zero changed pixels and exactly two fewer draw calls. Existing checks cover all 1,936 grass tile centers, edge clipping, later objects outside the stencil, context restoration, sprite raster equivalence and 180 terrain coverage views per engine. Six additional cases pass in Chromium with ANGLE Metal, including the same pixel comparison and graphics restoration.

Profiling showed approximately 0.4 ms of JavaScript rendering work per frame. Changing the texture batch limit gave no consistent improvement, so batching remains unchanged. Removing the turf overlay temporarily raised the same frozen scene from 54 to 60 FPS. An initial alternating-mask probe measured 56 FPS for each inverted-mask sample and 58/60 FPS for the corner mask, with zero changed pixels in five full-frame comparisons. The longer SwiftShader run overlapped other browser workloads and measured 40/40 and 27/31 FPS for old/new pairs; its native 200-troop sample measured 50 FPS and the legacy roster 41 FPS. These shared-host results do not establish a stable global frame-rate improvement. The verified reduction is two full-screen passes with identical tested output.

The benchmark now supports `--compare-field-mask`, which alternates both implementations twice on the same frozen 200-troop village and restores the original scene configuration afterward. macOS `--metal` explicitly requests ANGLE Metal and rejects a silent renderer fallback. With the reported **Apple M5 Pro Metal renderer**, headless Chromium 153.0.8010.12 at 1440×960 and 1× pixel ratio held **60 FPS** idle, in battle, with 200 native troops and with 660 legacy actors. Its 95th-percentile frames were 16.7–16.8 ms. Both masks reached the same 60-FPS cap on this hardware. High-DPI rendering and physical phone/tablet performance remain unverified.

The production build passes Chromium and WebKit without reported errors. Chromium reloads, opens Army and plays a replay offline with 107 cached files, cache `crown-clan-38e6420cd7a5`. The existing terrain WebKit CI selection automatically includes the new framebuffer case. See [TERRAIN-CAMERA.md](TERRAIN-CAMERA.md) for implementation and benchmark commands.

Evidence: `output/playtest/field-mask-verification.json`, `render-profile-baseline.log`, `render-profile.json`, `render-profile.cpuprofile`, `render-layers-profile.json`, `field-corners-profile.json`, `field-mask-chromium.log`, `field-mask-webkit.log`, `field-mask-metal.log`, `field-mask-build.log`, `field-mask-production-report.json`, `field-mask-performance.json`, and `field-mask-metal-performance.json`.

## September 11 — eight camp level sprites and open gathering areas

All 366 model and asset tests pass across 37 files. Eight original built-in ImageGen sprites replace the camp's two tent-and-wall tiers, with clean alpha, explicit level selection, measured ground origins, untinted materials and matching placement previews. Camps retain 4×4 placement while home troops can gather on their outer tiles and avoid the central fire pit, buildings, walls, trees and rocks. Higher rock-ring camps leave stone debris; cooking-support camps leave wood debris. Health and upgrade bars follow the actual sprite origin. Exact prompts, source files and importer are documented in [CAMP-ART.md](CAMP-ART.md).

The first 23 focused browser scenarios pass in each engine. An expanded nine-case pass found one new test fixture with an empty army, which correctly could not start practice. Retaining one troop fixes that fixture; all four camp-art cases then pass in Chromium and WebKit, alongside the five previously passing combat-presentation cases. The combined runs provide 29 distinct passing scenarios per engine. Visual review found an immediate phone screenshot could precede the changed scene's render; camp-art captures now wait for `postrender`. The eight-level gallery, upgrade Info, mixed village roster, phone placement and open camp gathering were visually inspected after that correction.

The final production build passes Chromium and WebKit without reported errors, and requires successful loading of all eight camp textures. Chromium reloads, opens Army and plays a replay offline with 107 cached files, cache `crown-clan-75c106fb5d57`. Rebuilding the accepted sources byte-matches all eight committed WebPs.

`performance-check.mjs --camps` now measures native TH8 housing (four level-six camps, 200 one-space troops) separately from the preserved 660-actor legacy roster. The final headless Chromium/SwiftShader sample at 1440×960 measured 40 FPS idle, 37 FPS with 200 troops, 35 FPS with 660 actors and 40 FPS in battle, with 33.4 ms 95th-percentile frames. Host load rose from 4.45 to 8.10 during this sample on 18 logical CPUs. An earlier sample measured 28/39/31/45 FPS, and another browser renderer was observed using several CPU cores afterward. These are shared-host samples, not a controlled regression comparison or a physical-device certification. A reliable 60-FPS result remains unverified; performance, closer native art matching and animated camp fire remain open.

Evidence: `output/playtest/camp-art-verification.json`, `camp-art-unit.json`, `camp-art-chromium.log`, `camp-art-webkit.log`, `camp-art-final-chromium.log`, `camp-art-final-webkit.log`, `camp-art-verified-chromium.log`, `camp-art-verified-webkit.log`, `camp-art-final-build.log`, `camp-art-final-production-report.json`, `camp-art-rebuild.log`, `camp-art-performance.json`, `camp-art-final-performance.json`, `camp-level-gallery-webkit.png`, `camp-open-gathering-webkit.png`, `camp-art-info-webkit.png`, and `camp-cutouts-on-grass.png`.

## September 11 — native camp capacity and progression

All 363 unit tests pass across 36 files, including all 144 campaign attacks. Six dedicated camp cases cover native values, count/level ceilings, actual capacity, paid deadlines, old prepared armies and replay health. A further focused six-case run passes after strengthening the old deadline test: a purchased ten-minute upgrade stays unfinished at five minutes and completes at its saved deadline.

The initial 40-scenario browser selections each passed 39 scenarios in Chromium and WebKit. Both found the same outdated expectation that a second Giant could be added to the corrected starter's 27/30 army. The test now removes the first Giant and verifies that the unlocked Add control becomes available again. All five corrected/focused scenarios pass in each engine, providing 40 distinct passing scenarios per engine across the runs. Camp Info/Upgrade, shop counts, legacy reload and above-capacity remove/add flows are included; phone Info and capacity-warning screenshots were visually reviewed.

The new TH2 village has one completed level-two camp and 30 spaces. Explicit tables now drive camp housing, health, prices, timers, counts and occupant distribution. Existing camps, prepared armies and purchased deadlines are preserved. New recordings use combat version 14; older snapshots retain their original health as incompatible summaries. See [CAMP-PROGRESSION.md](CAMP-PROGRESSION.md). Native per-level camp artwork remains a separate unfinished pass.

Production Chromium and WebKit pass without reported errors. Chromium reloads, opens Army and plays a replay offline with 99 cached files, cache `crown-clan-4e0af935cc71`. The final formatting cleanup builds the identical cache. CI includes the new camp browser cases in its WebKit selection.

Evidence: `output/playtest/camp-progression-verification.json`, `camp-progression-unit.json`, `camp-progression-deadline-unit.json`, `camp-progression-chromium.log`, `camp-progression-webkit.log`, `camp-progression-rerun-chromium.log`, `camp-progression-rerun-webkit.log`, `camp-progression-production-report.json`, `camp-progression-final-build.log`, `camp-progression-info-chromium.png`, and `camp-over-capacity-320-webkit.png`.

## Native army footprints and save recovery — September 11, 2026

All 357 unit tests pass across 35 files. The full Chromium run passed 159 of 161 scenarios. The two failures were an Archer Tower test destination newly blocked by the enlarged camp and a wall-layout test reading a child element after a redraw detached it. The destination now uses clear ground; the layout test reads one DOM snapshot. The final 19 focused scenarios pass in each engine, giving 161 distinct Chromium and 36 distinct WebKit scenarios with passing coverage across the runs. A separate catalog rerun hit its 30-second timeout while both rendering suites were active; batching the level comparisons reduced browser round trips and the revised test passes in both engines.

Army Camp and Hero Hall use 4×4 footprints, proportionately larger artwork, and corrected camp gathering centers. Three campaign placements clear the expanded geometry. Save format 4 validates each historical geometry before relocating conflicts, preserves every accepted building and paid deadline, migrates layout slots and leaves unplaceable imports untouched. Dense tests cover mixed legacy defense/army footprints, fragmented layouts, ongoing obstacle removal, deterministic ordering and an impossible custom import. Startup offers recovery downloads when saved data cannot be opened and never rolls newer unplaceable progress back to an older usable copy. Replay version 13 retains older snapshots as summaries under their original bounds.

Production checks pass in Chromium and WebKit with no reported errors. Chromium reloads, opens Army and plays a replay offline with 99 cached files, cache `crown-clan-5e4a18a5f79f`. The first production attempt could not deploy from six fixed canvas positions; a bounded scan of visible canvas ground now waits for pointer processing and still requires an actual deployed troop. The final formatting build produces the identical verified cache. Phone Camp/Hall previews, mixed camp occupants, and recovery downloads were visually reviewed. These changes correct geometry and reliability; native camp capacity, health, economy and per-level artwork remain unfinished. See [NATIVE-GRID.md](NATIVE-GRID.md) and [CAMP-PRESENTATION.md](CAMP-PRESENTATION.md).

Evidence: `output/playtest/army-footprints-verification.json`, `army-footprints-final-unit.log`, `army-footprints-final-targeted.log`, `army-footprints-chromium.log`, `army-footprints-webkit.log`, `army-footprints-final-chromium.log`, `army-footprints-final-webkit.log`, `army-footprints-production-report.json`, `army-footprints-final-build.log`, `army-footprint-camp-chromium.png`, `army-footprint-herohall-webkit.png`, `camp-roster-phone.png` and `save-recovery-webkit.png`.

## Terrain fitted to the buildable field — September 11, 2026

All 345 unit tests pass. After correcting the clipping issues found during development, all 26 focused browser scenarios pass in each of Chromium and WebKit. These cover all 1,936 buildable tile centers, turf alignment and clipping at all four edges, subsequent world-object rendering, graphics-context restoration, camera coverage, actual placement, building artwork, collection, replay and twenty raid transitions. The terrain asset rebuild check and final production build also pass.

The new 1672×941 backdrop fits at 1.35× world size. The accepted refinement reduces conspicuous grass patches, and a 64×32 repeating turf texture replaces fixed prototype dirt strips. All sampled buildable centers sit on grass; the earlier backdrop failed the same palette check at 18 centers. The first masking approach used a Canvas-only API; the new framebuffer test caught the ineffective WebGL clipping. A later probe caught a stencil-release option that did not inherit the inverted mask setting. The release now explicitly matches those settings, and both browsers verify drawing outside the field after it.

The final production check reports no errors in either browser. Chromium reloads, opens Army and plays a replay offline with 99 cached files, cache `crown-clan-5d332058113c`. Phone, desktop overview and western-boundary screenshots were visually inspected with normal scene objects. The image tool returned 1672×941 despite the initial 4K request; no upscaling or 4K claim is made. Full native scenery composition, building-level artwork and device performance certification remain open. See [TERRAIN-CAMERA.md](TERRAIN-CAMERA.md) and `art/source/terrain-field-v4.json` for source details and exact prompts.

Evidence: `output/playtest/terrain-field-verification.json`, `terrain-field-production-report.json`, `terrain-field-check.log`, `terrain-field-final-build.log`, `terrain-field-verified-chromium.log`, `terrain-field-verified-webkit.log`, `terrain-field-final-phone.png`, `terrain-field-final-overview.png` and `terrain-field-final-west-edge.png`.

## Expanded village and native defense footprints — September 11, 2026

All 345 unit tests pass, including dense legacy migration, saved layouts, damaged-health and paid-deadline preservation, expanded-grid pathfinding and campaign viability. The full Chromium run passed 150 of 154 scenarios. After fixes, 26 focused scenarios pass, covering all four failures and a new deterministic input regression: 155 distinct Chromium scenarios have passing coverage across the full run and follow-up. All 46 focused WebKit scenarios pass. CI now includes the native-grid, building-art and placement-preview cases in its WebKit selection.

The field now has 44×44 buildable tiles, with 3×3 Cannons, Archer Towers and Mortars. Save format 3 relocates conflicting legacy defenses deterministically and reports moved buildings once. Sixteen campaign placements were corrected. Combat version 12 retains older result summaries while refusing playback under changed geometry. Browser testing found an overwritten scene-ready callback hiding the migration notice, an older-backup fixture incorrectly using the current save version, and a redraw that could detach a pressed Save button or interrupt Army category scrolling. These are resolved; the input fix is described in [INPUT-GESTURES.md](INPUT-GESTURES.md).

The final production build passes Chromium and WebKit without reported errors. Chromium reloads, opens Army and plays a replay offline with 98 cached files, cache `crown-clan-272db0eddc04`. Phone placement at (43,43), the normal phone village and the desktop grid overview were visually reviewed. The larger field is usable, but the existing terrain source is softer at its increased display scale. The two-tile simulation border is local; remaining catalog dimensions and per-level artwork still need work. See [NATIVE-GRID.md](NATIVE-GRID.md).

Evidence: `output/playtest/native-grid-verification.json`, `native-grid-production-report.json`, `native-grid-final-check.log`, `native-grid-chromium.log`, `native-grid-fixes-chromium.log`, `native-grid-final-webkit.log`, `native-grid-final-phone.png`, `native-grid-final-overview.png` and `native-grid-far-corner-webkit.png`.

## Placement preview follows the active pointer and camera — September 11, 2026

All 22 targeted browser scenarios pass in each of Chromium and WebKit after the placement follow-up. Coverage includes stationary-pointer keyboard pan, zoom and viewport resize; DOM shop pointer retention through a scene refresh; return to canvas input on release; actual shop placement by tap/drag; touch input; phone layouts; and the building-art cases. The earlier appearance pass completed 338 unit tests and 27 browser scenarios per engine (46 distinct browser scenarios across the two phases).

The final production build passes both engines without reported errors; Chromium also reloads, opens Army and plays a replay offline with 98 cached files, cache `crown-clan-300ec5409ef6`. The new regressions reproduced a stale preview after a camera transform and a 440-world-pixel jump to an outdated canvas pointer during a shop drag. Holding the economy clock ensures its periodic refresh cannot mask the camera defect.

The preview sprite and footprint now use the same snapped tile and validity result each frame. A phone capture after zooming a stationary cursor shows the upgraded Gold Mine over the blocked footprint; the measured anchor error is zero world pixels. See [BUILDING-PRESENTATION.md](BUILDING-PRESENTATION.md). Evidence: `output/playtest/placement-preview-verification.json`, `placement-preview-production-report.json` and `placement-camera-phone-webkit.png`.

## Consistent building artwork during movement — September 11, 2026

All 338 unit tests and 27 targeted browser scenarios in each of Chromium and WebKit pass. The production build passes both engines with no reported errors; Chromium also reloads, opens Army and plays a replay offline with 98 cached files, cache `crown-clan-f0d4678aece6`.

Moving previews retain the placed building's level artwork, size and ground anchor, and refresh when a paid upgrade finishes. The progression panel uses the artwork for its displayed level. The initial catalog check reproduced the old shrinking preview; the corrected run compares every building kind at four requested levels. A phone relocation checks blocked placement, cancellation, actual pointer placement and reload without resource or builder changes. Its first proposed destination overlapped a Gold Mine; the corrected fixture uses clear village tiles.

Visually reviewed the phone Archer Tower moving preview and the TH5 progression card. See [BUILDING-PRESENTATION.md](BUILDING-PRESENTATION.md) for implementation and remaining artwork limits. Evidence: `output/playtest/building-appearance-verification.json`, `building-appearance-production-report.json`, `upgraded-building-move-*.png` and `progression-level-art-webkit.png`.

## Wall balance, free placement and early Cannon progression — September 11, 2026

All 338 unit tests pass. The health/free-placement/TH1 Cannon pass completed 39 targeted browser scenarios in each of Chromium and WebKit. The subsequent TH5 elixir correction completed nine Wall scenarios in each engine, including one new case (40 distinct scenarios covered across the two phases). The final production build passes both engines without reported errors; Chromium also reloads, opens Army and plays a replay offline with 98 cached files, cache `crown-clan-bf593c648a1d`.

Walls use the official reduced level-1–7 health values. New pieces cost zero, remain placeable with an empty treasury, and stop at the Town Hall count limit. TH1 supports two Cannons and their level-2 upgrade. Saved damaged Walls retain their damage fraction after health reconciliation; paid legacy deadlines survive reload. Combat version 11 prevents playback of recordings made under the older Wall health rules while preserving their summaries.

Supercell’s TH5 elixir announcement resolves the conflicting community prose: level 4 → 5 Walls can use elixir. Exact-budget selection works with zero gold, mixed rows containing lower-level pieces reject elixir without partial charges, and successful upgrades persist without timers or reserved builders. See [WALL-PROGRESSION.md](WALL-PROGRESSION.md) and [DEFENSE-SOURCE-AUDIT.md](DEFENSE-SOURCE-AUDIT.md) for sources and remaining limits.

Phone screenshots were visually reviewed for the actual free Wall shop tile, destination health, and elixir-funded group controls. The shop evidence initially captured the horizontal list before the Wall tile was scrolled into view; the capture now explicitly reveals that tile. Earlier test fixtures were updated for free placement, earlier Wall destruction and the reduced health of a Wall Breaker target. Evidence: `output/playtest/wall-current-verification.json`, `wall-current-production-report.json`, `free-wall-shop-*.png`, `th5-elixir-walls-*.png` and `th1-cannon-gate-*.png`. Native footprints, the full catalog, remaining building/troop balance, artwork and online systems are still incomplete.

## Defense source reconciliation — September 11, 2026

All 335 unit tests pass after updating the five defense cost/time tables and the official low-level Cannon health/damage values. The full Chromium run passed 142 of 143 scenarios; its only failure expected the obsolete six-hour Air Defense level-2 timer. The corrected two-hour assertion then passed in Chromium and WebKit, giving all 143 Chromium scenarios passing coverage across the full run and targeted rerun. Nineteen focused WebKit scenarios pass. Production checks pass in both engines with no reported errors, including offline reload, Army and replay in Chromium with 98 cached files.

Migration checks cover old paid construction and upgrade deadlines across reloads: reaching the newly shortened duration cannot finish a previously purchased job early or charge it again. Actual projectile tests verify every accepted Cannon damage tier. The Lightning damage fixture now uses level-4 Cannons so their health exceeds the fixed spell damage, preserving the test's full-damage assertion. Combat version 10 identifies the Cannon correction and retains incompatible result summaries.

Phone Cannon and Wizard Tower Info screens were visually reviewed for health, fractional damage, revised prices/times and control reachability. See [DEFENSE-SOURCE-AUDIT.md](DEFENSE-SOURCE-AUDIT.md) for the rejected stale entries, source limitations and next Wall/TH1 corrections. Evidence: `output/playtest/defense-current-verification.json`, `defense-current-production-report.json` and `*-progression-*.png`.

## Trap progression and vertical spring feedback — September 11, 2026

All 330 unit tests pass, including exact trap price boundaries, instant placement with occupied builders, paid upgrade reloads, accepted legacy levels, blast-radius edges, spring targeting and half hero damage, healing during stun, and campaign viability. Eighteen browser scenarios pass in each of Chromium and WebKit, covering the four phone placement/upgrade flows, hidden traps, replay playback, effect timing and vertical spring presentation. The production build passes both engines without reported errors; Chromium also reloads, opens Army and plays a replay offline with 98 cached files.

The spring arc follows battle time, pauses without drifting, and becomes stationary with reduced motion. Visual review exposed two simultaneous activation labels and sounds; instantaneous springs now emit only their spring feedback, with a browser regression assertion. Phone Info screenshots were checked for damage, growing Giant Bomb radius, spring capacity and upgrade prices/timers. Combat version 9 keeps older summaries while refusing incompatible playback.

The initial new browser fixture used an ambiguous upgrade selector; scoping it to the Info panel resolved the fixture failure. Source reconciliation rejected stale trap references and also exposed a need to re-audit the earlier defense price/time tables. Remaining timing and art limits are explicit in [TRAP-PROGRESSION.md](TRAP-PROGRESSION.md). Evidence: `output/playtest/trap-verification.json`, `trap-production-report.json`, `*-audit-*.png` and `spring-toss-*.png`.

## Air Defense and Wizard Tower — September 11, 2026

All 307 unit tests pass after replacing prototype health, damage, prices and timers with explicit level tables. Twenty-five browser scenarios pass in each of Chromium and WebKit, covering all five defense Info panels, paid upgrade reloads, shop gates, traps, village editing, replay playback, seeking and file sharing. The production build passes both engines with no reported errors; Chromium also reloads, opens the Army drawer and plays a replay offline with 98 cached files.

New combat checks exercise every accepted Air Defense/Wizard Tower damage level, range edges, firing cadence at 20/30/60 fps, inactive upgrades, air-only single-target rockets, rocket impact after launcher destruction, and Wizard Tower's exact one-tile splash boundary on both layers. An existing hero replay fixture needed extra defensive crossfire to reach automatic Iron Fist under the corrected tower health/damage. The replay is still compared exactly against its recording. Combat version 8 preserves older result summaries while refusing incompatible playback.

Phone screenshots were visually reviewed for fractional damage, prices, timers and the singular “1 tile” label. See [AIR-WIZARD-PROGRESSION.md](AIR-WIZARD-PROGRESSION.md) for source values and remaining limits. Evidence is recorded in `output/playtest/air-wizard-verification.json` and `air-wizard-production-report.json`.

## Mortar artwork — September 11, 2026

Six original level sprites replace the two atlas tiers across the village, Info, paid upgrades and moving previews. All 282 unit tests pass. Eighteen targeted scenarios pass in each of Chromium and WebKit, including all six muzzle anchors, airborne shells after launcher destruction, reduced motion, replay seeking and shared defense presentation. Production checks pass in both engines; Chromium reloads, opens the Army drawer and plays a replay offline with 98 cached files. All six new Mortar textures are explicitly required by the production check. The deterministic asset check also passes.

Visual review covers transparent edges against grass, a six-level gallery, the phone Info panel and shell flight. The first new Info test incorrectly tried to reopen a still-open sheet after an upgrade; it now checks the live update. Source alpha near 253 was normalized to full opacity, and generated checkerboard backgrounds were rejected. See [MORTAR-ART.md](MORTAR-ART.md) for provenance and remaining fidelity limits. Detailed evidence is in `output/playtest/mortar-art-verification.json`.

## Earlier milestones

Verified locally on September 10, 2026. The hero/progression pass has **99 passing simulation/save tests**, a successful production build, and **71 precached files**. All **43 browser scenarios** pass across the full run and targeted rerun: the first run passed 40/42, revealing one obsolete Town Hall expectation and a real phone Army-menu clipping bug. After fixing both and adding a dark-elixir collection case, all 22 affected hero/army/expansion/specialist scenarios passed. Other scenarios retained their passing full-run result.

Production checks in Chromium and WebKit pass for boot, shop, research, tab handoff, imported hero saves, gem-finished hero upgrades, pointer deployment and H-key ability use. Chromium also reloads and plays the upgraded hero offline with cached artwork. Reports: `output/playtest/production-report.json` and `hero-production-report.json`. Run `node scripts/production-check.mjs` and `node scripts/hero-production-check.mjs` after a build with the development and preview servers running.

## Defect audit — economy, persistence, editing and escaping (September 11, 2026)

Regression coverage now includes the following economy, persistence, editing and imported-content safeguards. The current verification results are recorded in `output/playtest/replay-audit-verification.json`; older pass counts below describe earlier milestones.

- **Free resources from every new producer.** `makeBuilding` seeded 1,800 stored resources for mines and collectors — intended only for the opening village, but `place` uses the same helper. A Gold Mine cost 1,800 elixir and paid back 1,800 gold; a Collector did the reverse. New buildings now start empty and `initialSave` seeds its own.
- **Collecting from buildings that were not finished.** `collect` tested only `stored >= 1`, so the Collect button drained a mine that was still under construction or mid-upgrade — the renderer already suppressed the bubble for those, so the model and the view disagreed. Collection now skips anything with a timer running.
- **A raid abandoned by closing the tab spent the army for nothing.** Troops leave the camps on deploy and the battle is not part of the save, so a refresh or an app switch lost them with no loot, trophies or stars. `suspendBattle` now settles an attack that has committed troops exactly as surrendering does, and leaves a scouted-but-untouched attack free.
- **Markup in an imported village reached the DOM.** `validateSave` accepts any layout name, and the layout panel interpolated it into `innerHTML` unescaped, unlike the army preset names beside it. The name is now escaped; a browser scenario imports a scripted name and asserts no element is built from it.
- **Restoring a layout could stack two buildings on the same tiles.** Stored arrangements say nothing about buildings raised since, and neither the restore nor save validation checked, so the corruption survived a reload. Restores, undo and redo are now applied only as a whole, and refused with an explanation otherwise.
- **Wall builder rules were inconsistent.** Gold/elixir upgrades and new placement require a free builder, complete instantly, and never reserve that builder afterward.
- **Empty collections all reported the same reason.** Full storages, a Dark Elixir Drill with no storage to empty into, and an unfinished building each said "your collectors are working". Each now names what is actually blocking it.
- **Loot bars promised more than the raid could bank.** `finishBattle` clamped the award to storage headroom while the bars showed the full amount, so a 100% raid into full storages read 8,500 gold during the battle and 0 on the result. Headroom is captured at the start of a campaign attack and the bars, the result and the award now agree. Replays keep showing what the recorded attack earned.

Not changed: dark elixir still needs a Dark Elixir Storage before any can be kept, which is deliberate and separately tested. Only the silent failure around it was fixed.

## Hero and progression pass

- Added 12 model cases covering Hero Hall completion, TH4/TH7/TH8 gates, dark-elixir charges, builder reservations, one-time offline completion, gem finishes, legacy and malformed saves, dark drill downtime and overflow, hero-only combat, duplicate/blocked deployment, ability healing/rage/summons, automatic activation, spring damage/no ejection, defeat/reuse, history, independent building ceilings, and construction/upgrade affordability at every supported tier.
- Added five browser cases covering new art and shop gates, the progression panel, upgrade persistence, real pointer deployment and keyboard ability activation, fresh repeat attacks, portrait/landscape controls, dark collection and reload.
- Fixed the expanded Army action list clipping its first buttons behind the phone drawer header. Actions use a two-column grid and remain scrollable in short viewports. Rechecked existing army/preset and specialist flows.
- Hero-card health updates preserve the actual button node until its interaction state changes. Disabled/spent/dead states update without replacing the card on every economy tick.
- Production hero checks exercise actual import, upgrade, finish, practice, deployment and ability controls; production needs no development model exposure. Chromium repeats this flow offline. Both engines report no browser or asset errors.
- Screenshots under `output/playtest/`: `heroes-desktop.png`, `heroes-mobile.png`, `heroes-landscape.png`, `hero-battle-desktop.png`, `progression-desktop.png`, `progression-mobile.png`, `dark-elixir-village.png`, and `production-hero-*.png`.
- The first screenshots caught a modal mid-animation; final visual captures disable CSS animation. Reviewed source sprites, desktop hero panel/battle, and phone hero/progression panels.
- Reference rules, local balance choices, migration behavior and remaining features are documented in [HERO-PROGRESSION.md](HERO-PROGRESSION.md). Source artwork and exact generation prompts are in [HERO-ASSETS.md](HERO-ASSETS.md).

## Defense and trap pass

- Added 13 simulation/save cases in `tests/defenses.test.ts` for trap placement and count/unlock gates, collisions, construction and upgrades, save round trips and level rejection, concealment, path/deployment exclusion, troop targeting, Bomb/Giant Bomb fuses and escape, air-only tracking splash, single-target springs, ejected death-bomb suppression, oversized knockback, collision-safe pushback, anti-stacking, bomb damage during spring knockback, inactive upgrading traps, fresh practice arming, spell immunity, full-clear scoring, and Wizard Tower targeting/splash.
- Added five browser cases in `tests/browser/defenses.spec.ts`: missing-asset detection and correct shop unlocks; pointer placement, trap info, upgrade and reload; concealed practice traps, pointer activation and repeat reset; phone portrait/landscape controls; campaign miniature concealment and visible Wizard Towers.
- Fixed small ground-trap selection beneath neighboring rooftops, selected shop-category visibility after redraw, and the initial position of a troop ejected before its first rendered frame.
- Original generated RGBA sources are preserved outside the production build. The five shipped WebPs are built with `scripts/defense-assets.mjs`; prompts and source paths are in [DEFENSE-ASSETS.md](DEFENSE-ASSETS.md). Rebuilding all five sprites produced identical SHA-256 hashes (`output/playtest/defense-asset-hashes.json`).
- The 144-battle campaign audit passes after adding traps to stages 2–12 and Wizard Towers to stages 6, 8, 10 and 12. Every stage has a three-star veteran approach, stage 1 remains approachable, and starter armies cannot fully clear the final fortress. The final defense multiplier changed from 1.7339 to 1.55 to offset the new defenses.
- Visually reviewed desktop and phone trap shop, trap information in portrait/landscape, activation in practice, and a campaign Wizard Tower. Screenshots: `traps-shop-desktop.png`, `traps-shop-mobile.png`, `trap-info-desktop.png`, `trap-info-mobile.png`, `trap-info-landscape.png`, `trap-trigger-practice.png`, and `wizard-tower-campaign.png` under `output/playtest/`.
- Live-game costs/timers, exact catalog progression, additional defense and trap types, physical-device performance, and asynchronous defensive attacks remain outside this pass. The enlarged tank fixture tests a future oversized troop branch; the current roster itself has no troop above the spring capacity.

## Army preparation and practice pass

- Added 15 simulation/save tests for free instant preparation, weighted spell housing, editing and underflow, active-battle guards, one-time legacy queue migration, preset capacity and persistence, bounded names, atomic replenishment, practice isolation, campaign results, and the twenty-entry battle log.
- Added five browser tests for real add/remove controls, spell capacity, preset names and reload, practice deployment/casting/surrender, repeat attacks, log persistence, and portrait/landscape results.
- Fixed a redraw race that could replace an in-progress preset name. Unsaved input survives rerenders and is cleared when importing another village.
- Reset scene objects when the battle instance changes, including an immediate result-to-repeat transition. If replenishment fails because a facility is upgrading, repeat attack returns to the Army drawer instead of starting with an incomplete composition. Practice also preserves imported resources above current storage caps.
- Screenshots: `quick-armies-desktop.png`, `quick-armies-mobile.png`, `practice-desktop.png`, `practice-mobile.png`, `practice-result-desktop.png`, `practice-result-mobile.png`, `practice-result-landscape.png`, `campaign-result-landscape.png`, `battle-log-desktop.png`, and `battle-log-mobile.png`, under `output/playtest/`.
- Remaining system-level differences and next priorities are explicit in [EXPERIENCE-PARITY.md](EXPERIENCE-PARITY.md). Passing these tests does not imply complete CoC parity.

## Specialist raiding pass

- Added 13 simulation/save tests in `tests/raiding.test.ts`: Goblin preference and double damage, fallback targeting, path-aware wall breaching, a single explosion per Wall Breaker, weaker researched death bombs, no-wall fallback, training/research/replenishment, mortar blind spot and air immunity, delayed splash and dodging, shells surviving the firing mortar, defense target retention, partial resource looting, full-clear loot, and version-2 save migration.
- Added five browser tests in `tests/browser/raiding.spec.ts`: troop portraits and details, training plus reload, troop/spell hotkeys and actual deployment, mobile tray bounds and selected-card visibility, older-backup import, and enemy mortar inspection during scouting.
- Updated old fixed army fixtures to contain zero of the new troop types, retaining the existing 144-battle campaign balance audit.
- The new sprites use their original generated alpha, with a procedural bob and attack impulse instead of a directional walk atlas.

## Earlier automated coverage (army and practice baseline)

- **74 simulation and save tests pass.** Placement collisions and bounds, construction completion, builder reservations, upgrades, offline resource caps, production resuming after upgrades, storage saturation, instant troop preparation, camp capacity, malformed-save rejection, A\* navigation, deployment boundaries, a complete opening battle, one-time quest rewards, crowd separation, corrupted backup recovery, and IndexedDB unavailability. Research is gated by laboratory level, charges once, completes offline once, and increases deployed troop health and damage. Batch preparation is atomic, troops and spells cost no elixir, and army replenishment checks both housing limits before changing either composition.

  New in this pass:
  - **Scouting phase** — the battle clock holds for thirty seconds, starts on the first deploy, and starts on its own when scouting expires. `deployBlocked` is asserted to be the same predicate the red boundary is drawn from, so the line the player sees and the rule the model enforces cannot drift apart.
  - **Air layer, in both directions** — a ground-only cannon leaves a balloon untouched over 6 seconds of simulation; an air defense leaves a swordsman untouched over the same window; an air defense does tear into a balloon. A wall line stops a ground troop and takes damage from it, while a balloon crosses the same line and leaves every segment at full health.
  - **Spells** — brewing respects spell factory capacity and refuses (without charging) beyond it; Lightning damages every building inside its radius exactly once and nothing outside it; Rage measurably increases damage dealt over a fixed window against an identical control run; Healing restores a wounded troop standing inside it without exceeding its maximum; auras expire and stop applying.
  - **Town Hall gating** — independent per-building TH1–8 ceilings now control upgrades and first unlocks. Info panels report the next required Town Hall. Building counts remain locally tuned.
  - **Timer and gem curves** — upgrade seconds increase monotonically per level, a late Town Hall upgrade exceeds an hour, and the gem curve hits its documented anchors (1 gem at a minute, 20 at an hour, 260 at a day).
  - **Edit mode** — dragging relocates, occupied ground is refused without mutating the building, undo and redo restore exact positions, three layout slots store and restore, and the state stays valid throughout.
  - **Save migration** — a version-1 village fails validation, migrates cleanly, keeps its gold and its existing troop levels, gains `balloon: 0` and an empty spell book, and loads into a working model. A building level above that building's maximum is rejected.
  - **Second pass** — the wall tool re-arms after each segment and puts itself down when the next one is unaffordable; a felled balloon damages the buildings inside its radius exactly once and nothing outside it; a drag across four tiles is a single undo step; research climbs to level 5 behind a matching laboratory and stops there, with the laboratory ceiling asserted equal to the troop ceiling; the tutorial's `built` counter ignores walls, its `trained` counter follows batch size, and a battle's carried spell book keeps a slot recorded after the spell is spent.

- **33 browser tests pass in Chromium.** Boot, collection, pointer selection, upgrading and gem completion, save persistence through reload, shop filtering, placement and cancellation, instant army preparation, actual troop deployment and simulated battle results, campaign unlocking, settings, modal keyboard focus, camera zoom/pan/reset, portrait layout, landscape layout, touch selection, stable sheets during passive resource ticks, export, valid import, invalid import rejection, research at desktop/phone sizes, research persistence, exclusive tab handoff with the newest save, forced WebGL context loss/restoration, and twenty consecutive raids with object cleanup and a final save reload.

  New in this pass:
  - The shop **drawer** is asserted not to block the playfield (`scene.uiBlocked === false`), while a real dialog still does.
  - A building is placed **twice** — once by tapping its price and then tapping the map, and once by dragging the tile art out of the drawer and dropping it on the village — with the building count checked after each.
  - **Drag-deploy**: a press-then-drag across the grass deploys more than two troops along the path and starts the battle; a **double-tap** on one spot deploys five.
  - The **scouting banner** is visible before the first deploy and gone after it, and the destruction bar appears once the battle is live.
  - A **spell** is selected and cast, and the resulting aura is confirmed in the model.
  - **Surrender** opens a confirmation, "Keep fighting" dismisses it, and confirming produces the result screen.
  - The **info sheet** renders its stat table with exactly two improved rows for an Air Defense (hitpoints and damage, not range or attack speed) and shows a real cost and build time.
  - **Edit mode** is driven with the mouse: a building is dragged to a new tile, undo restores it, redo re-applies it, and a layout slot stores more than forty positions.
  - **Town Hall gating** is checked in the shop: a locked building's buy button is disabled and its tile carries no drag handle until the Town Hall is raised.
  - **First-run coaching** shows step 1, rings the Collect button, advances to step 2 and moves the ring to the Shop once a collection lands, marks a filled store with `.full`, and stays dismissed across a reload after Skip.
  - **Wall runs**: three walls are laid with three taps without reopening the shop, the placement banner survives each one, and Escape puts the tool down.

- **Production smoke checks pass in Chromium and WebKit.** The optimized build boots, renders the game, and opens the shop drawer and the laboratory without JavaScript errors. A second tab waits and resumes after the owner closes in both engines.
- **Offline reload passes in Chromium.** The service worker pre-caches the complete production asset manifest (62 files); a disconnected reload renders the village and opens the army drawer. Cache matching tolerates Vary headers on these same-origin static files. Cached assets include local fonts.
- The earlier full rebuild verified the original 46 shipping images. The two added specialist sprites were also rebuilt and checked for identical SHA-256 hashes; both retain genuine alpha transparency.
- TypeScript checking and the Vite production build pass.

## Campaign and endurance evidence

`output/playtest/campaign-balance.json` records 144 complete fixed-step battles: 12 stages × 3 army compositions × 4 starting approaches. All twelve authored layouts are distinct and every occupied tile is checked for overlaps and bounds.

| Army      | Composition (swordsmen / archers / giants / wizards) | Research | Spaces |
| --------- | ---------------------------------------------------- | -------- | -----: |
| Starter   | 14 / 12 / 3 / 3                                      | Level 1  |     53 |
| Developed | 18 / 18 / 8 / 6                                      | Level 2  |    100 |
| Veteran   | 20 / 20 / 16 / 10                                    | Level 3  |    160 |

These are deliberately **ground-only, spell-free** armies, so the matrix measures the same thing it did before the air layer and spells were added. The starter army clears the opening stage, the veteran army clears every stage from all four tested approaches, and the starter army cannot three-star the final fortress. They do not establish balance for every player strategy, and in particular they do not measure balloon-led or spell-supported attacks.

Air defenses were added to stages 5–12 (one each for 5–8, two each for 9–12). Because they cannot fire at ground troops, they are pure additional structure for these ground armies — they raise the bar for 100% destruction without adding threat. Defense damage now scales 12% per building level, and the per-stage `defense` multipliers were divided by the level factor for each stage's enemy tier, so enemy output is unchanged and this matrix stays comparable with the previous run.

The twenty-raid browser check runs full simulated battles, enters results, returns home, and samples scene object counts after effects expire; the recorded counts are in `output/playtest/raid-endurance.json`. Accelerated battles are a transition/cleanup check, not a multi-hour memory certification.

The session mechanism uses an exclusive origin-scoped browser lock; see [Web Locks API documentation](https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API). Only the owner loads and mutates a village. The waiting page acquires ownership after the first tab closes. This requires HTTPS or a trustworthy local origin.

## Performance evidence

Headless Chromium on the local macOS workstation, 1440×960 viewport, device scale factor 1, four-second sampling windows:

| State                                  | Mean frame rate | 95th percentile frame duration |
| -------------------------------------- | --------------: | -----------------------------: |
| Village, decorative troops active      |          59 FPS |                        16.8 ms |
| Opening battle, starting army deployed |          57 FPS |                        16.8 ms |

The specialist-pass sample records 59 FPS in the village and 57 FPS with the full starting army, including Goblins, Wall Breakers and Balloons. Both 95th-percentile frame times were 16.8 ms. These are local observations, not a physical-device performance guarantee. Re-run `node scripts/performance-check.mjs` against the development server for a new report.

The defense-pass static build is approximately 4.2 MB on disk. Its 53 optimized game images occupy approximately 2.6 MB. Original PNGs stay outside the shipped build. The Phaser engine is separately cacheable, approximately 357 KB gzipped; application JavaScript is approximately 45 KB gzipped.

## Visual review

This pass also checks the Goblin and Wall Breaker details on desktop and phone, the expanded army drawer, battle tray scrolling, mortar range inspection, and the phone timer/loot/scouting layout. The redundant delayed welcome toast was removed so it cannot obscure a dialog or scouting controls.

Screenshots in `output/playtest/` cover the home village, the shop and army drawers, the campaign, the scouting phase, an active battle with a red boundary and a cast spell, victory, edit mode, the saved-layouts panel, the building info sheet, mobile portrait, touch-selected buildings, landscape phone, Chromium/WebKit production shop, offline army drawer, and the laboratory on desktop and phone. Reviewed for alpha artifacts, consistent scale and anchors, HUD obstruction, responsive dialog bounds, selection readability, wall connections, and troop animation.

Resolved in the second pass:

- The deployment-boundary cache was keyed only on surviving building ids, so two stages with the same ids and the same survivor count could show a stale outline; the stage index is now part of the key.
- Edit mode recorded one undo entry per tile crossed, so reversing a single drag took as many presses of undo as tiles; a drag now opens exactly one entry.
- A tap that could not deploy still armed the double-tap window, so the next tap deployed five where the player expected one.
- The laboratory could be upgraded to level 8 while research stopped at troop level 3, leaving four levels that bought nothing. Research runs to troop level 5. The new Town Hall progression pass allows laboratory level 6 at TH8; research beyond troop level 5 remains unfinished.
- `retrain` returned a bare `undefined` when it had brewed spells but no troops to train, which read as a silent failure.
- Relocating a building outside edit mode pushed an entry onto an undo stack nothing could reach.

Resolved during the first pass:

- The red deployment boundary was first drawn in the overlay layer and crossed over rooftops; it now draws in a ground-marks layer beneath every building.
- Opening the building info sheet cleared the very selection it describes, because every panel ran `model.cancel()`; the info panel now preserves it.
- The shop tile's price button also started a drag, so a single tap ran `beginBuild` twice; the price button is now a tap target and the rest of the tile is the drag handle.
- Battle labels ("Total destruction", the loot caption) were unreadable over bright terrain and now carry a stronger shadow; loot totals moved inline onto their bars.
- The scouting banner overlapped the toast strip and moved below it.
- The edit-mode caption and hint collided with the toolbar; the hint folded into the caption and the caption moved to the top.
- Starting gold initially exceeded the starting storage capacity, which silently made collection a no-op.
- The starting village's air defense was first placed on ground the shop tests build on.

Resolved in the previous pass (retained):

- Rejected an animation sheet with a baked-in checkerboard; normalized a real-alpha replacement.
- Corrected atlas extraction order and cell boundaries.
- Added continuous wall connections and a coherent fortification layout.
- Kept menus mounted during passive resource ticks and kept the import file input outside replaced dialog markup.
- Restored keyboard focus to current DOM controls after modal closure; made the background HUD inert beneath dialogs.
- Prevented a corrupt local backup from hiding a healthy IndexedDB save.
- Fixed cached-module misses caused by Vary headers during disconnected reloads.
- Stopped upgrading collectors from producing during their construction interval.
- Added deterministic troop separation and consistent attack/death feedback.
- Replaced repeated enemy layouts and eliminated overlapping enemy footprints in later stages.
- Added explicit troop research and removed the hidden laboratory damage multiplier so displayed troop damage matches normal attacks.
- Protected saves from concurrent tabs, including reloading the latest state when ownership transfers.

The visual review also covers the first-run coaching banner (`coach-desktop.png`), the player profile and achievement list (`profile-desktop.png`), and the five-level research panel (`research-desktop.png`).

## Known limits

- Army preparation is now free and instant. Build, upgrade, and research prices/timers still use a simplified local economy. Factory capacity grows by two housing spaces per level; exact live-game progression remains unimplemented.
- The campaign matrix does not cover balloon-led, specialist-led, or spell-supported attacks; focused interaction tests cover the new specialists.
- Specialist health/damage and the resource loot shares are tuned for this local campaign, not the current live game economy.
- Air troops have a float cycle but no distinct attack or death animation.
- Most buildings share base artwork at levels 1–4 and final-tier artwork at level 5 and above. Walls have distinct artwork at levels 1–8 and Mortars at levels 1–6; the other buildings still need per-level art.
- Physical iOS/Android performance, multi-hour sleep/resume endurance, and accessibility review remain release gates.

## Replay audit — September 11, 2026

- Recording, seeking, speed controls, portable files and read-only playback are covered by model tests and real browser input in Chromium and WebKit.
- Version 4 records raid-limited storage headroom. Regression cases compare final loot and results, verify that home research uses home levels while watching, and bound tiny-step playback and reconstruction work.
- Imported replay limits preserve older recordings as incompatible summaries. Unsupported new recordings are omitted without invalidating the village save.
- Portrait and landscape playback targets are at least 44 CSS pixels; the 320px layout and 844×390 production view were visually reviewed.
- `npm run test:production` now records a practice attack through the canvas, watches/seeks/restarts/exits the replay in Chromium and WebKit, compares home resources/army, and reopens the saved recording offline in Chromium.
- Reports and screenshots: `output/playtest/replay-audit-verification.json`, `production-replay-*.png`, and `replay-controls-*.png`.

## Campaign rules — September 11, 2026

- Source reference now preserves all 90 native villages, 17,686 building placements and 95 source hashes; it is separate from the twelve playable authored layouts.
- Campaign scouting and combat are untimed. Per-village loot persists across attempts and reloads, overflow is reported, and results do not change trophies. Existing saves retain their resources, stars and historical results.
- The full model/asset suite passes 679 tests in 61 files. The 288-battle matrix resolves naturally, including two attacks beyond three minutes and a 527-second maximum. A focused regression fixes a ranged unit stranded outside attack range despite its cell center being in range.
- Chromium and WebKit each pass 31 browser scenarios at DPR 2. Settled campaign map, battle and result captures cover desktop, portrait and landscape. Surrender remains outside the scrolling army tray at tablet widths.
- `npm run test:campaign:production` verifies the built bundle's finite inventory, five-minute replay, portable file sharing and reload in both engines. Chromium repeats reload and sharing offline; WebKit offline remains unchecked.
- Details and transition/rounding limits: [CAMPAIGN-RULES.md](CAMPAIGN-RULES.md). Evidence: `output/playtest/campaign-rules-verification.json` and `campaign-*.png`.

## Goblin building foundation · September 11

683 model tests pass, plus six NPC rendering scenarios across Chromium/WebKit and five Chromium artwork/replay regressions. Both production engines import, seek and export the new NPC identities; Chromium also reloads their art and opens the recording offline. The playable campaign is still the authored twelve-map catalog. See [GOBLIN-BUILDINGS.md](GOBLIN-BUILDINGS.md) for exact scope and evidence.


## September 12: native Dark Elixir Storage and three-resource campaign accounting

[Storage implementation and fidelity limits](DARK-ELIXIR-STORAGE.md) and [campaign rules](CAMPAIGN-RULES.md) describe the finished pass. The full model suite passes 972 cases across 77 files. Twelve affected browser cases pass per engine at DPR 2, plus two settled Chromium Info capture reruns. Fourteen imported assets, thirteen source levels and all 160 control frames per level pass reproducibility checks. Independent original-texture raster references cover 39 level/fill pairs; GPU mean maximum-channel error stays below 0.657/255, with no rendering change after context restoration or forced single-texture batching.

General, hero and Dark Elixir replay production checks pass in Chromium and WebKit with no page/asset errors. Chromium verifies the 264-file `crown-clan-501ab0fbe562` offline cache, hero behavior and standalone three-resource replay export/import/rewind. The production replay uses an explicit supported-entity fixture with a native level-13 storage and source catalog loot; it does not bypass guards on the real campaign layout. Expected result is 700 Dark Elixir received and 550 lost to capacity, with unchanged viewer balances. WebKit offline behavior remains unchecked.

Evidence is indexed by `output/playtest/dark-storage-verification.json`, with logs, source/release hashes, GPU summaries, screenshots and three production reports alongside it. Exact native-client world registration and resource-control thresholds, storage construction/upgrade/effects/ruins, later campaign mechanics and physical-device qualification remain unfinished. First 50 native campaign villages remain playable; later unsupported levels are still reported honestly.


## September 12: native Goblin buildings and passive Town Hall levels

[The final implementation](GOBLIN-BUILDINGS.md) replaces placed Goblin Hall/Hut recreations with native polygons, textures, flag animation and foundations. Source hitpoints now cover all eleven passive levels and all 71 Hall placements in the 90-layout catalog. Replay validation distinguishes those levels from the Home Village ceiling while rejecting weapon-bearing Hall 12. Invaders now reports only Hidden Tesla 7 and Bomb Tower 3 as its remaining gates; the complete playable campaign remains the first 50 stages.

987 model cases pass across 78 files. Eight integrated browser cases pass per engine, followed by a corrected lifecycle case passing in each engine: nine affected cases total at DPR 2. The lifecycle assertion now orders retained meshes by their render depth when comparing flag poses. An earlier GPU fixture error that sampled every flag at time zero was corrected to use each witness time; source comparison thresholds were not relaxed. Twelve independent source-pixel cases cover all flag states, Hut, foundations and a long clock. Maximum per-case mean channel error is below 0.785/255, with no compared pixel exceeding 16/255; batching and WebGL context restoration change zero pixels.

General and NPC production checks pass in Chromium/WebKit with no page/asset errors. The NPC fixture proves a level-11 Hall at 6,800 HP survives portable export/import and seeking. Chromium verifies offline native preview decoding and shared replay reopening using the 270-file `crown-clan-459fed3dacb2` cache. WebKit offline operation and physical devices remain unchecked.

`output/playtest/native-goblin-verification.json` indexes exact source/runtime hashes, logs, the resolved fixture/assertion failures, GPU metrics and final production reports. Campaign source regeneration checks all 97 pinned inputs; the Goblin importer checks all six assets and source frames. Native-world projection, foundation association/persistence, scouting flag behavior, destruction/effects/audio and remaining campaign mechanics are documented limits, not completed fidelity claims.

## September 12: native Tesla asset capture and isolated electrical groups

The [Tesla import](../reference/tesla/README.md) preserves all 17 source levels, 54 exports, 124 shapes, 65 clips, twelve effect groups, twelve particle records and five native sounds. Twenty-six assets reproduce exactly from the pinned client files, including copied sampling regions and 17 distinct portraits. The shared Python reader's hidden-control behavior passes all 19 reader tests; the existing Goblin asset regeneration still matches.

The [native scene renderer](NATIVE-SCENE-RENDERER.md) preserves seven isolated additive subclips. Independent original-texture fixtures cover twelve Tesla reveal/electricity/effect cases. Both Chromium and WebKit pass at DPR 2, with per-case mean maximum-channel error below 0.58/255, zero pixel changes after context restoration or forced single-texture batching, and no GL errors. Separate browser coverage verifies the overlap-alpha calculation, physical zoom, retained objects, idle-group removal and complete framebuffer/listener cleanup. The three existing X-Bow, Dark Elixir Storage and Goblin GPU scenarios also pass in each engine: five affected browser scenarios per engine in total.

1,005 model tests pass across 80 files. The production build and general production checks pass in Chromium/WebKit with no page/asset errors. Chromium verifies the 296-file `crown-clan-322baf7799b4` offline cache. WebKit offline operation remains unchecked. Evidence includes `output/playtest/native-tesla-renderer-verification.json`, source-reference PNGs and `native-tesla-mesh-{chromium,webkit}.png`.

The work fixes two observed Phaser 4.2.1 problems: custom blend registration returned the preceding index, and framebuffer restoration tried to delete invalid handles from the lost context. The corrected tests retain the original pixel thresholds and check GL errors across restoration. This is an asset and renderer foundation; live Tesla placement/combat/replays still use the preceding six-level presentation. No campaign gate was bypassed or unlocked. Native executable compositing, particle trajectories, world registration, sound selection and complete Tesla integration remain unfinished.


## September 12: live native Tesla levels, emergence and replay integration

[The Tesla implementation](HIDDEN-TESLA.md) now renders all 17 source levels, opening trapdoors, independent electrical groups, source construction/upgrade scaffolds and the fixed damaged-building composition. The original reveal Ogg joins the shared battle-clock audio mixer. The runtime reads all source HP/DPS/cost/timer/Town Hall rows; home purchases retain TH7 level 3 and TH8 level 6. Invaders now reports Bomb Tower 3 alone, and the first 50 native villages remain the complete playable campaign.

1,025 model/asset tests pass across 80 files. Sixteen affected browser scenarios pass per engine at DPR 2: ten Tesla gameplay/presentation/replay scenarios, two native Tesla GPU/lifecycle scenarios, three existing native-building GPU regressions and the shared X-Bow replay/audio scenario. The final ten Tesla scenarios were rerun with settled frame captures. They include phone construction and upgrade scaffolds, destruction during emergence, original reveal sound gain/speed/stop, and exact retained mesh/group equality after level-7 and level-17 replay seeks. Independent Tesla source-pixel error remains below 0.58/255 mean maximum-channel error, with zero batching/restoration pixel changes and all checked GL errors zero.

The build and general production checks pass in Chromium/WebKit with no page/asset errors. The 296-file offline cache is `crown-clan-08d14307def0`. `npm run test:tesla:production` imports a supported-entity recording containing Tesla level 17 at 1,750 HP through the shipping UI, verifies hidden/raised/rewound states, exports the original level and checks the final result. Chromium repeats portrait decoding for all 17 levels and the recording checks offline; WebKit offline remains unchecked. This fixture does not bypass any campaign or home-purchase guard.

The validation harness now waits two rendered frames for scene screenshots, resumes Phaser synchronously for active-audio checks and resolves the current production replay slider in the browser task that dispatches its input/change events. These address observed stale-frame, paused-scene and detached-element test failures without relaxing runtime assertions or pixel thresholds. The scouting text assertion also checks the actual public `type` field.

Evidence is indexed in `output/playtest/native-tesla-live-verification.json`, including implementation/source/build hashes, model/browser logs, native GPU metrics, settled screenshots and both production reports. Native beam/secondary/hit/grass particles, zap/pickup/drop audio, camera shake, executable world/timeline behavior, later campaign mechanics and physical device qualification remain unfinished.

## September 12: native Tesla attack arcs, impacts and zap audio

[Tesla attacks](HIDDEN-TESLA.md) now use original animated arc polygons, level-specific coil bursts, impact textures and both native zap samples. Source emitter counts, lifetimes, normal/additive flags, scale/fade settings and sound gain/pitch ranges drive the presentation. A bounded derived shot history reproduces particles and sound through pause, speed changes and replay seeks without changing combat randomness, damage or cadence. Arc endpoints follow the source reveal geometry and retain source/target death positions until expiry. The previous authored polyline and synthesized crackle are removed.

**1,041 model/asset tests pass across 81 files**, followed by a successful TypeScript/production build. **Fourteen distinct browser scenarios pass per engine at DPR 2**: eleven Tesla gameplay/presentation/replay cases, two independent GPU/lifecycle cases and shared X-Bow replay/audio coverage. The final death-position change was checked with 36 focused model tests and the death/lifecycle plus level-7/17 replay scenarios in each engine, followed by the complete model/build run. Those checks verify exact particle mesh equality after seeking, native audio gain/speed/stop, reduced motion, final death positions, expiry and listener disposal.

All 26 native assets and the sparse effect/emitter projection reproduce from pinned sources. **Nineteen independent source-pixel witnesses** now include rotated/reflected arc variants, both emitter blend modes, impact textures and the level-10 coil burst. Maximum per-case mean maximum-channel error is **0.550/255 in Chromium and 0.767/255 in WebKit**. The largest per-case fraction of pixels exceeding 16/255 channel error is below 0.027%; the worst individual edge difference is 35/255. Forced single-texture batching and context restoration change zero pixels; all checked GL errors are zero. These are sampled source-art comparisons, not native executable equivalence.

General production and native level-17 portable replay checks pass in both engines without page/asset errors. Chromium passes offline reload, Army, replay and all native Tesla portrait decodes using the **296-file `crown-clan-951d985dc6ec` cache**. The production fixture retains source level 17 and 1,750 HP through export/import and concealment rewind; home/campaign guards are unchanged. WebKit offline and physical devices remain untested.

Visual review confirmed coil placement at the configured source height. One earlier WebKit GPU lifecycle attempt failed to import a module and passed unchanged on rerun; its cause is undetermined. No source-pixel tolerance was relaxed. Evidence is indexed in `output/playtest/native-tesla-attack-verification.json`, with final source/build checks, browser logs, GPU metrics and frozen production reports.

Native particle projection, targeted radius/inertia/rotation semantics, sound-row selection, collision restitution and reveal handoff remain documented local interpretations. Grass emission, pickup/drop playback, reveal shake, Bomb Tower 3 and later campaign mechanics remain pending. The playable native campaign remains its first 50 villages and the full production-clone goal remains active.

## September 12: native Tesla emergence grass

The [Tesla reveal](HIDDEN-TESLA.md) now includes the original three-piece grass emitter and all four source grass variants. It preserves source count, emission/lifetime ranges, normal blending, scale and fade settings. The shared deterministic emitter reconstructs the same positions and mesh data after pause and rewind. Grass survives destruction during emergence and clears on expiry, battle finish, concealment rewind and reduced motion. Ground sorting and ballistic projection remain local interpretations; native inertia and depth/collision semantics are unverified.

**1,046 model/asset cases pass across 81 files**, with TypeScript, formatting and the production build passing. **Thirteen affected browser scenarios pass in each engine at DPR 2**: eleven Tesla gameplay/replay cases and two source-pixel/lifecycle cases. No test failed during this pass. Settled emergence screenshots were captured in Chromium/WebKit and visually reviewed. All 23 independent GPU witnesses pass; maximum per-case mean maximum-channel error is **0.611/255 in Chromium and 0.829/255 in WebKit**. Each grass variant differs by at most **2/255** in any channel. Context restoration and forced single-texture batching change zero pixels, and all checked GL errors are zero. The original 26 shipped assets and pinned references are unchanged; the expanded 23-case reference sheet reproduces from original textures.

General production and level-17 portable replay checks pass in Chromium and WebKit with no page/asset errors. Chromium verifies offline reload, Army, replay, all 17 portrait decodes and portable export/import/rewind with **296 cached files in `crown-clan-6302f2940122`**. WebKit offline and physical devices remain unchecked. Evidence, exact hashes, source-pixel metrics and frozen production reports are indexed by `output/playtest/native-tesla-grass-verification.json`.

Pickup/drop audio and associated grass bursts, reveal camera shake, native executable motion evidence and later campaign mechanics remain pending. This pass adds emergence grass only; the playable native campaign remains its first 50 villages.

## September 12: native Tesla pickup/drop and placement cancellation

[Home Tesla handling](HIDDEN-TESLA.md) now plays the original pickup/drop files at source 80% volume and normal pitch, with the original three-piece grass emitter. The home render clock drives a bounded transient queue separate from saves/replays. Canvas taps, shop drags and edit gestures share successful-action feedback. Invalid drops emit no placing effect, one edit drag retains one undo step, and cancel/mute/pause/battle transitions clean up the effects. Generic Tesla placement tones are removed, and successful moves/cancellation clear stale placement warnings.

An existing purchase bug was reproduced: a shop `pointercancel` reached the normal drop handler and bought the building. Commit `06c7a8d` handles cancellation before placing or spending resources, with an independent generic shop regression. **1,052 model/asset cases pass across 82 files**, followed by a passing TypeScript/production build and formatting checks. **Twenty-two distinct affected browser scenarios pass per engine at DPR 2**: eleven existing battle Tesla cases, six new home handling cases, three UI input cases, one editor case and shared X-Bow replay/audio coverage. The final six home cases were repeated in each engine after the stale-warning fix. Actual sources play once per accepted action at gain 0.096 and rate 1; pause, mute, reduced motion, cancellation, scene cleanup and undo/redo assertions pass. Settled desktop/phone placement screenshots were visually reviewed.

The browser harness initially dragged the price button, then sampled the decorative image before its drawer animation settled. It now waits on the real art-container handle; the image intentionally ignores pointer events. The listener baseline now includes the first rendered home Tesla. Two existing UI assertions were stale: Barracks can be visible at its purchase limit, and closing Quick armies restores the Army drawer. Those assertions now verify the intended current behavior. An overlapping failed run also lost one trace artifact during output cleanup; final browser runs are serial within each engine. Runtime assertions and source-pixel tolerances were not relaxed.

General production and level-17 portable replay checks pass in both engines without page/asset errors. All five original Tesla sounds are requested, joining sixteen decoded native samples across Tesla, Santa and X-Bow. Chromium passes offline reload, Army, portrait decoding and portable replay import/export/rewind with **296 cached files in `crown-clan-ac3614d5a00a`**. WebKit offline and physical devices remain unchecked. The original 26 Tesla assets, pinned references and 23-case source-pixel evidence remain unchanged from the preceding grass pass.

`output/playtest/native-tesla-handling-verification.json` records exact source/runtime/build hashes, browser/model logs, the reproduced purchase failure, resolved fixture issues, settled screenshots and frozen production reports. Native reveal shake, executable particle/attachment/projection evidence and later campaign mechanics remain unfinished; the full clone goal stays active.

## September 12: Tesla reveal camera motion

[Tesla emergence](HIDDEN-TESLA.md) now includes camera motion for the source's **200 ms** window and honors its replay flag. Its deterministic local waveform derives directly from reveal timestamps without consuming combat randomness or changing camera scroll. Repeated samples, pause and seek produce the same transform. The camera layer composes with existing Phaser effects, and screen projection now uses the same transformed matrix as pointer picking. Reduced motion, scouting, expiry, battle completion and home exit suppress the native layer immediately; shutdown restores its hook.

**1,056 model/asset cases pass across 83 files**, with TypeScript, formatting and the production build passing. **Twenty-three browser scenarios pass per engine at DPR 2**: three new shake cases, three existing camera cases, eleven battle Tesla cases and six home handling cases. The new cases check desktop/phone pointer round trips, zoom scaling, unchanged pan, wall-clock and system pause, source destruction, 1×/2×/4× replay, rewind, home-state isolation and shutdown. The camera suite also checks terrain coverage through viewport changes, zoom extremes and camera bounds. Settled Chromium/WebKit emergence screenshots were captured; the phone WebKit image was visually reviewed.

The initial replay fixture compared the live view at zoom 1 with a freshly opened replay's default zoom. It now explicitly uses the same zoom for that comparison; no runtime behavior or equality tolerance was relaxed. General production and native level-17 portable replay checks pass in both engines without page/asset errors. Chromium passes offline reload, Army and portable replay export/import/rewind with **296 files in `crown-clan-e20fffe5b481`**. Source Tesla assets, source graphs and the preceding 23-case independent pixel-reference evidence remain unchanged.

The original camera-engine waveform is not available: source strength 20 is locally mapped to 6.4 world pixels per axis, with interpolated 30 Hz noise, tapering and bounded combined impulses. Native amplitude conversion, distance attenuation and combination rules remain unverified. Existing unrelated Phaser shakes still use their previous wall clock. `output/playtest/native-tesla-shake-verification.json` indexes the exact runtime/build hashes, reports, logs and the resolved fixture discrepancy. Bomb Tower level 3 and later campaign mechanics remain pending; the playable native campaign is still its first 50 villages.

## September 12: native Bomb Tower asset foundation

[The native Bomb Tower reference](../reference/bombtower/README.md) preserves all 13 source bodies, separate foundation/scaffolds/rubble, three projectiles, four death bombs and three rooftop defender families with three idle/attack directions each. Two independent SC6 graphs retain **43 exports, 53 clips and 163 shapes**. Five texture crops and six unchanged Ogg files total **1,038,614 bytes**. The importer verifies 19 input hashes against the pinned client fingerprint and reproduces every output pixel, sound byte and reference field. Thirteen effect records, 25 emitter records and all defender animation-table columns are retained. General impact/explosion particle artwork and live integration remain pending.

The native animation CSV uses independent named tables with varying columns. The importer preserves each selected table's own header, including action frame 11, 24 fps, 101-frame idle and 21-frame attack clips. Level 3 changes to a different defender, projectile, hit effect and destruction effect, in addition to its 750 HP, 32 DPS and 220 death damage. This pass does not increase entity limits or bypass campaign guards. The live game still uses authored Bomb Tower art at levels 1–2; the native catalog remains its first 50 villages.

**1,061 model/asset tests pass across 84 files**, followed by TypeScript and the production build. The initial suite run overlapped both GPU comparisons and CPU source reconstruction; an unchanged Sweeper image test exceeded its five-second limit. Running the full suite without that load passed with no timeout or assertion changes. The importer and all **45 independent source compositions** reproduce from original textures. GPU comparisons pass in Chromium and WebKit at DPR 2: maximum per-case mean maximum-channel error is **0.473/255** and **0.607/255**, respectively. Maximum sparse channel difference is **20/255**, with fewer than **0.0035%** of colored pixels in any case exceeding 16. The normal canvas's forced single-texture batching and context restoration each change zero bytes, and all six final GL checks are zero. The original source sheet and final WebKit GPU sheet were visually reviewed.

Initial pixel comparisons exposed 5–8 high-contrast differences on the three tiny projectile fuses. Source/packed geometry, all sampled poses and texture regions matched exactly. The default canvas has four-sample antialiasing, whereas the CPU witness samples pixel centers; comparing through a single-sample camera framebuffer resolves those differences without changing artwork, runtime code or the original tolerance. Lifecycle checks remain on the production canvas. A diagnostic run restoring context while forced full-camera compositing was enabled emitted GL `INVALID_OPERATION`; that optional Phaser path remains unqualified and is not used by the production scene. The final fixture performs the single-sample comparison after restoring the normal canvas and its native isolated groups.

General production checks pass in Chromium and WebKit without page/asset errors. Chromium offline reload, Army and replay pass with **307 files in `crown-clan-7f7af767ce12`**. Runtime TypeScript/CSS remains byte-identical to the preceding verified Tesla shake commit; the service worker adds the eleven prepared native assets. `output/playtest/native-bomb-tower-foundation-verification.json` records source/runtime/build hashes, pixel metrics, diagnostic and final logs, screenshots and a frozen production report. Live body/defender registration, direction and animation timing, native weapon presentation/audio and level-3 campaign integration are next.

## September 12: native Bomb Tower live presentation

[The Bomb Tower](BOMB-TOWER.md) now draws its original foundation/body, directional rooftop defender, construction/upgrade scaffolds, rubble, thrown bomb, ground shadow and exposed charge. Thirteen transparent source portraits share the live ground registration for shop, Info and moving previews. Native polygon bounds drive picking and progress/health bars; incoming projectile attachment excludes transparent portrait padding. The original additive defender fuse remains isolated on the GPU. Source frame 11 aligns to actual launch timestamps using bounded derived history, and pause/rewind reconstruct animation without changing damage, targeting, cadence or combat randomness.

**1,064 model/asset tests pass across 85 files**, followed by TypeScript, formatting and the production build. **Twenty-six distinct affected browser scenarios pass per engine at DPR 2**: seven Bomb Tower cases, three shared building-presentation cases, two native pixel-comparison cases, eleven existing Tesla cases and three camera-shake cases. The Bomb Tower cases cover desktop/390/320-pixel Info/reload, touch placement, real construction and upgrade timers, source throwing frames, persistent projectile registration after destruction, the one-second charge, replay reconstruction and reduced motion. Final Bomb Tower gameplay checks ran again after correcting incoming projectile height. No assertion tolerance or timeout was relaxed.

The importer reproduces **24 assets totaling 1,878,305 bytes** from the same 19 pinned inputs: five source texture crops, thirteen independently rendered portraits and six original Ogg files. All **45 individual source compositions** retain their preceding GPU results: maximum per-case mean maximum-channel error **0.473/255 in Chromium and 0.607/255 in WebKit**, zero changed bytes across single-texture batching/context restoration, and six zero GL errors. The new comparison renders all **13 complete live assemblies** at the original portrait sampling density. Maximum per-case means are **0.275/255 and 0.303/255**; one level-11 edge pixel differs by 23 and 22 respectively, below **0.0028%** of colored pixels in that case. All assembly GL checks are zero. Source/packed sampling tolerances are unchanged. The complete WebKit assembly sheet and Chromium flight/fuse screenshots were visually reviewed.

General production checks pass in both engines without page/asset errors. A new portable level-2 replay test imports through the production UI, verifies firing at 0.30 seconds and an exposed fuse at 2.75 seconds, compares the final result, exports/imports the recording and rewinds both states. Chromium repeats the portable replay offline and decodes every source portrait. Offline reload, Army and general replay also pass with **320 files in `crown-clan-b96ab6654a75`**. WebKit offline and physical devices remain unchecked. Frozen reports, exact runtime/build/asset hashes and all logs are indexed by `output/playtest/native-bomb-tower-live-verification.json`.

Native world projection, directional/mirror selection, attack-scale inheritance, zero-based action-frame handoff and ballistic conversion remain documented local interpretations. The exposed bomb uses original art, source 115% scale and a local ground anchor; native emitter offsets, 2.5-second lifetime/fade semantics and full particle-engine behavior remain unverified. Original impact/explosion artwork and sound playback are still pending. Gameplay stays at levels 1–2 and the native catalog's first 50 villages; level 3 is not enabled by this presentation milestone. The full production-clone goal remains active.

## September 12: native Bomb Tower particles, audio and explosion motion

[The Bomb Tower](BOMB-TOWER.md) now uses the original smoke, debris, fire, rings and crater for projectile trails, impacts, collapse and the death explosion. All six original sounds play with source volume/pitch ranges, including pickup/drop feedback for accepted home gestures. Recorded shot, landing, destruction and charge timestamps reconstruct the effects and a one-second camera impulse without changing combat randomness or damage. Trails persist briefly after impact, empty landings still emit feedback, repeated/spawned source rows remain distinct, and the crater keeps its source 100-second lifetime while a battle is active. Reduced motion keeps subdued stationary ground markers; pause, mute, cancellation, replay seeking, finish and home exit clean up or rebuild the appropriate graphics and actual audio nodes.

**1,071 model/asset tests pass across 86 files**, followed by TypeScript and the production build. Formatting passes for all 20 changed TypeScript/MJS files. An additional repository-wide formatting check reports 30 unchanged files and the new Python-generated particle manifest; generated witnesses retain the importer's canonical JSON. **Thirty-nine affected browser scenarios pass per engine at DPR 2**: seven existing Bomb Tower cases, six new effects/handling cases, three source-pixel/live-assembly comparisons, eleven battle Tesla cases, six Tesla home cases, three Tesla camera cases and three shared building-presentation cases. The new tests compare actual meshes and isolated group contents through live/replay/rewind at 1×/2×/4×, desktop/phone camera-pointer round trips and zoom scaling, actual sample buffers/pitch/gain, empty landings, mute/pause/resume, cancellation, reduced motion and listener cleanup.

The importer reproduces **27 assets totaling 2,240,499 bytes from 22 pinned inputs**: eight original texture crops, thirteen portraits and six unchanged Ogg files. The new particle graph contains **39 exports, 44 clips and 57 shapes**. All **84 independent source witnesses** reproduce from original textures and pass GPU comparisons. The 39 new particle cases have maximum per-case mean maximum-channel errors of **0.664/255 in Chromium and 0.853/255 in WebKit**, with a maximum channel difference of **2/255**. The previous 45 body/defender cases and 13 complete assemblies retain their preceding results. Forced single-texture batching and source-graph context restoration change zero bytes. A separate live explosion with 13 isolated additive groups also survives context loss with **zero changed bytes** in both engines. All reported GL checks are zero. Settled phone combat/placement and production replay screenshots were visually reviewed.

The first particle comparison rejected two original debris exports solely because they covered 62/64 pixels, below a body-oriented minimum of 100. Their eligibility check now permits small original primitives; all pixel-error criteria remain unchanged. Replay tests now record uniform 50 ms steps so intermediate seek targets exist and freeze the independent home economy's wall-clock timestamp during state comparisons. Production screenshots wait for two animation frames after synchronous model seeking so the canvas shows the new explosion instead of the preceding fuse frame. Final browser and portable runs pass after these harness corrections; no runtime equality tolerance or timeout was relaxed.

General production and portable level-2 replay checks pass in both engines without page/asset errors. The production UI imports, exports and rewinds firing at **0.30 seconds**, the fuse at **2.75 seconds** and the explosion at **3.60 seconds**, reproducing the final one-star/50% result. All 27 native assets load. Chromium repeats the recording offline, decodes all thirteen portraits and all six original sounds, and passes general offline reload/Army/replay checks with **323 cached files in `crown-clan-e308cee0d1d2`**. Exact source/runtime/build hashes, frozen reports, metrics and logs are indexed by `output/playtest/native-bomb-tower-effects-verification.json`.

Native particle inertia/slowdown, single-rebound restitution, fade-field overlap, variant/depth/ground projection and camera amplitude/waveform remain documented local interpretations. Explicit explosion/spawned-ground sound rows both play; native duplicate sound selection and impulse combination are unverified. The exposed charge retains the existing one-second gameplay lifecycle and local registration. WebKit offline, physical devices and complete native executable equivalence remain unchecked. Gameplay stays at levels 1–2 and the native catalog's first 50 villages; the level-3 numerical/campaign audit and the full production-clone goal remain pending.

## September 12: complete Bomb Tower progression and native Invaders

[Bomb Tower combat](BOMB-TOWER.md) now consumes all thirteen pinned source levels for health, damage, death charges, prices, destination timers and Town Hall requirements. Saves and practice retain those supported levels; home purchases/upgrades still enforce the Town Hall 8 maximum of level 2. The Info panel displays the level-3 comparison and Town Hall 9 requirement. The existing authored stage rule now assigns level 3 to its stages 9–12. Save version 4 and replay version 32 remain compatible with their previously accepted level-1/2 towers.

Invaders, native village 51, is playable with all **272 original entities**, its level-3 Bomb Tower and four level-3 X-Bows in their original targeting modes. The tower uses **750 HP, 35.2 damage per throw and 220 death damage**, plus the original second defender/projectile/charge families and second hit effect. A legal 200-space Giant/Dragon fixture fires at 1.70 seconds, arms the death charge at 2.45 seconds and resolves it at 3.45 seconds. It settles naturally at **two stars and 65% destruction**, conserves all three source loot pools, credits progress once and reconstructs the complete final battle through replay. The catalog now supports exactly its first **51 villages**; village 52 still requires Seeking Air Mine level 3.

**1,119 model/asset tests pass across 88 files**, including 153 native campaign battle scenarios, all thirteen tower damage/death levels, source-row comparisons and portable replay at every visual-family transition plus level 13. TypeScript, the production build and scoped formatting pass. **Twenty-five distinct browser scenarios pass in both Chromium and WebKit at DPR 2**: thirteen existing Bomb Tower gameplay/effects/handling cases, four Invaders/upgrade-panel cases, four native campaign cases and four native Skeleton Trap cases. The four new cases were rerun after disabling CSS transitions during screenshots and requiring nonempty source meshes. Desktop/phone combat, 390/320-pixel Info, original variant selection, full-state replay equality, home isolation, audio/context lifecycle and checked GL errors pass. An actual version-32 level-2 file exported by the preceding implementation still produces its original one-star/50% result.

The 22 pinned source inputs and 35 retained source/asset/particle-witness files match the preceding implementation byte for byte. No art graph, texture, native pose or particle equation changed in this milestone; the previous 84 source-witness and 13 live-assembly pixel comparisons remain recorded in the preceding evidence rather than being claimed as new runs. The live additive-explosion context test ran again in both engines with zero changed bytes and zero GL errors. Settled desktop/phone combat, Info and production fuse/explosion captures were visually reviewed.

General production checks and both portable replay fixtures pass in both engines without page/asset errors. `npm run test:invaders:production` opens the restored village's campaign gate through the shipping UI, imports/exports the complete 272-entity recording and rewinds firing at **1.80 seconds**, the fuse at **2.75 seconds** and the explosion at **3.60 seconds**. The existing level-2 production fixture also passes. Chromium repeats both recordings offline and decodes all thirteen original portraits and six sounds. The **323-file cache is `crown-clan-32db0a22be32`**. Frozen reports, compatibility evidence, hashes and logs are indexed by `output/playtest/native-bomb-tower-progression-verification.json`.

Initial validation caught two stale campaign assertions at the old 50-village boundary, an incorrect test assumption about the portable parser's return type, an incorrectly named level-3 hit emitter and a raid expectation derived from a different fixture. These were corrected to the actual API, source rows and shared legal fixture; no numerical tolerance, campaign guard or runtime behavior was weakened. Native executable timing/projection/particle equivalence, later campaign mechanics, WebKit offline and physical devices remain unqualified. The complete production-clone goal remains active.

## September 12: native Seeking Air Mine assets and original Info portrait

[The Seeking Air Mine source foundation](../reference/seeking-mine/README.md) now preserves all eight numerical levels, four setup/projectile families, original reveal/upgrade/spent/shadow clips, every referenced particle and five unchanged sounds. The world graph retains **30 exports, 35 clips and 52 shapes**. The actual `evil_airtrap_lvl1_info` UI export supplies the Info portrait, including its three original embedded textures and nonpainting named bounds metadata. The importer reproduces **16 assets totaling 690,354 bytes from 15 fingerprint-verified inputs**: six lossless texture crops, four world previews, the original 360×420 Info picture and five Ogg files.

The UI source required two explicit importer additions. The SC6 reader now parses empty TextField bounds and embedded single-mip KTX 1 ASTC RGBA 4×4 textures. Unsupported visible text, formats, layers, mips and orientation metadata still fail closed. This pinned UI stream has an exact 108,270,988-byte decompressed size, slightly above the ordinary 100 MiB cap; only its verified import opts into that bounded size. **All 22 Python importer tests pass**, including channel order/alpha, non-block-aligned dimensions, malformed/truncated KTX data, the decompression limit and retained empty layout bounds. The existing Bomb Tower importer still reproduces all 27 assets and source graphs unchanged.

**1,125 model/asset tests pass across 89 files**, with a successful TypeScript/production build and scoped formatting. The new source tests verify all eight level rows, variant families, exact geometry/UV remapping, all cropped sampling regions, the original Info image and asset/sound membership. The one initial Info test failure was an incorrectly assumed separator in the renderer's existing mesh key; it now checks the actual source shape slot with its `:` leaf separator.

**67 independent source-pixel compositions pass in Chromium and WebKit at DPR 2**, covering every world export, multiple setup/flight/reveal frames, nine intentionally empty opening frames, three explicitly additive emitter cases and the original Info image. Maximum per-case mean maximum-channel error is **0.664/255 in Chromium and 0.853/255 in WebKit**; maximum individual channel error is **3/255**, with no pixels exceeding 16/255. Forced single-texture batching and WebGL context restoration change **zero bytes**. All six recorded GL checks per engine are zero. The complete WebKit comparison sheet, original Info picture and level-7 world preview were visually reviewed. No pixel threshold or runtime timing was relaxed.

General production checks pass in both engines without page/asset errors. `npm run test:seeking-mine:assets` separately fetches and decodes all 16 new assets, including all five original Ogg files and the Info picture, through an isolated shipping preview without development hooks. Chromium repeats all decodes and verifies exact metadata/byte counts offline. The new **339-file cache is `crown-clan-9f75e076a3f6`**. Frozen reports, source/runtime/build hashes, decoded sound durations and pixel evidence are indexed by `output/playtest/native-seeking-mine-foundation-verification.json`.

The asset audit exposed the original reveal's **24 fps** timing alongside `ActionFrame=7`; the current live simulation still assumes 7/30 seconds. Source projectile and reveal openings are intentionally empty, so their native handoff needs deliberate integration. Live mines continue to use the authored art and level-one gameplay; the first 51 campaign villages, save version 4 and replay version 32 remain unchanged. Native presentation, activation timing, full progression, effects/audio, home handling and replay integration are the next required work. Native executable equivalence, WebKit offline and physical-device qualification remain unproven; the production-clone goal remains active.

## September 12: Seeking Air Mine source progression and release timing

The live simulation now uses all eight retained Seeking Air Mine damage, cost and build-time rows. It interprets release as action frame 7 divided by the original clip's **24 fps**, replacing the former 30 fps assumption. The focused source projection also retains all four projectile exports, shadow, speed, start offset/height, scale, play-once flag and emitter for the upcoming presentation work. The source importer reproduces every asset and reference after this extension. Home availability remains one mine at TH7 and two at TH8, capped at level 1; all later levels survive save/load, practice and portable snapshots with one-hitpoint trap identity. The Info panel shows level 2's 1,800 damage and TH9 requirement, without offering an upgrade at TH8.

The native campaign now enables its first **54 unchanged layouts**. Cross and Bows has 343 entities and eight level-three mines; Forest Outing has 434 entities and nineteen mines; Skeleton Run has 336 entities and thirty-two mines. All **59 mines deal the original 2,100 single-target damage**. Magic Practice remains gated by Wizard Tower level 11 and the campaign Shrink Trap. No entity, level, position or existing guard was replaced or bypassed to enable these stages.

**1,148 model/source tests pass across 91 files**, including all 162 supported native army/layout combinations. Independent retained CSV inheritance checks every mine level's damage, costs, times and later TH requirements. Boundary tests verify no movement through exactly 7/24 seconds, 3.5-tile/s travel, one impact and no neighboring splash. A shared legal 200-space TH8 fixture uses level-five Balloons and level-three Dragons with a level-six Laboratory. All three new villages finish naturally, and portable playback reconstructs flight, final units/buildings/traps/results and backward seeking without changing the viewer's village. Combat version **33** expires incompatible playback while preserving v32 result records and save version 4.

**20 affected browser scenarios pass per engine at DPR 2** in Chromium and WebKit. These cover 320/390-pixel mine Info, the TH9 gate, placement, flight and replay, Sweeper regression, campaign navigation, Invaders and native skeletons. The new `npm run test:seeking-mine:production` additionally imports and exports all three complete recordings through the shipping UI in both engines, compares exact mine flight states and results after rewinding, preserves home resources/army and confirms the next campaign gate. Chromium repeats all three recordings offline. There are no page or asset errors. The production fixture resolves Cross and Bows at 60%/two stars, Forest Outing at 45%/zero stars and Skeleton Run at 50%/one star; these are fixture results, not a campaign balance certification.

TypeScript, the production build, general production checks in both engines and scoped formatting pass. The **339-file cache is `crown-clan-e27aade2184f`**. The 320-pixel WebKit Info panel and Skeleton Run production capture were visually inspected. Logs, source/runtime/build hashes and frozen reports are indexed by `output/playtest/native-seeking-mine-combat-verification.json`. Validation corrected stale campaign-count assertions and two production-driver navigation assumptions; checks were not weakened. The initial low-level air fixture did not reach a mine in Cross and Bows, so the fixture now researches troops within the actual TH8/Laboratory-6 limits while preserving the source layout.

Live mine artwork, effects and audio remain authored pending native presentation integration. The 7/24-second release is an interpretation of source fields, not proof of the native executable's action-frame numbering or projectile handoff. Exact motion, target-loss semantics, native particles, later campaign mechanics, WebKit offline and physical-device qualification remain unfinished. The production-clone goal remains active.

## September 12: original Seeking Air Mine presentation in the live game

All eight mine levels now use their four original animated families, original setup/construction/upgrade/reveal/projectile/shadow/spent clips, original Info portrait, particle textures and five unchanged sounds. Selection, moving previews and progress bars follow the source geometry. The ground reveal keeps its complete timeline independently of impact; projectile playback preserves the empty introduction and clamps its play-once ending. Reduced motion and completed battles clear flights, particles, flashes and shake. A zero-HP mine uses its original spent hatch without generic rubble underneath.

The source audit also recovered `gen_appear_fx`, the direct 30 fps clip referenced by Bomb Appear. Its original texture-18 sampling region raises the retained world graph to **31 exports, 36 clips and 53 shapes**. All **17 assets / 700,383 bytes** reconstruct exactly from **16 fingerprint-verified inputs**. The independent witness generator reproduces **72 cases, including eleven empty frames**. Chromium/WebKit maximum per-case mean maximum-channel errors are **0.6633/255 and 0.8521/255**; the maximum individual channel error is **3/255**, with zero pixels exceeding 16/255. Forced single-texture batching and context restoration change zero bytes, and all six GL checks per engine are zero. These verify source sampling and composition, not native executable motion.

The particle sampler is shared with Bomb Tower; extraction preserved 260 sampled Bomb Tower poses exactly. Mine trail history stores at most eight live birth points and direction vectors, reconstructing turns and impact through replay. Storing vectors keeps platform-specific `atan2` rounding in presentation rather than replay-derived state; production comparisons caught and resolved that difference without relaxing exact equality. Looping hatch smoke samples only its live lifetime window. Original volume/pitch and stable event keys drive activation, impact and home handling audio.

**1,162 model/source tests pass across 93 files**, including all 162 supported native army/layout combinations. **28 affected browser scenarios pass per engine at DPR 2** across the regression run and added state case. The final three mine scenarios were rerun after the direction-vector and zero-HP fixes. Coverage includes all four home families, original Info, moving previews, construction/upgrade/spent states, concealed emergence, original impact emitters, exact backward seeking, reduced motion and scene cleanup. Captures wait for Phaser's rendered frame; the WebKit construction/spent view, original Info and close-up flight/impact were visually inspected. Home state galleries deliberately isolate higher-level art and are not home-balance fixtures.

The shipping mine check imports, exports, rewinds and completes Cross and Bows, Forest Outing and Skeleton Run in both engines with exact flight states and results, preserved home resources/army and no page/asset errors. It verifies requests for all **14 assets actually used by the live mine scene**. Chromium repeats all three portable recordings offline. The separate asset-delivery check decodes all 17 retained assets and all five Ogg files in both engines, then repeats identical byte/dimension/duration checks offline in Chromium. General production checks also pass in both engines after updating their required artwork list from the three retired sprites to the original assets. The production build and scoped formatting pass; the **340-file cache is `crown-clan-9f6f3374d5b1`**. Frozen reports, runtime/build/source hashes and captures are indexed by `output/playtest/native-seeking-mine-live-verification.json`.

Combat version **33**, save version **4**, the TH8 mine level-one cap and the first **54 playable native villages** are unchanged. The [source audit](../reference/seeking-mine/README.md#live-presentation-and-fidelity-limits) records local registration, rise/handoff, effect pairing, projectile offsets, particle equations, spawn-limit and camera-strength interpretations. Native executable parity, later campaign mechanics, WebKit offline and physical-device qualification remain unproven. The production-clone goal remains active.

## September 12: original Wizard Tower foundation and screen composition

[The Wizard Tower source reference](../reference/wizard-tower/README.md) now preserves all seventeen levels, their original bodies, separate foundation/scaffolds/rubble, eleven rooftop Wizard families in three directions, four projectile tiers, all referenced particle artwork and seven sounds. Three source graphs contain **135 exports, 141 clips and 396 shapes**. Seven lossless texture crops, seventeen transparent previews and seven unchanged Ogg files total **31 assets / 3,198,493 bytes**. The importer reproduces every pixel, sound byte and reference from **23 fingerprint-verified inputs**. Sparse level inheritance, original animation-table headers, action frame 14, projectile speeds 500/900 and nontracking flags remain inspectable. Variant particle rows inherit their containing emitter's omitted SWF, retaining all debris variants.

The higher-tier projectiles require original screen-blended containers and animated group RGB transforms. The shared renderer now retains source blend 4 alongside normal/additive, composites children into an isolated buffer and applies RGB multiplication/addition after composition. A dedicated shader clamps straight RGB before premultiplication; source-over alpha and group opacity remain distinct. Synthetic overlapping translucent shapes independently verify screen/additive results, color saturation, group reuse, leaf screening and transparent groups. Their exact pixels survive forced single-texture batching and context loss; five create/clear cycles keep framebuffer counts stable and remove all added listeners. Context restoration also exposed stale handles in Phaser's pooled filter buffers. The recovery handler now invalidates all lost framebuffer handles while retaining wrappers for reconstruction. Unsupported blends and group alpha addition still fail closed.

**1,171 model/source tests pass across 94 files**, and **23 Python tests**, TypeScript, scoped formatting and the production build pass. **Nineteen affected browser scenarios pass per engine at DPR 2**, across the screen/Tesla/X-Bow run, three new Wizard Tower source comparisons and twelve Bomb Tower/mine/storage/Goblin regressions. These include original body/particle/assembly pixels, live effects and audio, context restoration, backward seeking, reduced motion, home construction/upgrades and spent trap states. No pixel threshold, source geometry or runtime timing was relaxed.

All **252 independent Wizard Tower source compositions** reproduce from original textures: 71 body/general-particle cases, 132 rooftop cases and 49 effect cases, including four empty frames. Both browsers change **zero bytes** after single-texture batching or context restoration, with all six GL checks zero for each graph. Maximum per-case mean maximum-channel error is **0.6633/255 in Chromium and 0.8521/255 in WebKit**. Body cases have maximum channel difference 2; effect cases 3 in Chromium and 4 in WebKit. Rooftop cases have nineteen pixels per engine above 16/255, maximum 61/255 and at most **0.1293%** of colored pixels in any case. The edge audit places every such pixel center on an original polygon boundary or within 0.000001 pixel of one, consistent with CPU/GPU boundary coverage and rounding. Original geometry, transforms and packed texture sampling regions match. The retained artwork was not shifted to avoid these boundary cases. All seventeen previews and the effect sheet were visually inspected; source clipping guards required wider preview framing for level 14.

General production checks pass in Chromium and WebKit without page/asset errors. Chromium passes offline reload, Army and replay with **371 cached files in `crown-clan-8114857797c5`**. The new `npm run test:wizard-tower:assets` separately fetches and decodes all 31 assets, including all seven original sounds, through the shipping build in both engines. Chromium repeats identical bytes/dimensions/sound durations offline. Frozen reports, source/runtime/build hashes, logs, screenshots and the boundary audit are indexed by `output/playtest/native-wizard-tower-foundation-verification.json`.

Live Wizard Tower artwork, full level progression, directional attack/launch registration, source projectile behavior, particles/audio and replay integration remain pending. The 360×380 source previews use local framing and an unverified rooftop anchor; they are not original Info exports. Combat version **33**, save version **4**, the home level-six Wizard Tower cap and the first **54 playable native villages** remain unchanged. Magic Practice remains gated by Wizard Tower level 11 and the campaign Shrink Trap. Native executable parity, WebKit offline and physical-device qualification remain unproven. The production-clone goal remains active.


## September 12: complete Wizard Tower combat and portable browser consistency

[Wizard Tower gameplay](AIR-WIZARD-PROGRESSION.md) now consumes all seventeen pinned source levels for health, damage, costs, destination timers and Town Hall requirements. All supported levels survive save/load and practice; new home construction and upgrades still enforce the TH8 level-six ceiling. The source projectile projection supplies four tiers: levels 1–4 travel at **5 tiles/s**, and levels 5–17 at **9 tiles/s**. Shots retain the target position captured at launch, replacing the previous 16-tile/s tracking behavior. The one-tile blast applies once to the chosen air/ground layer, including its boundary and excluding later-spawned units. A destroyed tower's launched shot still resolves. These interpretations do not establish native executable motion or action-frame handoff.

Graduation Ceremony, native stage 58, now has supported mechanics with all **324 original entities** and Wizard Tower levels **9, 7, 9, 7** at **1,600, 1,200, 1,600, 1,200 HP**. Its original stage-57 prerequisite remains enforced. The mechanically supported catalog contains its first 54 villages plus Graduation Ceremony; Magic Practice still requires the campaign Shrink Trap. The shared legal TH8 air fixture finishes Graduation naturally at **24% destruction and zero stars**, retaining the original layout and conserving source loot. This verifies defeat and replay, not a claim that this army should clear that village.

Production replay comparisons exposed last-bit differences in `Math.hypot` movement/projectile deadlines and `Math.atan2` Sweeper aim across JavaScript runtimes. Combat distances now use explicit squared terms and a square root within the bounded world grid. Sweeper aim/gusts store direction vectors; dot/cross products preserve the existing cone, width and acquisition tolerance, with angles derived only for rendering. Regression tests independently check all eight cone directions, both width edges and exclusion behind the front. Nine complete fixtures—five Wizard Tower levels and four original mine/Sweeper villages—compare SHA-256 hashes of the entire serialized battle at **every 0.05-second sample** against Node. All **7,876 states per browser** match exactly in Chromium and WebKit. No rounding or numerical comparison tolerance was introduced to pass replay checks. Other combat scenarios and native executable equivalence remain unqualified.

**1,210 model/source tests pass across 97 files**, including all 165 supported native army/layout combinations. The Wizard Tower importer reproduces all 31 assets and source projections, and TypeScript, the production build and scoped TypeScript/JavaScript formatting pass. All **36 retained art/sound/reference files** other than the intentionally extended combat projection match the foundation commit byte for byte. No native geometry, texture, renderer or particle equation changed; the foundation's 252 source-pixel cases remain prior evidence rather than new runs.

**Thirty-three affected browser scenarios pass per engine at DPR 2** across the final 25-case combat/progression/campaign/replay/Info run and eight Air Sweeper/mine cases. The new phone checks inspect level-four speed comparison, the TH9 gate at level six and level-seventeen save/reload. Seventeen level markers initially forced the description outside the 320-pixel Info card; a six-line CSS fix allows the markers to wrap while keeping text and markers within the card. Settled 320-pixel Info and shipping level-seventeen/Graduation flight captures were visually reviewed.

The shipping Wizard Tower check imports, exports, finishes and rewinds six recordings in both engines with exact projectile states, results and unchanged home army/resources. Chromium repeats all six offline. The existing three Seeking Air Mine recordings and general production checks also pass in both engines without page/asset errors. The final build retains **371 cached files in `crown-clan-74baf29671c3`**. Reports, per-step hashes, source/runtime/build hashes and logs are indexed by `output/playtest/native-wizard-tower-combat-verification.json`.

Combat version **34** expires earlier playback while keeping old results readable; save version **4** remains unchanged. Wizard Tower bodies, rooftop animation, original projectiles, particles and audio still await live presentation integration. Source height/offset conversion, action-frame timing, later campaign mechanics, WebKit offline and physical-device qualification remain unfinished. The production-clone goal remains active.


## September 12: original Wizard Tower presentation in the live game

All seventeen Wizard Tower levels now render their original bodies, separate foundations, construction and upgrade scaffolds, rubble and eleven animated rooftop Wizard families. Original projectile and particle graphs supply all four fireball tiers, cast charges, trails, impacts and collapse debris. The original seven sounds supply attack, hit, destruction and accepted pickup/drop gestures. The shared source renderer keeps original geometry, color transforms and isolated screen/additive groups. Source previews replace the authored image in Info and moving ghosts; selection follows the complete native body/actor bounds.

Bounded shot, hit and destruction histories derive from actual combat events and reconstruct through replay without consuming combat randomness. Original action frame 14 aligns with each recorded launch, followed by the remaining source attack frames. Visual inspection corrected right-facing source views that were initially mirrored the wrong way and fireball artwork whose leading axis is positive Y. The original flame now trails behind the shot. Fixed target registration preserves empty landings and shots from destroyed towers; airborne impacts sort above flying troops. Reduced motion uses idle rooftop actors and suppresses transient particles/projectiles. Pause, mute, cancellation, seeking, finish and home transitions clean up or rebuild native views and sample nodes.

**1,218 model/source tests pass across 99 files**, including all 165 supported native army/layout combinations. TypeScript, the production build and scoped formatting pass. All **37 source/reference/asset files** other than the updated source README match the preceding combat commit byte for byte. Native geometry, texture crops, previews, sound bytes, shared renderer and particle equations are unchanged; the foundation's 252 source compositions remain preceding evidence rather than new runs.

All **seventeen complete live assemblies** pass independent source-preview comparisons in both browsers at DPR 2, with maximum per-level mean maximum-channel errors **0.29583/255 in Chromium and 0.39409/255 in WebKit**. Levels 11 and 12 each contain two pixels above 16/255, with maxima 33/255 and 32/255 respectively by engine; the other fifteen levels stay at or below 16. Existing mean/fraction criteria and original geometry were not changed. All reported GL checks are zero. A live tier-four fireball/charge composition with seven isolated groups survives context restoration with **zero changed bytes** in both engines.

The browser coverage includes all seventeen home selection bounds, preview textures/origins, setup/construction/upgrade/rubble states, 320/390-pixel Info and persisted level seventeen, valid/blocked touch placement, original sample identity/gain/pitch, missed landings after tower destruction, trail expiry and replay reconstruction of meshes/groups. Existing Bomb Tower effects, Seeking Air Mine, defense progression and native campaign scenarios also pass. Full serialized combat state still matches Node at every 50 ms sample across nine complete fixtures: **7,876 states per engine**. The combined runs qualify **38 distinct browser scenarios per engine**. The first combined run passed 27/29 cases; the two new ghost-color assertions incorrectly passed CSS coordinates to an internal canvas-coordinate method. Using the existing `canvasPoint` conversion fixes the harness without changing the runtime. The seven effect/home cases were rerun after this correction and after fitting the phone capture to the actual CSS viewport. A separate earlier WebKit startup timeout occurred when a parallel runner closed the development server; the shared-server rerun passed without increasing timeouts.

The shipping Wizard Tower check loads all **31 original assets**, imports/exports/completes/rewinds all six recordings and preserves home army/resources in Chromium and WebKit. The committed level-ten version-34 fixture is the unchanged 7,290-byte export from combat commit `bf2f169`; its SHA-256 is `582c00d4091e3ababcdda5f87c67d9766ce03f915470178d5d73d1e046ab31e8`. It still produces the same replay content and result after native presentation integration. Chromium repeats all six recordings offline. Separate asset delivery decodes every image and all seven unchanged Ogg files in both engines and repeats identical byte/dimension/duration checks offline in Chromium. General production checks pass without page/asset errors. The **371-file offline cache is `crown-clan-8081b091be29`**. Frozen reports, runtime/build/source hashes, captures and logs are indexed by `output/playtest/native-wizard-tower-live-verification.json`.

The [source reference](../reference/wizard-tower/README.md#live-presentation-and-fidelity-limits) documents local scale/anchor, directional boundaries, frame-14 handoff, projectile axis/offset/height, timeline fitting, particle depth and audio layer interpretations. Source-frame inspection and browser reconstruction do not establish native executable projection, motion or sound mixing. Replay version **34**, save version **4**, the home level-six cap and native campaign gates remain unchanged. Native executable equivalence, later campaign mechanics, WebKit offline and physical-device qualification remain unfinished. The production-clone goal remains active.

## September 12: correct native Bomber facing

Original defender-frame inspection confirmed that the Bomber's three views face right. The live selector had mirrored them for right-side targets, so throws appeared to face away from their target. The selector now mirrors only left-side targets. Source geometry, texture pixels, animation timing, projectile trajectories and combat state are unchanged.

**78 focused model/source tests pass across six files**, including actual launches toward all six view/mirror sectors for all three source families. The new cases verify that a throw retains its recorded direction when the target moves or dies, then faces the next target during windup. **Ten affected browser cases pass per engine at DPR 2**, covering live source-frame facing, native projectile/fuse/rubble, original effects and audio, replay seeking, reduced motion, home handling and context restoration. The corrected WebKit flight and original defender sheet were visually inspected. TypeScript and the production build pass.

The shipped level-two and Invaders recordings import/export, rewind and reproduce their original results in Chromium and WebKit, with all 27 native assets loaded and no page/asset errors. Chromium repeats both recordings offline. The build retains **371 cached files in `crown-clan-f638ac2e3642`**. Evidence is frozen in `output/playtest/native-bomber-facing-verification.json`. This correction establishes source-facing orientation; exact native directional boundaries, world projection and launch handoff remain unverified. Replay version 34 and save version 4 are unchanged, and the production-clone goal remains active.

## September 12: original campaign Shrink Trap source foundation

[The Shrink Trap reference](../reference/shrink-trap/README.md) now retains both source trap identities, the actual single-player placements, spell/global fields, original flask animation, armed/unarmed compartment, direct reveal clip, aura, eight particle emitters and four original sounds. Its graph contains **23 exports, 25 clips and 35 shapes**. The importer reproduces **nine assets / 217,901 bytes from 14 fingerprint-verified inputs**: three lossless texture crops, two registered 226×340 previews and four unchanged Ogg files. Every source geometry/transform/timeline and cropped texture sampling region is retained.

The source review distinguishes legacy values from current behavior. The retained spell still contains `ShrinkHitpointsRatio=50`, but Supercell's [February 2022 release notes](https://supercell.com/en/games/clashofclans/blog/release-notes/quality-of-life-bonus-update-2/) explicitly remove HP reduction. The behavior-evidence record requires preserving current and maximum HP during future integration. The trap's three-tile damage-radius field, spell's four-tile radius, 20-second duration, 75 pulses, action frame 14 and seven-second global are recorded separately; their native handoff and recovery semantics are not guessed from the field names.

**1,224 model/source tests pass across 100 files**, followed by TypeScript, the production build and scoped formatting. The importer and independent witness generator both reproduce their outputs. **52 original-texture compositions pass in Chromium and WebKit at DPR 2**, including every trigger frame, long-aura samples and six explicit additive emitter variants. Maximum per-case mean maximum-channel errors are **0.66327/255 and 0.85205/255**, with maximum individual error **7/255** and no pixels above 16/255. Forced single-texture batching and context restoration change **zero bytes**; all six GL checks per engine are zero. The original armed preview and complete WebKit source-comparison sheet were visually inspected.

Five witness cases are intentionally empty: trigger frame 13, both reveal endpoints, and the original empty circle export with both source and emitter blend settings. An initial unit assertion incorrectly required all trigger frames to draw; it now explicitly requires the terminal frame to stay empty, matching the independent original-source witness. A separate test had miscounted the six effect records as seven. The full run also exposed a host-load-dependent replay test that expected its step-count quota to finish within a fixed number of updates while the independent eight-millisecond wall-clock guard could stop updates early. Commit `ce148ae` freezes the clock only for the step-count test; the separate advancing-clock test still verifies actual yielding. No runtime limit or source-pixel criterion was weakened.

The new `npm run test:shrink-trap:assets` fetches and decodes every image and all four Ogg files through an isolated shipping preview in both engines, then repeats identical byte/dimension/duration checks offline in Chromium. General production checks pass in both engines without page/asset errors, including Chromium offline reload/Army/replay. The **380-file cache is `crown-clan-143493246946`**. Runtime source remains byte-for-byte identical to the preceding Bomber-facing fix; this foundation adds source records, static assets and verification. Frozen reports, source/runtime/build hashes and captures are indexed by `output/playtest/native-shrink-trap-foundation-verification.json`.

Magic Practice retains all eight original campaign Shrink Traps and remains gated. Live activation, area/clock semantics, status application/recovery, original particles/sound, reduced motion and replay integration remain required. Replay version **34**, save version **4**, existing home availability and campaign gates are unchanged. Native executable equivalence, WebKit offline and physical-device qualification remain unfinished; the production-clone goal remains active.

## Native Shrink Trap live campaign — September 12, 2026

Magic Practice (native stage 55) now preserves and activates all eight source `12000017` Shrink Traps. The supported catalog is the first **55 villages plus Graduation Ceremony**; original prerequisites remain in force. High Pressure still requires Air Sweeper 7, Mortar 11 and Cannon 15. No Flight Zone still requires its garrison, Clan Castle and Air Sweeper 5.

The trap's two-tile trigger, four-tile spell area, 75 pulses, refresh/recovery and health preservation are integrated with original graphics and audio. [SHRINK-TRAP.md](SHRINK-TRAP.md) records the exact local handoff, post-movement membership sampling, wide-step limitation, radius choice, hero/Rage combination and visual half-scale. These are tested engine interpretations, not a claim of native executable parity.

Validation for this milestone:

- **1,257 tests across 105 files pass**, including partial expiry, both attacker layers, Healers, heroes, Rage, overlapping traps, spring suspension, unchanged HP/healing/damage magnitudes, source references, cooldowns, portable replay, treasury persistence and home-save rejection.
- **59 independent GPU cases per engine** pass in Chromium and WebKit at DPR 2. Eight phases exercise the range's **685-frame child inside a one-frame outer clip**. Five expected-empty samples remain empty. Maximum per-case mean error is **0.6633 Chromium / 0.8521 WebKit**; maximum channel difference is **7**, with no pixels beyond the established large-error threshold. Single-texture batching and context restoration change zero pixels; all six GL checks per engine return zero.
- **Three live viewport cases per engine** pass at 1440×960, 390×844 and 844×390. Checks cover concealed/passable placement, original bottle/base, active status size, source aura, paused rendering, reduced motion, recovery and cleanup. The separate portable-replay/audio case also passes in both engines: eight backward-seek phases reproduce state/geometry/size; the original sample uses gain 0.108 and playback rate 2 at 2× replay speed, then stops on pause.
- The intact 200-space TH8 fixture completes naturally at **62.9 seconds, 33% destruction, zero stars**. **1,259 complete battle-state hashes per engine** match Node at every boundary. Together with the nine previous defense fixtures, all **9,135 complete states per engine** still match. This covers defeat and reconstruction, not a native-client balance comparison or proof the fixture can clear the village.
- The shipping UI imports, seeks, finishes, exports and reimports Magic Practice recordings in both browsers. Original pulse/status values and final results match Node, and Chromium repeats the workflow offline. All nine source assets decode in both browsers; all four original Ogg files decode, with no page or network errors.
- The older Wizard Tower production suite passes its six fixtures in both browsers and offline in Chromium. The immutable historical version-34 replay retains SHA-256 `582c00d4091e3ababcdda5f87c67d9766ce03f915470178d5d73d1e046ab31e8` and identical replay contents. General production checks pass with the **380-file** offline cache `crown-clan-d98263c885c6`.
- Source import reproduces all 14 pinned inputs, unchanged nine asset payloads and the compact combat projection. The independent GPU witness reproduces all 59 cases. Build/type checking, scoped formatting and `git diff --check` pass.

The nested particle timing correction is separately committed as `5bad60a`. Existing particle callers retain their original duration arithmetic; the override is explicit for the Shrink Trap range. Review covered the original source previews and live desktop/phone screenshots. Original source geometry, transforms, tint and variant blends are preserved; local world registration and particle projection are documented.

Two initial replay assertions were corrected: byte-order comparison of object properties was replaced by structural state comparison, and home isolation is checked before asynchronous audio decoding permits ordinary wall-clock resource production. A repeat-treasury test now supplies an army before starting a second raid. These were fixture/assertion errors; combat values were not relaxed. The initial explicit placement expectation was corrected against the pinned campaign coordinates.

Evidence is retained in `output/playtest/native-shrink-trap-live-verification.json`, the `shrink-trap-live-*` and `shrink-trap-replay-*` logs, both source-GPU reports, both 1,259-state determinism reports, the native Shrink Trap production report and exported production replays. Earlier frozen milestone manifests remain intact. Replay version **34** and home save version **4** remain unchanged. Native executable/physical-device qualification, WebKit offline qualification, later campaign mechanics and the broader production-clone goal remain open.

## Original Air Sweeper source foundation — September 12, 2026

The [Air Sweeper source reference](../reference/air-sweeper/README.md) retains all seven source levels, complete 360-frame nozzle/sector controls, the 325-frame labeled loading clip, seven upgrade bodies, construction/base/shadow and both rubble families. Its original projectile, four effect records, four emitters with 25 particle variants, and four original sounds are included. **16 fingerprint-verified input pins** reproduce **45 exports, 54 clips, 166 shapes and 66 assets / 3,072,913 bytes**. The six texture crops retain exact source texels and sampling neighbors. All 56 transparent previews share 284×330 bounds and source registration.

**1,262 tests pass across 106 files**, followed by TypeScript, the production build and scoped formatting. The importer and independent witness generator reproduce their outputs. **128 original-texture pixel witnesses per browser** pass in Chromium and WebKit at DPR 2, covering every retained export, all 56 level/direction combinations, upgrade/construction/rubble states, independent and disabled child controls, all loading label boundaries and attack frames, and every particle variant. Maximum per-case mean errors are **0.66327/255 Chromium and 0.85205/255 WebKit**; maximum individual channel error is **6/255**, with no pixels above 16/255. Forced single-texture batching and context restoration change **zero bytes**. All six GL checks per engine return zero. Each sheet uses 479 meshes, zero blend groups and one cached source-color variant for `smoke01`'s 77/255 RGB multiplier. The original level-seven preview and complete WebKit witness sheet were visually reviewed.

The projectile's `dummy_particle` is a tiny white polygon, not an empty export or a complete traveling wave. At 2× its entire visible footprint lay on one pixel-center boundary, exposing a CPU/GPU edge-inclusion difference. Its diagnostic now magnifies the unchanged source geometry to 32×; all 256 painted pixels match exactly in both engines. Other witnesses remain at most 2×. No source pixels, geometry or comparison thresholds were changed. The transparent-border test now computes the maximum alpha over the same complete border before asserting, avoiding over a million individual matcher calls and a host-load timeout.

`npm run test:air-sweeper:assets` fetches and decodes all **66 assets and four original Ogg files** through an isolated production preview in both engines. Chromium repeats identical byte/dimension/duration checks offline. No page or asset errors occur. General production checks also pass in both engines, including Chromium offline reload, Army and replay. The new offline cache is **`crown-clan-3135de77181a`, 446 files**. Browser diagnostics use a separate port because another local project occupies the default loopback development port; the production drivers already own ephemeral preview ports.

Runtime source remains **126 files**, byte-for-byte identical to the Shrink Trap live milestone, SHA-256 **`3165e547576369982918983cf950b7aab833855ab8a69904343d5a75643dc35a`**. This commit packages source records/assets and verification. The live Air Sweeper still uses 32 authored sprites and its four-level table. Native source labels span 224 idle, 91 loading and 10 attack frames at 30 fps; their native relationship to 600-ms preparation, 5,000-ms attacks and `CoolDownOverride=4800` is not established. Original procedural wind-wave rendering, live registration/rotation/loading, effects/audio, reduced motion and replay integration remain required.

Frozen evidence is indexed by `output/playtest/native-air-sweeper-foundation-verification.json`; logs use the `air-sweeper-*` prefix. Earlier frozen manifests are preserved. High Pressure remains gated on Air Sweeper 7, Mortar 11 and Cannon 15; No Flight Zone still requires Air Sweeper 5, its garrison and Clan Castle. Home TH8 availability, replay version **34**, save version **4** and the supported campaign catalog remain unchanged. Native executable and physical-device equivalence, WebKit offline qualification and the broader production goal remain open.

## Live original Air Sweeper — September 12, 2026

All seven source levels now use original world bodies, independently controlled nozzle/sector parts, labeled loading/fire playback, construction/upgrade scaffolds, rubble, source particles and four original sounds. The 56 source previews share the live registration for shop/Info/moving ghosts; selection and bars use geometry bounds. The former authored Sweeper sprites and synthesized wind noise are no longer used by the scene. [AIR-CONTROL.md](AIR-CONTROL.md#live-original-air-sweeper-presentation) records the local 1.2 scale, `(0,50)` anchor, loading-clock fit, source-particle projection, first rubble variant and remaining authored traveling gust curves.

The seven original HP/push/cost/time rows are supported in retained saves and practice. The TH8 upgrade ceiling remains level four. Source TH9/10/11 gates remain visible for levels 5/6/7. High Pressure now needs **Mortar 11 and Cannon 15**; No Flight Zone still needs its **garrison and Clan Castle**. Neither incomplete village is silently unlocked. Replay version **34**, save version **4** and the first-55-plus-Graduation campaign catalog remain intact.

Validation:

- **1,277 tests across 108 files pass**, including all seven push strengths, full facing controls, source label boundaries, construction/upgrade/rubble, bounded history after target loss/stun/destruction, home ceilings, exact source costs/HP, portable replay and historical recordings. TypeScript, production build and scoped formatting pass.
- **128 independent source GPU compositions per engine** pass again in Chromium and WebKit at DPR 2. Maximum per-case mean errors remain **0.66327/255 and 0.85205/255**, maximum channel error **6/255**, and no pixels exceed 16/255. Single-texture batching and context restoration change zero bytes; all six GL checks per engine are zero.
- **56 actual live assemblies per engine** match their independent original-source portraits, including registration of the separate base and all eight directions. Maximum per-case mean errors are **0.28131/255 Chromium and 0.32294/255 WebKit**; maximum channel error is **6/255**, with no pixels beyond 16/255. Every level also passes live setup/construction/upgrade/rubble, picking and preview-bound checks.
- **Three combat/replay viewport cases per engine**, at 1440×960, 390×844 and 844×390, reconstruct nine forward/backward seek samples of actual loading, firing, gust, source particles and view geometry. The natural level-seven battle destroys the defense, clears the village and produces three stars. Original firing gain is **0.108**, and pitch at 2× replay lies in **1.9–2.1**. Pause, resume, mute, reduced motion, source destruction, finished playback and return-home cleanup pass without retained sound/effect instances or renderer-listener growth.
- Desktop/phone handling tests verify one original pickup/place sound, blocked drops, source-colored valid previews, original grass, cancellation and battle transitions. The eight prior air-control UI/flight tests pass in both engines. Info checks at **390 and 320 pixels** show original level-five stats, seven total levels and the TH9 gate without an upgrade button.
- The five new Air Sweeper fixtures produce **3,534 full battle-state hashes per engine**, each identical to Node. With the ten existing defense/campaign fixtures, all **12,669 complete states per engine** match. This verifies the full serialized simulation, including the added bounded presentation history, rather than only results or positions.
- Two immutable version-34 recordings were produced with the actual previous source, commit **`8e786b6d3fb270062f30597b3b3717283b6c2e42`**. Level-one and level-four recordings retain all **682 and 689 physical states**, respectively, after excluding only the newly added `airSweepers` presentation history. Every remaining field is canonically compared at each boundary, including results. Fixture SHA-256 values are `d885c0d6de2f118d6f900d44063f1a445947ad7261e3faf8c0313d4116ed1bc9` and `e566cfd5678a6fdef88d696449ba9ec21eb6231c726b80c25e2ac28a84490413`.
- `npm run test:air-sweeper:production` imports a retained level-seven village and portable recording through the shipping UI, seeks, completes, exports, rewinds and verifies home isolation in both engines; Chromium repeats offline. All **66 source assets / four Ogg samples** decode without page/network errors. General production checks also pass in both engines, including Chromium offline reload/Army/replay. The current **446-file** cache is **`crown-clan-88c520059dcd`**.
- Source import and independent pixel witnesses reproduce exactly. All 66 source asset payloads remain unchanged. The runtime now contains **129 files**, SHA-256 **`7c20e527d95dd11f1653fa059cf0ae8abe0757ae72ffcfa17a290e991b60a37a`**. Visual review covered the complete source/live galleries and phone firing/destruction views.

Handling tests caught and removed the generic placement tone layered over the original sound. A copied diagnostic then referenced the obsolete Wizard Tower data key; correcting the fixture allowed the original grass assertions to run. The previous authored-sprite occupancy threshold was replaced with exact source-preview pixel hashes because the original common bounds have different transparent margins. The general release check now requires all 66 live source assets instead of requiring requests for the retired authored sprites. Pixel error thresholds and combat expectations were not relaxed.

Frozen evidence is indexed by `output/playtest/native-air-sweeper-live-verification.json`. Earlier frozen manifests remain intact. Native executable animation-clock, cone/procedural-wave, particle-motion and mixing correspondence remain unverified; the current rendering is source-backed with documented local choices. Physical-device/WebKit-offline qualification, later campaign defenses/garrisons and the broader production-clone goal remain open.

## Original Mortar source foundation — September 12, 2026

> Qualification correction: the September 13 framebuffer-bound verification below supersedes the original large body-sheet browser capture. The original source witnesses and frozen records remain preserved.

The [Mortar source reference](../reference/mortar/README.md) retains **eighteen original levels**, their rotating barrels, separate optional gear-up controls, foundations, construction/scaffold and all six rubble variants. It also retains **thirteen shell families, seven effect records, 24 emitters, 64 ordered particle variants and five original sounds**. Sixteen fingerprint-verified source inputs reproduce **78 exports, 110 clips, 346 shapes and 28 assets / 3,551,318 bytes**. The five lossless source texture crops preserve every referenced sampling region and neighbor; all eighteen normal-mode portraits share registered **346×336** bounds.

Source inspection found eight discrete barrel views across the first fourteen turrets' 360-frame controls. Levels 15–18 retain 361 frames, including their actual final source frame. The separate gear-up child is preserved and explicitly disabled in normal portraits. Repeated hit-effect rows and the yellow trail's alternating additive/non-additive variants remain intact. The source level-one through level-ten HP, DPS, costs and durations agree with the current runtime table.

**1,282 tests pass across 109 files**. TypeScript, production build and scoped formatting pass. The importer and independent Python pixel witnesses reproduce their outputs. **173 body and 140 effect compositions per engine** pass in Chromium and WebKit at DPR 2, covering all 78 original exports, all eighteen levels in eight views, enabled/disabled controls, late terminal frames, rubble, projectile samples and all 64 emitter variants. The original level-eleven portrait and both complete WebKit comparison galleries were visually reviewed.

Maximum per-case mean maximum-channel errors for bodies are **0.37883/255 Chromium and 0.56567/255 WebKit**; effect maxima are **0.66327/255 and 0.85205/255**. Four body cases, levels 12/13/17, have **30 pixels per engine above 16/255**, with maximum errors **89/255 and 90/255**. Their outliers follow a single column or row at original polygon boundaries, mathematically on or within **0.000003815 pixels** of sampled pixel centers. The edge audit records the original transformed coordinates. These differences remain in the unmasked comparisons; source geometry, sample roots and acceptance thresholds were not changed. Effect cases have maximum errors **2/255 and 3/255**, with no pixels above 16/255. Two `d6` smoke endpoint samples intentionally paint nothing.

Each body sheet uses **850 meshes and no blend groups**. Each effect sheet uses **160 meshes, six isolated additive groups and nine cached source-color variants**. Forced single-texture batching and WebGL context restoration change **zero bytes** in all four sheets. All six GL checks per sheet return zero.

`npm run test:mortar:assets` fetches and decodes all **28 assets and five original Ogg files** through an isolated shipping preview in both browsers. Chromium repeats identical byte/dimension/duration checks offline. The general release and production-browser checks pass in both engines without page/asset errors, including Chromium offline reload, Army and replay. The new **474-file** cache is **`crown-clan-2b862ab90f23`**.

Runtime source remains **129 files**, byte-for-byte identical to Air Sweeper live commit `dd2a74e`, SHA-256 **`7c20e527d95dd11f1653fa059cf0ae8abe0757ae72ffcfa17a290e991b60a37a`**. Replay version **34**, save version **4**, the home TH8 cap and supported campaign catalog remain unchanged. Frozen evidence is indexed by `output/playtest/native-mortar-foundation-verification.json`; raw reports and captures are preserved under `output/playtest/mortar-foundation-evidence/`.

Live original Mortar rendering, later source levels, shell motion/damage handoff, source particles/audio and reduced-motion/replay integration remain required. High Pressure still needs Mortar 11 and Cannon 15; No Flight Zone still needs its garrison and Clan Castle. Native executable/physical-device equivalence, WebKit offline and the broader production-clone goal remain open.

## September 12 — original Mortar live integration

All eighteen original normal Mortar levels now render and simulate with their source base, turret, construction, scaffold, rubble, thirteen shell families, shadows, particles and five unchanged Ogg sounds. The shared source table supplies HP, DPS, prices, times and Town Hall requirements; home Town Hall eight remains capped at level six and four Mortars. High Pressure remains gated by Cannon level 15. Gear-up mode remains unavailable.

Combat version **35** uses the original projectile speed of five tiles per second, with a fixed landing site and inclusive 1.5-tile ground splash. Version **34** remains playable and exportable with its previous 1.15-second flight. Three recordings generated by the actual archived `0f9449e` executable retain every one of **3,143** prior physical battle-state hashes. The older Air Sweeper fixtures retain another **1,371** states. The new timing changes the fixed Magic Practice fixture from 33% to 42% destruction; all 75 shrink pulses and complete replay/seek reconstruction remain checked.

**1,300 tests pass in 111 files**, and typechecking/build, scoped formatting and the original-source importer pass. **Thirty-nine distinct relevant browser scenarios pass per engine**, covering 22 complete Node/browser simulation comparisons, all eighteen live normal assemblies, all eighteen construction/upgrade/ruin states and selection/ghost registration, phone paid upgrades, all eighteen airborne projectile levels after launcher destruction, paused flight/impact, reduced motion, backward seeks, desktop/touch placement audio, three effect/audio viewport scenarios and graphics-context recovery. Initial checks found and fixed a shell remaining visible at a resolved floating-point impact boundary. Snapshot comparison now sorts display-list samples so a normal renderer depth sort cannot masquerade as motion.

The independent live-portrait comparison has maximum per-level mean channel errors **0.325111 Chromium / 0.351541 WebKit**, no pixels above 16/255, and zero GL errors. Both engines restore a frozen level-eighteen shell, trail and smoke with **zero changed bytes out of 22,118,400 canvas bytes**, no GL errors and unchanged listener counts. Foundation source-witness edge-coverage differences remain documented separately and were not hidden or regenerated.

The shipping Mortar driver passes original-asset decode, new level-eighteen replay import/seek/finish/export, three historical version-34 Mortar imports and exports, and unchanged home resources/army in Chromium and WebKit. Chromium also reloads all 28 assets and replays the new fixture offline. The Wizard Tower production driver separately exercises current version-35 recordings and its unchanged archived version-34 file. General production shop, research, collection, tab handoff, replay and offline checks pass with **474 cached files**, cache `crown-clan-eebf2cc54adf`, and no reported browser errors.

The live body uses local scale 1.2 and anchor (0,80). Source speed/height/offset, artwork, effect rows and sound parameters are retained, but nearest-sector facing, the 115-pixel sinusoidal visual arc, particle motion, screen shake projection, knockback and final native mixing are not verified against the original executable. No authored barrel recoil was present in the retained source. Remaining broader work includes Cannon level 15, garrison/Clan Castle behavior, further native art replacement, online systems, physical-device qualification and the full production-clone goal. The phone Info card also still wraps the eighteen level indicators; a small layout follow-up is recorded separately from this combat milestone.

Raw logs, screenshots and reports use `output/playtest/mortar-live-*`, `mortar-*` and `native-mortar-live-*`. The immutable live verification manifest snapshots that evidence under `output/playtest/mortar-live-evidence` after commit. Earlier foundation manifests remain unchanged.

## September 12 — phone building-level indicator layout

Building Info indicators now use one row of equal grid tracks that shrink to the available width. Mortar's eighteen markers and Wizard Tower's seventeen stay inside their cards at 320px and 390px; the Mortar also fits 844px phone landscape. The existing gold state, marker height, gaps and desktop maximum width remain intact. A first flex-shrink attempt exposed WebKit overflow, so the final layout uses bounded grid columns.

Five relevant browser scenarios pass per engine, including the actual paid Mortar upgrade, unchanged building registration, matching source portrait, complete contained marker row, Wizard Tower TH9 gate and persisted level-seventeen reload. Separate phone screenshots were visually checked after the upgrade toast finished its display interval. TypeScript and production build pass with 474 cached files, cache `crown-clan-b2a7619755b9`. No combat rules or recording versions changed. Evidence: `output/playtest/info-marker-{chromium,webkit,build}.log`, `info-marker-capture-webkit.log` and `mortar-info-markers-{320,390,844}-{chromium,webkit}.png`. This resolves the marker-wrap follow-up from the Mortar milestone.

## Original Cannon source foundation — September 13, 2026

> Qualification correction: the September 13 framebuffer-bound verification below supersedes the original large direction-sheet browser captures. The original source witnesses and frozen records remain preserved.

The pinned public client **18.400.21** now supplies all **21 normal Cannon bodies and 15 alternate bodies**, exact direction and animation timelines, named turret/gear controls, bases, construction/scaffolds/rubble, eleven projectile families, seven effect records and five original Ogg sounds. Seventeen inputs pass SHA-256 and independent bundle-fingerprint SHA-1 checks. The retained graph has **101 exports, 153 clips and 1,305 shapes**. Six lossless texture packs, twenty-one 410×346 source portraits and five unchanged sounds total **32 assets / 12,251,854 bytes**. See [the source reference](../reference/cannon/README.md).

This commit is a source foundation. Live runtime files remain identical to `fec2015`; the twelve-level Cannon progression, TH8 level-10 ceiling, home count limits, simulation and compatible replay versions **34/35** remain unchanged. High Pressure is still gated by Cannon 15. The original building table lists TH2 for Cannon level 2, while current home rules permit it at TH1; this discrepancy remains explicitly recorded for investigation during live integration.

Every normal/alternate root preserves 360 frames at 30 fps. Levels 14–15 retain their 81 parent placement patterns and nested animation. Normal turret timelines preserve irregular nine/eleven-frame bands, the 41 patterns and single-frame transitions at level 12, 35 patterns at levels 17–20, and all 365 frames at level 21. Source geometry is not reduced to assumed uniform direction bins. The independent Python witnesses use original full SCTX textures, while browser rendering uses packed textures and the production mesh renderer.

**1,529 compositions per engine** pass in Chromium and WebKit: 1,332 direction compositions, 90 control/animation/construction/base/rubble compositions and 107 effect compositions. Coverage includes all 101 exports and all 47 ordered emitter variants. All sixteen browser scenarios pass, with zero changed bytes under forced single-texture batching and graphics-context restoration, and all GL checks zero. Maximum per-case mean channel error is **0.663266** in Chromium and **0.852041** in WebKit. Eight direction cases contain 45/46 pixels above 16/255 respectively, each within 0.000003815 pixels of an original triangle edge. One animated level-15 detail contributes six such pixels per engine, within 0.000415199 pixels of an original triangle edge. Effects have no outliers above 16/255. These measured coverage differences remain in the reports; neither source geometry nor acceptance thresholds were changed.

All **1,305 tests in 112 files** pass in the final full regression run, along with build/typechecking, scoped formatting, source import reproduction and complete independent witness reproduction. The initial full run passed 1,304 tests and timed out one existing Air Sweeper historical-replay test while CPU reconstruction and both browser suites ran concurrently. The complete rerun passed after those jobs finished, with no test changes. Original sounds and all packed assets also pass SHA-256 delivery/decode checks in both engines and after a Chromium offline reload. General production checks pass both engines; Chromium reload, Army and replay work offline with **506 cached files**, cache `crown-clan-2515340af819`.

The level-1/15/21 portraits and WebKit detail, effect and later-level directional pages were visually reviewed. Frozen evidence is indexed by `output/playtest/native-cannon-foundation-verification.json`; raw snapshots are retained under `cannon-foundation-evidence`. Earlier frozen manifests remain untouched. Live original Cannon presentation, source-speed tracking, bounded effect/audio history, old/new replay compatibility and the Cannon 15 campaign unlock remain the next integration work. Geared-up simulation, original executable projection/timing, physical-device qualification, WebKit offline qualification, garrison/Clan Castle behavior and the broader production-clone goal remain open.


## Original Cannon live integration — September 13, 2026

All twenty-one original normal Cannon levels now use retained source meshes, named turret controls, independent level-14/15 root animation, original bases, construction/scaffolds/rubble, eleven projectile families, seven effect records and five Ogg sounds. World meshes preserve additive groups; phone Info/Shop/selection portraits use exact cropped source pixels. Fifty-three packaged assets total **13,244,811 bytes**. All home counts remain unchanged; TH8 ends at Cannon 10. The pinned source requirement of TH2 for Cannon 2 replaces the former wiki-derived TH1 exception, with paid legacy deadlines preserved.

Combat version **36** adopts source-speed 12-tile/second tracking projectiles. Actual movement bounds projectile travel, damage lands once, target loss keeps the last destination and launcher destruction does not remove a flying shot. Histories retain sixteen shots/hits and source-lifetime-bounded trail births. Source smoke keeps negative speeds and follows firing direction through opt-in particle settings. Old versions **34/35** retain their prior normal Cannon speed/deadline/physical fields; version 34 also retains fixed Mortar flight. Four Cannon recordings captured from archived pre-integration code reproduce **3,375 exact physical-state hashes**. The existing Mortar and Air Sweeper historical witnesses also pass in the complete unit suite.

High Pressure (native index 55) is now reachable after its actual prerequisite and keeps all four original Cannon 15 placements, each with 1,500 HP and 105 DPS. An untouched-layout legal 200-space TH8 ground army produces a natural defeat after 557 steps; all four Cannons fire. No result or star award is forced. No Flight Zone (index 56) remains gated by garrison defenders and Clan Castle behavior. Supported native layouts are 0–55 and 57; the latter still lies beyond the unsupported prerequisite path. The tutorial Cannon retains its earlier decoded image pixels, 94-pixel width, separate weapon and generic effect path; normal level-one art uses a distinct texture name.

Validation:

- **1,338 tests / 115 files pass**, plus TypeScript, scoped formatting, importer reproduction, independent live-witness reproduction and production build. The earlier full run's stale version/gate assertions and one historical-test timeout under browser load are retained in the logs; the complete final run passes without weakening historical checks.
- **29 distinct live browser cases per engine** cover all twenty-one states/previews/picking bounds, original-image tutorial regression, 320/390-pixel portrait and 844-pixel landscape Info/paid upgrades, moving/blocked placement, five decoded sounds, effects, pause/mute/reduced motion, rewind, destruction, battle/home cleanup, source assembly and High Pressure UI prerequisites. Follow-up targeted runs resolve the tutorial texture collision and inspect its decoded original pixels.
- **8,470 full combat-state samples per engine** match Node exactly across nine Cannon levels and High Pressure, for **16,940 cross-engine comparisons**. Cannon presentation-history distance arithmetic uses the existing explicit deterministic distance helper; the first WebKit run exposed a `Math.hypot` rounding difference that is fixed.
- **21 live source compositions per engine** compare packed runtime meshes to independently sampled original full SCTX textures on the same background. Maximum per-level mean channel error: **0.350461 Chromium / 0.371193 WebKit**. Levels 7/8 each have one pixel above 16/255 per engine, with maximum difference 17; no thresholds or source geometry were changed. Transparent portrait comparisons were replaced by same-background compositions to account correctly for additive glows on levels 12–15. The previous frozen **1,529 source compositions per engine** remain independent foundation evidence.
- Frozen live shots/trails/smoke survive graphics-context restoration with **zero changed bytes**, unchanged listener counts and zero GL errors in both engines. Phone Info, firing, construction and High Pressure screenshots were visually reviewed.
- The final production build ships **527 cached files**, cache **`crown-clan-9aeaa4ac5795`**. Both browsers import, seek, export and reopen current Cannon and High Pressure replays plus all four historical Cannon files. Chromium also replays offline after reload. All **53 assets** decode and match their filesystem SHA-256 in both engines; Chromium returns identical asset hashes, dimensions and decoded audio metadata offline. The general production checks pass both engines, including offline Army/replay coverage in Chromium.

Live evidence is frozen separately in `output/playtest/native-cannon-live-verification.json` with raw snapshots under `cannon-live-evidence`; prior frozen milestones and immutable historical fixture bytes remain untouched. Local angle mapping, altitude/world registration and particle equations remain explicitly unverified against the native executable. Geared-up mode, garrison/Clan Castle, further original art and content, online systems, physical-device/WebKit-offline qualification and the full production-clone goal remain open.

## Source pixel comparisons bounded by the framebuffer — September 13, 2026

The garrison death-frame check revealed a validation defect: the old source-sheet harness requested readback regions larger than the actual framebuffer. `displaySize` caps the backbuffer at 16 million pixels and 8192 pixels per dimension. At the former viewport sizes, the 9000-pixel Cannon sheets and the 6600-pixel Mortar body sheet were partly outside that buffer. Matching unrendered regions could therefore look like passing comparisons. Their original large-sheet browser results do not establish complete coverage and are explicitly superseded here. Source reconstruction, immutable Python witnesses, small live-assembly comparisons and historical physical replay hashes are unaffected.

The corrected harness renders row-aligned strips of at most **2400×6000 pixels**, keeps the original witness textures intact, translates their visible source region, and checks both dimensions against the measured framebuffer before any readback. Reports retain source page/offset, measured buffer size and compared size. No source geometry, source PNG, runtime file or acceptance threshold changed.

Both Chromium and WebKit pass all **1,529 Cannon compositions in thirteen strips** and all **313 Mortar compositions in three strips**. Every original case is included exactly once. Forced single-texture batching and graphics-context restoration produce zero changed bytes, and all GL checks are zero. Cannon maximum per-case mean error remains **0.663266 / 0.852041**; corrected captures contain **54 / 55** pixels above 16/255, all within 0.000415199 pixels of an original triangle edge. Mortar retains **30 / 30** such pixels, within 0.000003815 pixels of an original triangle edge. The limits remain mean error <1 and outlier fraction <0.003.

Evidence includes `garrison-and-cannon-mesh-{chromium,webkit}-final.log`, `garrison-mortar-and-troops-chromium-final.log`, `garrison-mortar-mesh-webkit-final.log`, the new `native-cannon-*-part-*` and `native-mortar-body-part-*` reports, and `native-foundation-strip-edge-audit.json`. These records are included in the later garrison foundation freeze; earlier frozen manifests and snapshots are not rewritten.

## Campaign garrison and original Castle/troop foundation — September 13, 2026

The pinned **18.400.21** source now supplies all ten campaign alliance rosters, their exact Castle placements, fourteen Clan Castle tiers, Goblin Castle, nineteen complete character/defensive-substitution families and the Dragon 7 / Balloon 8 animation blocks. No Flight Zone keeps one Dragon 7 and three Balloons 8: 35 spaces in a Castle whose ordinary capacity is 30. Stages without a Castle are retained without a fabricated building. Source troop levels remain distinct from visual levels; shifted later families are not silently indexed or clamped. See [the garrison source reference](../reference/garrison/README.md).

Twenty-seven original exports retain **51 clips, 89 shapes**, six lossless texture packs, empty clan-name/badge/attack-pivot metadata and additive groups. Nineteen portraits and nineteen exact cropped icons bring the total to **44 assets / 7,177,180 bytes**. Source inspection identified rising Z sleep markers in the 94-frame Castle roots; guarding presentation must not blindly run those timelines. Dragon retains three views and nested wing/glow clips. Balloon retains idle glow, all 34 attack frames and all 11 death frames, including its empty final frame. Source action-frame conventions and native clock selection remain separate from mesh preservation.

Validation:

- **1,348 unit tests in 117 files pass**, including all existing historical replay checks and ten new source/asset cases. All **26 Python native-format/packing/parser tests** pass. The catalogue and artwork importers reproduce their exact outputs; all 1,380 independent full-SCTX witness compositions reproduce. TypeScript, scoped formatting and production build pass.
- **1,380 source compositions per engine pass in thirteen framebuffer-bounded strips**. These cover every Castle root pattern and terminal frame, treasury controls/glints, bases/construction/scaffolds/ruins, both Dragon wing cycles in all three views, mirrored locator controls, and all Balloon glow/attack/death frames. Maximum per-case mean channel errors are **0.619167 Chromium / 0.834926 WebKit**. Both engines retain 30 pixels above 16/255, all within 0.001856 pixels of an original triangle edge. All GL checks are zero; forced single-texture batching and context restoration change zero bytes. The original thresholds remain unchanged.
- The corrected Cannon/Mortar source checks above add **1,842 compositions per engine**, for **3,222 complete comparisons and 29 distinct strip scenarios per engine**. Chromium additionally reruns the two troop strips after strengthening the Dragon additive-group assertion. The initial oversize captures and failures remain in the logs; they are not counted as complete coverage.
- All 44 assets decode and match filesystem SHA-256 in both production browsers; Chromium returns identical results offline. General production checks pass both engines, including Chromium offline reload, Army and replay. The build precaches **571 files**, cache **`crown-clan-9df5f92a2539`**. Castle tier portraits, source sleep markers, and actual WebKit Dragon/Balloon animation captures were visually reviewed.

The source catalogue was committed separately from the capture correction and artwork. Runtime stays unchanged from `1c72c636541cd0a52aa76a6580f5ff1be10562ef`: **141 TypeScript/CSS files**, SHA-256 **`65520cd44e286dec1c0627992b4b598ad08d7a3daa4ef52aa4546adec907632a`**. Combat version **36**, save version **4**, home limits and native campaign gates are intact. Frozen evidence is indexed by `output/playtest/native-garrison-foundation-verification.json`, with raw copies under `garrison-foundation-evidence`. Earlier frozen records and historical replay fixtures remain untouched.

Next integration includes Castle targeting and spell immunity, eligible garrison exits, source troop damage/splash/death effects, finite rosters, original combat effects/audio, animation state/clock handling, attacker retaliation and replay compatibility. Home Castle progression, donations/clans, later campaign families, native executable equivalence, physical-device/WebKit-offline qualification and the broader production-clone goal remain open.

## Town Hall 9 home tier — September 14, 2026

The home village now ends at **Town Hall 9**. The tier unlocks the X-Bow as a buildable home defense (two, levels 1–3), raises the Archer Tower, Cannon, Air Defense, Wizard Tower, Hidden Tesla, Mortar, Air Sweeper, Bomb Tower, Wall, trap, Army Camp, Spell Factory, Barracks, Laboratory, Dark Elixir Drill and Dark Elixir Storage ceilings, and takes the Barbarian King to level 30 behind a Hero Hall 3. Army housing reaches 220 and spell housing reaches 8. See [TOWNHALL-TIERS.md](TOWNHALL-TIERS.md).

Tier data is no longer a wiki transcription. `reference/townhall/catalog.json` now pins the client's own tables — `townhall_levels.csv`, `buildings.csv`, `traps.csv`, `heroes.csv` and `special_abilities.csv` from bundle `7f04bdfdc4124b1f49308423bb8f4aa8b137aae3` — with `scripts/import-native-townhall.py --check` re-downloading them, verifying every SHA-256 and reproducing the JSON byte for byte. Three different inheritance rules apply across those tables and are expanded and asserted rather than assumed. The pinned rows agree with the existing Town Hall 1–8 column for every entity the catalog gates from source, so this tier only added a ninth column. The King's twenty existing records also reproduce exactly, which independently confirms the destination-level reading convention before levels 21–30 were transcribed.

Three source allowances are deliberately withheld and asserted to be strictly below the original: the home Clan Castle (unimplemented), Blacksmith 2 (unlocks no equipment before Blacksmith 3 at Town Hall 10) and Town Hall 10 itself. The Town Hall's own hitpoints, price and duration keep their local curve; adopting the pinned row would rewrite the Town Hall 1–8 economy and every recorded battle built on it.

Recording version **45** carries the new ceilings. No combat rule changed and versions 34–44 remain playable, but an earlier recording still cannot contain a Barracks 11, Laboratory 7, Hero Hall 3, a King above 20 or a Town Hall above 8; versions below 40 keep the Dark Elixir Drill at level 3. Save version 4 is unchanged and nothing is granted retroactively.

Validation:

- **1,836 model/asset cases across 211 files; 1,823 pass.** The 13 failures are the pre-existing heavy fixture suites — `air-sweeper-historical`, `archer-tower-v41-historical`, `garrison-historical`, `inferno-historical`, `mortar-historical`, `native-mortar-reference` and the 288-battle `campaign-audit`. They exceed their own per-test timeouts on this host and **fail identically on the unmodified tree**, which was re-run to confirm. They are not regressions and this milestone therefore does not certify them.
- The new `tests/townhall-9.test.ts` contributes **10 cases**. Rather than restating the tier, it checks the whole ninth column against `reference/townhall/catalog.json`: every count, every ceiling, monotonic tiers, nine-row tables, and the three withheld gaps. It then exercises the tier as a village — the Town Hall 8 → 9 upgrade and its intact building list, X-Bow construction, the two-tower count gate, the stored targeting mode and both paid upgrades, the Hero Hall 3 path to King 21, army and spell housing, affordability of every purchase within Town Hall 9 storage, shipped artwork for every reachable level, and the version-45 recording boundary against a real version-44 rejection.
- `tests/king-combat.test.ts` compares all thirty King records to the pinned catalog. The Air Sweeper, Bomb Tower, Cannon, Archer Tower, Hidden Tesla, Dark Elixir Storage, Skeleton Trap, Seeking Air Mine, Army Camp, Wall, Clan Castle and army-facility suites each gained their ninth tier entry, and the facility suite now buys Barracks 11, Laboratory 7 and Spell Factory 4 at their exact source prices and timers.
- TypeScript, scoped Prettier formatting over the changed files and the production build pass. The build precaches **861 files**, cache `crown-clan-bcabc43cc43d`.

**Browser scenarios, production smoke checks and campaign audits were not re-run for this tier.** `tests/browser/heroes.spec.ts` was updated to expect nine progression tiers but has not been executed here; the X-Bow shop-gate scenario still starts from a Town Hall 2 village and is unaffected. Physical-device qualification, WebKit offline and the full production-clone goal remain open, as does the Archer Queen that the original Town Hall 9 unlocks alongside Hero Hall 3.

## The home village ladder to Town Hall 18 — September 14, 2026

The home village now runs to **Town Hall 18**, the last tier the pinned client defines, and the six late defense families fight in it. See [TOWNHALL-TIERS.md](TOWNHALL-TIERS.md).

`reference/townhall/catalog.json` grew from the tier columns alone to the complete home catalog: 18 tiers, **37 gated entities, 296 per-level rows across 24 tabled entities and all 110 Barbarian King records**, still reproduced byte for byte by `scripts/import-native-townhall.py --check` against five signed sources.

`src/game/tiers.ts` now derives both tier columns from that reference instead of transcribing them. Two rules produce the whole ladder: a tier's count is the original count except for six buildings this game hands out differently before Town Hall 9, and a tier's ceiling is the original gate capped by what this game implements, zero wherever the tier permits none of that building. Applying them **reproduces every Town Hall 1–9 value that shipped before**, verified against the previous revision's own arrays including its count-normalisation pass; the single intended change is the Town Hall's own ceiling.

Army Camp, Wall, Barracks, Laboratory, Spell Factory, Air Defense and the five trap tables now read the same reference. They already matched it exactly — checked row by row before the switch — so the only value correction is the Bomb's level-4 build time, 40 minutes locally against 30 in the source. Upgrade prices and durations are original for every home building. The prototype curve they replace multiplied cost by 1.85 per level while storage grew by 1.5, so it outgrew what a village could hold and stalled the ladder around Town Hall 13. The Inferno Tower had been falling through to that curve because it was missing from the audited defense lookup.

The Inferno Tower (TH10), Eagle Artillery and Tornado Trap (TH11), Scattershot (TH13), Monolith and Spell Tower (TH15) are now buildable at home, and home and practice battles step late-family state as campaign battles do. Campaign-only identities stay campaign-only, and the armed Builder's Hut turret remains campaign-only, so the home hut is held at level 1. A home Spell Tower is placed carrying Rage and cycles all three original weapons.

Validation:

- **1,837 model/asset cases across 211 files; 1,828 pass.** The nine failures are the pre-existing heavy fixture suites — `air-sweeper-historical`, `campaign-audit`, `garrison-historical`, `inferno-historical`, `mortar-historical` and `native-mortar-reference` — which exceed their own per-test timeouts on this host and **fail identically on the unmodified tree**. They are not regressions and are not certified here.
- `tests/townhall-tiers.test.ts` replaces the Town Hall 9 suite with **11 cases** checking the ladder against the catalog rather than a transcript: every count and ceiling at all 18 tiers, monotonic columns, a zero ceiling wherever a tier permits none of a building, nothing above what the game implements, the four withheld gaps proven strictly below the source, the original requirement for a spread of levels, army and spell housing, hero caps, affordability of every purchase within its own tier's storage, and shipped artwork for every reachable level. It plays the ladder too: the Town Hall 8 → 9 → 18 climb, X-Bow construction and its count gate, the Hero Hall 3 path to King 21, all six late families built and fought in a practice battle, the Spell Tower weapon cycle, and the version-46 boundary against real version-44 and version-45 rejections.
- `tests/king-combat.test.ts` compares all 110 King records to the catalog. The facility gate suite now derives its own expectations from the pinned gate column instead of a nine-entry literal. The Archer Tower, Dark Elixir Drill, Builder's Hut, late-campaign-level and trap suites were updated to the new ceilings.
- Recording version **46** carries the ladder with no combat rule change. Versions 34–45 keep their own limits: a version-45 recording still cannot hold a Town Hall above 9, an Archer Tower above 12, a Wall above 16, a King above 30, or a late family outside a campaign village.
- TypeScript, scoped Prettier formatting over the changed files, the importer check and the production build pass. The build precaches **861 files**, cache `crown-clan-f328b153dbee`.

One test defect was found and fixed while writing this: the late-family village case placed buildings across multi-day build timers, and tree regrowth during those ticks intermittently occupied the Monolith's tile. The village is now cleared before each placement.

**Browser scenarios, production smoke checks and campaign audits were not run for this ladder.** The late families have never been rendered or fought in a home village in a real browser; only the model-level simulation is covered here. Their Info panels, shop tiles and context controls at the new tiers, the Spell Tower selector's appearance, and the roughly 40 MB of late artwork loading from a home village are all unverified in a browser. Hitpoints and storage capacity remain this game's own values, the troop and spell rosters still stop at five levels each, and the Archer Queen, the later hero roster, Dark Barracks, siege machines and the Town Hall's own Giga weapons remain unimplemented. Physical-device qualification, WebKit offline and the full production-clone goal remain open.
