# Original client source archive

All 9,075 files in the pinned Supercell client 18.400.21 fingerprint are stored in `files/` (2,030,883,678 bytes). The raw archive is intentionally outside the public game bundle and ignored by Git. `fingerprint.json` is retained here; the complete checksummed manifest and rebuild instructions are in [reference/full-client/README.md](../../../reference/full-client/README.md).

Recreate or verify the archive with `scripts/import-native-full-client.py` (`--check` verifies it). Artwork and audio belong to Supercell; this is a public-client source snapshot, not generated artwork or an endorsement.
